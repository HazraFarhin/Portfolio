import type { RefObject } from 'react';

interface ScrollRevealOptions {
  y?: number;
  duration?: number;
  start?: string;
}

/**
 * Stub -- not yet implemented (RED phase of TDD cycle for 01-05 Task 1).
 */
export function useScrollReveal(
  _ref: RefObject<HTMLElement>,
  _options: ScrollRevealOptions = {}
): void {
  // intentionally empty
}
