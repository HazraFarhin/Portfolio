---
phase: 04-contact-form-deployment-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements: [CONT-01]

must_haves:
  truths:
    - "resend is listed in package.json dependencies and resolvable via `npm ls resend` with no invalid/missing markers, only after human legitimacy re-confirmation of its [SUS]-flagged verdict (T-04-SC, Package Legitimacy Gate)."
  artifacts:
    - package.json
    - package-lock.json
  key_links:
    - "resend installed here -> imported by Plan 04-02's api/contact.ts to call the Resend API"
---

<objective>
Install the `resend` npm SDK after a human re-confirms its [SUS]-flagged legitimacy verdict from 04-RESEARCH.md's Package Legitimacy Audit, unblocking Plan 04-02's serverless function.

Purpose: 04-RESEARCH.md's Package Legitimacy Audit flags `resend` [SUS] on a "too-new" heuristic (latest version published 6 days before research) despite 9M+ weekly downloads and a verifiable `resend/resend-node` source repo — a likely false positive, but the Package Legitimacy Gate requires human confirmation before install regardless of the research doc's own reasoning. `@vercel/node` is deliberately NOT installed this phase: 04-RESEARCH.md's Pattern 1 (Web Standard `fetch` export) needs zero extra dependencies, so `@vercel/node`'s own [SUS] flag never reaches an install step and needs no separate checkpoint.
Output: `resend` present in `package.json` dependencies and `node_modules`, ready for Plan 04-02 to import. This install exists because CONTEXT.md D-01 locks the email-delivery provider to Resend (not Formspree), reusing the `RESEND_API_KEY` value that already exists in this repo's local `.env` rather than introducing a second, unused third-party service.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contact-form-deployment-hardening/04-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Re-confirm resend package legitimacy before install</name>
  <files>none — no code changes in this task</files>
  <read_first>.planning/phases/04-contact-form-deployment-hardening/04-RESEARCH.md (Package Legitimacy Audit section, lines 113-123)</read_first>
  <acceptance_criteria>
    <behavior>Human has explicitly typed "approved" (or equivalent affirmative) before Task 2's `npm install resend` runs — no auto-advance past this gate.</behavior>
  </acceptance_criteria>
  <action>Checkpoint task — the executor makes no code changes here. Pause and present the legitimacy evidence below to the human; do not run `npm install` until the human replies with an explicit approval. The human performs the checks in &lt;how-to-verify&gt; and confirms via &lt;resume-signal&gt;.</action>
  <what-built>Nothing installed yet. 04-RESEARCH.md's Package Legitimacy Audit table flags `resend` with a raw verdict of [SUS] ("too-new" heuristic — its latest version published 2026-07-28, 6 days before research ran). The audit's own note explains this is a likely false positive: `resend` has 9,082,525 weekly downloads and a long-established, verifiable GitHub source repo (`github.com/resend/resend-node`). This checkpoint exists because the Package Legitimacy Gate requires human confirmation for every [SUS]-flagged package before its install task runs, regardless of the research doc's own reasoning. Note: `@vercel/node` was also flagged [SUS] in the same audit but is deliberately not being installed this phase (Pattern 1's dependency-free Web Standard `fetch` export is used instead), so it needs no checkpoint of its own.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/resend and confirm: weekly download count is in the millions, and the "Repository" link points to github.com/resend/resend-node.
    2. Confirm the package is the official Resend Node.js SDK (matches CONTEXT.md D-01's locked decision to use Resend for email delivery).
    3. Confirm you are comfortable installing `resend` as a runtime dependency used server-side only (inside a Vercel Function, never bundled into the client) to call the Resend transactional email API.
  </how-to-verify>
  <verify>
    <automated>MISSING — this is a human legitimacy-confirmation checkpoint with no automatable postcondition of its own; Task 2's `npm ls resend` is the automated proof that the approved install occurred.</automated>
  </verify>
  <resume-signal>Type "approved" to proceed with the install in Task 2, or describe any concern that should block it.</resume-signal>
  <done>Human has explicitly approved installing `resend` after reviewing its npm registry page and source repo.</done>
</task>

<task type="auto">
  <name>Task 2: Install resend</name>
  <files>package.json, package-lock.json</files>
  <read_first>package.json, .planning/phases/04-contact-form-deployment-hardening/04-RESEARCH.md (Standard Stack, Installation section, lines 99-103)</read_first>
  <action>Run `npm install resend` to add it as a runtime dependency. Do not install `@vercel/node` (Pattern 1's dependency-free Web Standard `fetch` export supersedes it for this phase — Plan 04-02 types against the global `Request`/`Response` already available via `tsconfig.app.json`'s `lib: ["DOM"]`). Do not install the `vercel` CLI (optional, not required — the D-06 checkpoint in Plan 04-06 deploys via GitHub import + Vercel dashboard, not local CLI). Do not pin an exact version manually beyond what npm resolves — 6.18.1 or a compatible newer patch is expected.</action>
  <acceptance_criteria>
    <source>package.json "dependencies" includes "resend"</source>
    <cli>`npm ls resend` exits 0 and prints it installed with no "invalid"/"missing" markers</cli>
    <cli>`npm ls @vercel/node` reports it as NOT installed (not present in the tree) — confirms Pattern 1 was followed and no unneeded dependency was added</cli>
  </acceptance_criteria>
  <action_verify_note>N/A</action_verify_note>
  <verify>
    <automated>npm ls resend</automated>
  </verify>
  <done>package.json/package-lock.json updated with `resend` in dependencies; `npm ls resend` reports it installed; no other dependency in package.json is removed or downgraded; `@vercel/node` is not installed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry → local node_modules → Vercel build pipeline | Supply-chain trust boundary crossed at install time; this plan's sole purpose is to cross it safely for one new package |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-SC | Tampering | `npm install resend` | high | mitigate | 04-RESEARCH.md's Package Legitimacy Audit ran before this plan existed; the [SUS]-flagged `resend` package gets a blocking `checkpoint:human-verify` (Task 1) before install, never auto-approved regardless of `workflow.auto_advance`. `@vercel/node` (also [SUS]-flagged) is not installed this phase, so it carries no residual risk. |

</threat_model>

<verification>
`npm ls resend` reports it installed; `npm run build` still completes (nothing yet imports `resend` outside `api/`, so this only confirms the install didn't corrupt the existing build).
</verification>

<success_criteria>
Human has explicitly confirmed `resend`'s legitimacy; the package is present in `package.json`/`node_modules`, ready for Plan 04-02 to import inside `api/contact.ts`.
</success_criteria>

<output>
Create `.planning/phases/04-contact-form-deployment-hardening/04-01-SUMMARY.md` when done
</output>
