---
phase: 04-contact-form-deployment-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/SelectedWork.tsx
  - src/components/home/SelectedWork.test.tsx
  - src/router.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DEPL-03]

must_haves:
  truths:
    - "Selected Work's grid always renders exactly the 6 real case studies with no expand/collapse state — the \"see more\" toggle and its 5-item conditional branch are deleted entirely, not hidden (D-07/D-08)."
    - "REQUIREMENTS.md marks HOME-04 as superseded by DEPL-03 for v1, documenting that the \"see more\" expansion is intentionally removed rather than silently left checked off (CONTEXT.md D-08)."
  prohibitions:
    - "MUST NOT render any link (button or anchor) to any of the 5 deferred slugs (riyaah, icici-bank-atm-kiosk, ambit, northernarc, citrus) anywhere on the homepage (D-07)."
  artifacts:
    - src/components/home/SelectedWork.tsx
    - src/components/home/SelectedWork.test.tsx
  key_links:
    - "SelectedWork.tsx's removed deferred-slug links -> Plan 04-05's robots.txt/sitemap.xml independently ensure crawler exclusion even though no link exists"
    - "src/router.tsx's DEFERRED_SLUG_ROUTES stays registered (unchanged behavior) -> src/routes/coming-soon.tsx remains reachable by direct URL only (D-07)"
---

<objective>
Remove the "see more" toggle and all 5 deferred-slug case-study links from the homepage's Selected Work grid (D-07/D-08), and document in REQUIREMENTS.md that this intentionally supersedes HOME-04 for v1 (D-08's required documentation task) — closing the DEPL-03 gap Phase 3 knowingly left open.

Purpose: Phase 3's Selected Work section (03-02-PLAN.md) shipped the 5 deferred case studies behind a persistent "See more" toggle as a deliberate, temporary conflict with DEPL-03 ("not linked from any page"). STATE.md flags this as a Phase 4 MUST-DO. CONTEXT.md D-08 explicitly accepts that HOME-04 ("reveal the remaining 5 via a see more expansion") is no longer literally satisfied once those links are removed — REQUIREMENTS.md must record this, not silently stay checked off. `src/router.tsx`'s `DEFERRED_SLUG_ROUTES` stay registered per D-07 so no stray/cached link 404s; only the homepage's inbound links to them are removed.
Output: Simplified `src/components/home/SelectedWork.tsx` (6-card grid, no toggle), rewritten `src/components/home/SelectedWork.test.tsx`, an updated stale comment in `src/router.tsx`, and REQUIREMENTS.md recording HOME-04 as superseded by DEPL-03.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contact-form-deployment-hardening/04-CONTEXT.md
@.planning/phases/04-contact-form-deployment-hardening/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove the "see more" toggle and deferred-slug cards from SelectedWork.tsx</name>
  <files>src/components/home/SelectedWork.tsx, src/components/home/SelectedWork.test.tsx</files>
  <read_first>src/components/home/SelectedWork.tsx, src/components/home/SelectedWork.test.tsx, src/content/selected-work.ts</read_first>
  <behavior>
    - Test: renders exactly 6 case-study cards, in `caseStudies` order (keep the existing test at SelectedWork.test.tsx lines 16-28 unchanged — it already validates this).
    - Test: `screen.queryByRole('button', { name: /see more/i })` is null — no toggle button of any kind renders.
    - Test: `screen.queryByText('Coming soon')` is null — no deferred-slug "Coming soon" cards ever render (no expand action exists to reveal them).
    - Test: none of the 5 deferred slugs (`riyaah`, `icici-bank-atm-kiosk`, `ambit`, `northernarc`, `citrus`) appear in any rendered `href` attribute — assert via `screen.getAllByRole('link').every(...)` or equivalent.
    - Test: keep the existing "never renders any superseded fictional project title" guard (lines 81-86) unchanged — still valid post-change.
  </behavior>
  <action>In `src/components/home/SelectedWork.tsx`: remove the `useState` import (becomes unused) and the `deferredCaseStudies` import from `../../content/case-studies/deferred` (line 7). Delete the `const [expanded, setExpanded] = useState(false)` line (line 23). Delete the `{expanded && deferredCaseStudies.map(...)}` block (lines 44-54). Delete the `<Button variant="ghost" onClick={...} aria-expanded={expanded}>{expanded ? ... : ...}</Button>` toggle block (lines 57-63). Leave the real `{caseStudies.map((cs) => (...))}` grid (lines 32-42) untouched — this becomes the final, permanent rendering with no expand/collapse state. Update the file's top-of-file doc comment (lines 11-17) to remove the now-stale "appends the 5 deferred coming-soon stubs... behind a persistent see more toggle" description, replacing it with a short note that the section renders only the 6 real case studies per D-07/D-08. In `src/components/home/SelectedWork.test.tsx`: delete the 2nd through 5th `it()` blocks (lines 30-79, the toggle/expand/collapse/label assertions) and replace them with the 3 new assertions from the behavior block above (no toggle button, no "Coming soon" text, no deferred-slug hrefs); keep the 1st test (lines 16-28) and the 6th test (lines 81-86) exactly as-is. Remove the now-unused `deferredCaseStudies` import and `fireEvent` import if no longer used by any remaining test (re-check after edits — if no `fireEvent.click` calls remain, drop the import to satisfy `noUnusedLocals`).</action>
  <acceptance_criteria>
    <source>SelectedWork.tsx contains no import of `deferredCaseStudies` and no `useState` call</source>
    <source>SelectedWork.tsx contains no `aria-expanded` attribute and no reference to `selectedWorkContent.seeMoreLabel`/`seeLessLabel`</source>
    <behavior>rendering `<SelectedWork />` produces exactly 6 links matching `/View case study/` and zero elements matching role "button" with an accessible name containing "see more" (case-insensitive)</behavior>
    <cli>`npx vitest run src/components/home/SelectedWork.test.tsx` exits 0</cli>
    <cli>`npm run build` exits 0 (confirms no unused-import lint/type errors from the removed useState/deferredCaseStudies imports)</cli>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/home/SelectedWork.test.tsx</automated>
  </verify>
  <done>SelectedWork.tsx renders only the 6 real case studies with no toggle/expand state; SelectedWork.test.tsx asserts the toggle and deferred links are absent; both `npx vitest run` and `npm run build` pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update the stale D-07 comment in router.tsx</name>
  <files>src/router.tsx</files>
  <read_first>src/router.tsx</read_first>
  <action>Replace the comment block at lines 19-21 (currently reading "D-11: Phase 4 MUST delete/guard this whole block as part of its own DEPL-03 work (deferred slugs must not be publicly linked/indexed once real content lands).") with an updated comment reflecting the D-07 resolution: the block is intentionally kept as-is per Phase 4 CONTEXT.md D-07 — the routes themselves stay registered so a stray/cached link to `/case-study/riyaah` etc. resolves to the coming-soon page instead of a 404; only the homepage's inbound links to these routes were removed (Plan 04-04). Do not change `DEFERRED_SLUGS`, `DEFERRED_SLUG_ROUTES`, or any other line in this file — this task is a comment-only edit.</action>
  <acceptance_criteria>
    <source>router.tsx no longer contains the text "Phase 4 MUST delete/guard this whole block"</source>
    <source>router.tsx's DEFERRED_SLUGS array and DEFERRED_SLUG_ROUTES logic are byte-identical to before this task (comment-only change)</source>
    <cli>`npm run build` exits 0</cli>
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The stale "Phase 4 MUST delete/guard" comment is replaced with an accurate description of the D-07 resolution; no functional code changed.</done>
</task>

<task type="auto">
  <name>Task 3: Mark HOME-04 as superseded by DEPL-03 in REQUIREMENTS.md (D-08)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <action>In the "Homepage" section, change the HOME-04 line from `- [x] **HOME-04**: User can reveal the remaining 5 case studies (riyaah, icici-bank-atm-kiosk, ambit, northernarc, citrus) via a "see more" expansion` to `- [x] **HOME-04**: ~~User can reveal the remaining 5 case studies (riyaah, icici-bank-atm-kiosk, ambit, northernarc, citrus) via a "see more" expansion~~ — **Superseded by DEPL-03** (Phase 4, CONTEXT.md D-08): the "see more" expansion was removed entirely; the 5 deferred case studies are intentionally not linked from any page.` In the "Traceability" table, change the row `| HOME-04 | Phase 3 | Complete |` to `| HOME-04 | Phase 3 | Complete (superseded by DEPL-03) |`. Make no other changes to this file.</action>
  <acceptance_criteria>
    <source>REQUIREMENTS.md's HOME-04 line contains the literal substring "Superseded by DEPL-03"</source>
    <source>REQUIREMENTS.md's Traceability table HOME-04 row contains the literal substring "superseded by DEPL-03"</source>
  </acceptance_criteria>
  <verify>
    <automated>grep -ic "superseded by DEPL-03" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md's HOME-04 entry and its Traceability table row both explicitly record it as superseded by DEPL-03, per D-08 — not silently left checked off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Homepage (public) → deferred case-study routes | No new trust boundary crossed — this plan only removes outbound links; the routes themselves remain reachable and unchanged from Phase 3 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-06 | Information Disclosure | `/case-study/<deferred-slug>` routes remaining registered and reachable by direct URL | low | accept | Deliberate per D-07 — a stray/cached link should resolve to a "coming soon" page, not a 404; the routes render only placeholder copy (no fabricated case-study content, per `deferred.ts`'s existing accepted-risk note), so reachability itself discloses nothing sensitive |

</threat_model>

<verification>
`npx vitest run src/components/home/SelectedWork.test.tsx` passes; `npm run build` passes; `grep -ic "superseded by DEPL-03" .planning/REQUIREMENTS.md` returns 2 (one in the requirement line, one in the Traceability table).
</verification>

<success_criteria>
Selected Work renders only the 6 shipped case studies with no path to any deferred slug; REQUIREMENTS.md documents HOME-04 as superseded rather than silently left checked off.
</success_criteria>

<output>
Create `.planning/phases/04-contact-form-deployment-hardening/04-04-SUMMARY.md` when done
</output>
