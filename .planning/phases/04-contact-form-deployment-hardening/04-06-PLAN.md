---
phase: 04-contact-form-deployment-hardening
plan: 06
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04", "04-05"]
files_modified:
  - public/robots.txt
  - public/sitemap.xml
autonomous: false
requirements: [CONT-01, CONT-03, DEPL-01, DEPL-02, DEPL-03]
user_setup:
  - service: vercel
    why: "Public deployment hosting (DEPL-01) and serverless function runtime for api/contact.ts (CONT-01) — D-04/D-06"
    env_vars:
      - name: RESEND_API_KEY
        source: "Copy the existing value directly from this repo's local .env file (never retype/regenerate it)"
    dashboard_config:
      - task: "Import this repo's GitHub remote as a new Vercel project (accept auto-detected Vite build settings, do not override buildCommand/outputDirectory)"
        location: "Vercel Dashboard -> Add New... -> Project"
      - task: "Add RESEND_API_KEY as a Project Environment Variable (no VITE_ prefix)"
        location: "Vercel Dashboard -> Project -> Settings -> Environment Variables"

must_haves:
  truths:
    - "Deployment reachability is verified end-to-end against the live *.vercel.app URL per 04-VALIDATION.md's manual-only verification row, not just a local build check."
    - "Résumé download is a native browser <a href download> action against a static PDF file; the browser's own download manager handles concurrent/repeated clicks, no custom concurrency handling is required."
    - "Submitting the deployed contact form delivers a real, human-verified email to hazrafarhinwork@gmail.com's inbox (CONT-01), confirmed against the live inbox, not just a 200 response."
    - "Directly loading and refreshing /case-study/mashreq on the live deployed URL renders the case study without a Vercel 404 (DEPL-02)."
    - "public/sitemap.xml and public/robots.txt are backfilled with the real confirmed *.vercel.app domain (replacing the REPLACE_WITH_DEPLOYED_DOMAIN placeholder from Plan 04-05) and re-verified live."
  prohibitions:
    - "MUST NOT commit RESEND_API_KEY's value anywhere in the repository (source, .env.example, commit message, or this plan's SUMMARY) — it is entered only via the Vercel dashboard's Project Settings environment variables."
  artifacts: []
  key_links:
    - "RESEND_API_KEY from local .env -> manually copied into Vercel Project Settings env var, consumed by Plan 04-02's api/contact.ts at runtime"
    - "GitHub repo import -> Vercel auto-builds via `npm run build` (tsc -b && vite build), auto-detected per 04-RESEARCH.md Common Pitfalls 3"
---

<objective>
Connect this repo's GitHub remote to a Vercel account, deploy, add `RESEND_API_KEY` as a Vercel project environment variable, then verify every phase-4 requirement end-to-end against the live `*.vercel.app` URL — not local dev, not just a 200 response (D-06; ROADMAP.md's explicit standard for this phase).

Purpose: No Vercel account is connected to this repo's GitHub remote yet (04-RESEARCH.md Environment Availability confirms this has no code-only fallback). This is the same checkpoint shape 03-05-PLAN.md used for the résumé-conversion/social-URL gap: pause execution, give explicit manual steps, resume and verify once done. It is intentionally the last plan in this phase — it depends on Plan 04-02 (api/contact.ts + vercel.json), Plan 04-03 (Footer wiring), Plan 04-04 (deferred-link removal), and Plan 04-05 (robots.txt/sitemap.xml) all being complete, since it deploys and verifies the whole built site.
Output: A live, publicly reachable Vercel deployment with `RESEND_API_KEY` configured, and human-confirmed verification of CONT-01 (live email delivery), CONT-02 (visual success/error states), CONT-03 (résumé download), DEPL-01 (public reachability), and DEPL-02 (deep-link refresh). `public/robots.txt`/`public/sitemap.xml` backfilled with the real domain. Per CONTEXT.md D-13, `public/resume.pdf` is already the real, converted résumé from Phase 3 — this plan's CONT-03 work is verification only (click the link, confirm a working PDF downloads against the live deployed site), not a rebuild.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contact-form-deployment-hardening/04-CONTEXT.md
@.planning/phases/04-contact-form-deployment-hardening/04-RESEARCH.md
@.planning/phases/04-contact-form-deployment-hardening/04-VALIDATION.md
@.planning/phases/03-homepage-build/03-05-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Connect Vercel, deploy, and add RESEND_API_KEY env var (D-06)</name>
  <files>none — this task writes no repo files; it configures external Vercel project state</files>
  <read_first>.env (confirm RESEND_API_KEY exists locally, do not print its value), .planning/phases/04-contact-form-deployment-hardening/04-RESEARCH.md (Common Pitfalls 1 and 3, lines 312-328)</read_first>
  <acceptance_criteria>
    <behavior>A Vercel project exists, is connected to this repo's GitHub remote, has completed at least one successful deployment, and has RESEND_API_KEY set as a Project Environment Variable (not prefixed VITE_)</behavior>
  </acceptance_criteria>
  <action>Checkpoint task — the executor makes no code changes here. Pause and hand off to the human: connecting a GitHub repo to a Vercel account and setting a project environment variable requires the Vercel web dashboard (or an already-authenticated `vercel` CLI session neither installed nor pre-authenticated in this environment per 04-RESEARCH.md's Environment Availability table) — this cannot be automated from inside the repo. The human performs the steps in &lt;how-to-verify&gt; and confirms via &lt;resume-signal&gt;.</action>
  <what-built>Nothing automatable here — no Vercel account/GitHub connection exists yet (confirmed not connected per CONTEXT.md D-06), and creating one requires human interaction with the Vercel dashboard (account creation/login, GitHub OAuth authorization, environment-variable entry) that has no CLI/API path available without prior authentication in this environment.</what-built>
  <how-to-verify>
    1. Go to https://vercel.com and sign in (create an account if needed), then click "Add New... → Project".
    2. Import this repo's GitHub remote. Vercel should auto-detect the Vite framework preset (confirm build command shows `npm run build` / similar, output directory `dist` — do NOT override these, per 04-RESEARCH.md Pitfall 3).
    3. Before or immediately after the first deploy, go to Project Settings → Environment Variables and add a variable named exactly `RESEND_API_KEY` (no `VITE_` prefix) with the value from this repo's local `.env` file — copy it directly from `.env`, never type/paste it into any commit, chat message, or planning document.
    4. Trigger (or wait for) a deployment to complete. Note the resulting `*.vercel.app` URL — you will need it for Task 2.
    5. Confirm in the Vercel dashboard's deployment log that the build succeeded with no errors.
  </how-to-verify>
  <verify>
    <automated>MISSING — this is a human-only external-service checkpoint (Vercel account/GitHub connection) with no automatable postcondition from inside this repo; Task 2's live HTTP checks against the resulting URL are the automated proof that this step succeeded.</automated>
  </verify>
  <resume-signal>Reply with the live *.vercel.app URL once the deployment succeeds and RESEND_API_KEY is set, or describe any issue.</resume-signal>
  <done>A Vercel project is connected to this repo's GitHub remote, has at least one successful deployment, RESEND_API_KEY is set as a non-VITE_-prefixed Project Environment Variable, and the human has supplied the resulting live URL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Backfill sitemap/robots domain and verify all Phase 4 requirements live</name>
  <files>public/robots.txt, public/sitemap.xml</files>
  <read_first>public/robots.txt, public/sitemap.xml, .planning/phases/04-contact-form-deployment-hardening/04-VALIDATION.md (Manual-Only Verifications table, lines 67-75)</read_first>
  <acceptance_criteria>
    <behavior>public/robots.txt and public/sitemap.xml contain zero remaining occurrences of the literal "REPLACE_WITH_DEPLOYED_DOMAIN" placeholder, replaced with the real confirmed *.vercel.app URL from Task 1</behavior>
    <behavior>the live URL loads the homepage successfully (DEPL-01)</behavior>
    <behavior>a direct browser navigation + hard refresh on /case-study/mashreq on the live URL renders the case study, not a 404 (DEPL-02)</behavior>
    <behavior>submitting the live contact form with real values results in an email arriving at hazrafarhinwork@gmail.com (CONT-01), and the page shows the "Brief received." success state (CONT-02)</behavior>
    <behavior>clicking the résumé link on the live site downloads a valid, openable PDF (CONT-03)</behavior>
    <behavior>none of the 5 deferred slugs are reachable via any on-page link on the live site, and /robots.txt + /sitemap.xml both serve correctly at the live URL with the real domain and no deferred slugs present (DEPL-03)</behavior>
  </acceptance_criteria>
  <action>First, replace every occurrence of the literal `REPLACE_WITH_DEPLOYED_DOMAIN` in `public/robots.txt` and `public/sitemap.xml` with the real `*.vercel.app` domain confirmed in Task 1 (e.g. if the URL is `https://hazra-farhin-portfolio.vercel.app`, the domain segment is `hazra-farhin-portfolio.vercel.app`). Commit and redeploy (push to the connected GitHub branch, or trigger a redeploy from the Vercel dashboard) so the live robots.txt/sitemap.xml reflect the real domain. Then hand off to the human for the &lt;how-to-verify&gt; steps below — none of these can be simulated in Vitest/jsdom (04-VALIDATION.md's Manual-Only Verifications table: live inbox check, live PDF download, live deployment reachability, live routing layer behavior).</action>
  <what-built>vercel.json (Plan 04-02), api/contact.ts (Plan 04-02), the wired Footer.tsx submit flow (Plan 04-03), the deferred-link-free Selected Work grid (Plan 04-04), and robots.txt/sitemap.xml with the real domain now backfilled (this task) — the full set of Phase 4 changes, now live at the confirmed Vercel URL from Task 1.</what-built>
  <how-to-verify>
    1. Open the live `*.vercel.app` URL in a browser. Confirm the homepage loads (DEPL-01).
    2. Navigate directly (type the URL, don't click through the app) to `&lt;live-url&gt;/case-study/mashreq`, then hard-refresh the page. Confirm it renders the case study, not a 404 (DEPL-02).
    3. Scroll to the Brief/Footer section. Confirm Selected Work above it shows exactly 6 cards with no "See more" control, and confirm no link anywhere on the page points to `/case-study/riyaah`, `/case-study/icici-bank-atm-kiosk`, `/case-study/ambit`, `/case-study/northernarc`, or `/case-study/citrus` (DEPL-03).
    4. Visit `&lt;live-url&gt;/robots.txt` and `&lt;live-url&gt;/sitemap.xml` directly in the browser — confirm both load with the real domain (not the placeholder) and the 5 deferred slugs are absent from both (DEPL-03).
    5. Fill in and submit the live contact form with real test values. Confirm the "Brief received." success state renders (CONT-02), then check the `hazrafarhinwork@gmail.com` inbox and confirm the email arrived with the expected subject/body shape (CONT-01).
    6. Click the résumé download link. Confirm a valid PDF downloads and opens with all sections legible (CONT-03).
  </how-to-verify>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://REPLACE-WITH-LIVE-URL/case-study/mashreq — replace REPLACE-WITH-LIVE-URL with the confirmed domain from Task 1; expect 200 (executor runs this after the human supplies the URL, as a supplementary automated check alongside the manual steps above)</automated>
  </verify>
  <resume-signal>Reply "verified" once all 6 how-to-verify steps pass, or describe any failure (e.g. "no email received", "404 on refresh", "deferred slug still linked").</resume-signal>
  <done>public/robots.txt and public/sitemap.xml carry the real domain (no placeholder remains); the live URL is publicly reachable; a direct refresh on a case-study route works; the live contact form delivers a real email to hazrafarhinwork@gmail.com with visible success feedback; the résumé PDF downloads correctly; no deferred slug is linked or present in the live robots.txt/sitemap.xml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local .env → Vercel Project Settings (human-operated, out-of-band) | RESEND_API_KEY crosses from a gitignored local file into Vercel's secret store via manual human entry — never via a committed file or CLI argument that could be logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-08 | Information Disclosure | RESEND_API_KEY entry into the Vercel dashboard | high | mitigate | Human copies the value directly from the local `.env` file into the Vercel dashboard's Environment Variables UI; this plan's own `<action>`/`<how-to-verify>` text explicitly instructs never typing/pasting the value into any commit, chat message, or planning document (this plan's prohibitions list this explicitly) |

</threat_model>

<verification>
All 6 `<how-to-verify>` steps in Task 2 pass; `public/robots.txt`/`public/sitemap.xml` contain zero remaining `REPLACE_WITH_DEPLOYED_DOMAIN` occurrences after redeploy.
</verification>

<success_criteria>
The site is live at a public Vercel URL with correct SPA routing, the contact form delivers real email end-to-end, the résumé downloads correctly, and the 5 deferred case studies are unreachable from any page and excluded from the live robots.txt/sitemap.xml — every Phase 4 ROADMAP.md success criterion verified against production, not local dev.
</success_criteria>

<output>
Create `.planning/phases/04-contact-form-deployment-hardening/04-06-SUMMARY.md` when done
</output>
