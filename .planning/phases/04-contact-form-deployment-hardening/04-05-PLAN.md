---
phase: 04-contact-form-deployment-hardening
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - public/robots.txt
  - public/sitemap.xml
  - src/test/seo.test.ts
autonomous: true
requirements: [DEPL-03]

must_haves:
  truths:
    - "The 5 deferred case-study slugs remain reachable only by direct URL (no page links them, per D-07); robots.txt disallows and sitemap.xml omits all 5, per D-07."
  artifacts:
    - public/robots.txt
    - public/sitemap.xml
    - src/test/seo.test.ts
  key_links:
    - "public/sitemap.xml's REPLACE_WITH_DEPLOYED_DOMAIN placeholder -> backfilled with the real live Vercel URL during Plan 04-06's deployment checkpoint"
---

<objective>
Create `public/robots.txt` and `public/sitemap.xml` — neither exists in the repo today — excluding the 5 deferred case-study slugs from crawler discovery/indexing (DEPL-03), plus a new `src/test/seo.test.ts` asserting the exclusion.

Purpose: 04-RESEARCH.md's Alternatives Considered recommends a hand-maintained static `sitemap.xml` over a generator library — only 7 total indexable URLs (home + 6 shipped case studies) with a fixed route list doesn't justify a new dependency. This is a second, independent enforcement layer for DEPL-03 alongside Plan 04-04's link removal: even if a stray inbound link to a deferred slug ever appeared, `robots.txt` still disallows crawling it and `sitemap.xml` still never advertises it.
Output: `public/robots.txt`, `public/sitemap.xml` (with a `REPLACE_WITH_DEPLOYED_DOMAIN` placeholder in every `<loc>`/`Sitemap:` URL, backfilled once the real domain is known in Plan 04-06 — the actual `*.vercel.app` subdomain is not knowable until that checkpoint runs, per D-05), and `src/test/seo.test.ts`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contact-form-deployment-hardening/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public/robots.txt and public/sitemap.xml</name>
  <files>public/robots.txt, public/sitemap.xml</files>
  <read_first>.planning/phases/04-contact-form-deployment-hardening/04-RESEARCH.md (Recommended Project Structure, lines 179-192; Alternatives Considered, lines 92-97)</read_first>
  <action>Create `public/robots.txt` with `User-agent: *` followed by 5 `Disallow:` lines, one per deferred slug path: `/case-study/riyaah`, `/case-study/icici-bank-atm-kiosk`, `/case-study/ambit`, `/case-study/northernarc`, `/case-study/citrus`, then a blank line and `Sitemap: https://REPLACE_WITH_DEPLOYED_DOMAIN/sitemap.xml`. Create `public/sitemap.xml` as a standard `<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">` containing exactly 7 `<url><loc>...</loc></url>` entries: the homepage (`https://REPLACE_WITH_DEPLOYED_DOMAIN/`) and the 6 shipped case-study slugs in IA order (`https://REPLACE_WITH_DEPLOYED_DOMAIN/case-study/cad`, `.../case-study/verzion-cloud-migration`, `.../case-study/tata-capital-ai-interface`, `.../case-study/mashreq`, `.../case-study/astrosure.ai`, `.../case-study/adreport.io`). Do NOT include any of the 5 deferred slugs in sitemap.xml. The literal `REPLACE_WITH_DEPLOYED_DOMAIN` placeholder is intentional — it is grep-able and gets backfilled with the real confirmed `*.vercel.app` domain by Plan 04-06's deployment checkpoint, since the actual domain is not known until that checkpoint runs (D-05).</action>
  <acceptance_criteria>
    <source>public/robots.txt contains exactly 5 "Disallow:" lines, one for each of riyaah, icici-bank-atm-kiosk, ambit, northernarc, citrus</source>
    <source>public/sitemap.xml contains exactly 7 "&lt;url&gt;" entries and zero occurrences of any of the 5 deferred slugs</source>
    <cli>`node -e "require('fs').readFileSync('public/robots.txt','utf-8')"` exits 0 (file exists and is readable)</cli>
  </acceptance_criteria>
  <verify>
    <automated>test -f public/robots.txt && test -f public/sitemap.xml</automated>
  </verify>
  <done>public/robots.txt disallows all 5 deferred slugs and references the sitemap; public/sitemap.xml lists exactly the home page + 6 shipped case-study URLs and omits all 5 deferred slugs.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/test/seo.test.ts asserting robots.txt/sitemap.xml exclusion</name>
  <files>src/test/seo.test.ts</files>
  <read_first>src/content/case-studies/loader.test.ts (Vitest describe/it/expect convention reference), public/robots.txt, public/sitemap.xml</read_first>
  <action>Create `src/test/seo.test.ts` using `describe`/`it`/`expect` imported from `'vitest'` (matching this repo's existing `*.test.ts` convention). Use Node's `fs.readFileSync` with `path.join(__dirname, '../../public/robots.txt')` and `path.join(__dirname, '../../public/sitemap.xml')` to read both files as plain text (no new package needed — `fs`/`path` are Node built-ins, already available via the `@types/node` devDependency). Assert: (1) `robots.txt` contains the substring `Disallow: /case-study/riyaah` and the equivalent `Disallow:` line for each of the other 4 deferred slugs (`icici-bank-atm-kiosk`, `ambit`, `northernarc`, `citrus`); (2) `sitemap.xml` does NOT contain any of the 5 deferred slugs as a substring; (3) `sitemap.xml` DOES contain each of the 6 shipped slugs (`cad`, `verzion-cloud-migration`, `tata-capital-ai-interface`, `mashreq`, `astrosure.ai`, `adreport.io`) as a substring. Do not assert on the exact `REPLACE_WITH_DEPLOYED_DOMAIN` placeholder value or any specific domain string — Plan 04-06 replaces it later and this test must remain valid both before and after that replacement.</action>
  <acceptance_criteria>
    <cli>`npx vitest run src/test/seo.test.ts` exits 0 with all assertions passing</cli>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/test/seo.test.ts</automated>
  </verify>
  <done>src/test/seo.test.ts passes, proving robots.txt disallows all 5 deferred slugs and sitemap.xml both omits them and includes all 6 shipped slugs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Search engine crawlers → public/robots.txt, public/sitemap.xml | Both files are static, served verbatim, read only by external crawlers — no app code or user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-07 | Information Disclosure | public/robots.txt listing deferred slug paths | low | accept | `robots.txt` Disallow entries reveal the deferred slugs' URL paths exist (a routing-layer, not content-layer, fact) — this is the standard, unavoidable tradeoff of using robots.txt to guide crawlers away from a path at all; the paths render only inert "coming soon" placeholder copy (Plan 04-04's threat T-04-06), so nothing sensitive is disclosed |

</threat_model>

<verification>
`npx vitest run src/test/seo.test.ts` passes; `public/robots.txt` and `public/sitemap.xml` both exist and are valid per Task 1's acceptance criteria.
</verification>

<success_criteria>
robots.txt and sitemap.xml both exist, both exclude all 5 deferred slugs, and an automated test proves it — independent of whether any stray link to a deferred slug ever appears elsewhere on the site.
</success_criteria>

<output>
Create `.planning/phases/04-contact-form-deployment-hardening/04-05-SUMMARY.md` when done
</output>
