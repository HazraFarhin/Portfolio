---
phase: 01-foundation-motion-infrastructure
plan: 06
type: execute
wave: 5
depends_on: ["01-04", "01-05"]
files_modified:
  - src/content/hero.ts
  - src/router.tsx
  - src/routes/home.tsx
  - src/routes/home.test.tsx
  - src/main.tsx
  - src/App.tsx
autonomous: true
requirements: [QUAL-01, QUAL-02]

must_haves:
  truths:
    - "The Hero route exhibits GSAP+Lenis-driven smooth-scroll and scroll-triggered reveal motion without breaking native keyboard navigation or scroll-to-anchor behavior"
    - "Hero eyebrow, statement, CTA label, and meta-card copy match Homepage Copy V2.md §02 verbatim, sourced from src/content/hero.ts (not hardcoded inline in JSX)"
    - "Hero statement paragraph and secondary meta card content wrap within constrained widths at every viewport down to 320px with no horizontal scroll or truncation"
    - "Primary CTA button label renders in full at all breakpoints without truncation or ellipsis"
    - "React Router's Data Mode route tree renders the home route at / and reserves the /case-study/:slug shape as a code comment for Phase 2, without requiring restructuring"
  artifacts:
    - path: "src/content/hero.ts"
      provides: "HeroContent type + heroContent data (eyebrow, statement, ctaLabel, ctaHref, metaDescription, metaStatus)"
    - path: "src/router.tsx"
      provides: "createBrowserRouter route tree (Data Mode), home route + reserved /case-study/:slug shape"
    - path: "src/routes/home.tsx"
      provides: "Home route rendering the Hero section composed from components/ui/ + useScrollReveal"
    - path: "src/main.tsx"
      provides: "App entrypoint: StrictMode -> MotionProvider -> RouterProvider"
  key_links:
    - from: "src/routes/home.tsx"
      to: "src/content/hero.ts"
      via: "imports and renders heroContent fields"
      pattern: "heroContent"
    - from: "src/routes/home.tsx"
      to: "src/motion/useScrollReveal.ts"
      via: "calls useScrollReveal(heroRef) on the Hero section's root ref"
      pattern: "useScrollReveal\\("
    - from: "src/main.tsx"
      to: "src/motion/MotionProvider.tsx"
      via: "wraps RouterProvider in MotionProvider at the app root"
      pattern: "MotionProvider"
  prohibitions:
    - requirement_id: QUAL-01
      category: safety
      status: unverified
      flagged: true
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT let any component outside src/motion/* implement its own prefers-reduced-motion/matchMedia check -- Hero and every composed UI primitive must rely exclusively on MotionProvider/useScrollReveal's shared gate, with zero duplicate matchMedia references anywhere in src/components, src/routes, or src/content."
    - requirement_id: QUAL-01
      category: values
      status: unverified
      flagged: true
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT let the Hero's scroll-reveal motion obscure or fragment the statement paragraph's legibility mid-animation (e.g. letter-by-letter or word-scramble reveal effects) -- the reveal must be a simple opacity/translate fade-in that never renders the reading content in a partially-illegible intermediate state, consistent with the project's 'clarity over decoration' core value."
---

<objective>
Wire everything the phase built into one real, working page: the Hero section success criterion #2 requires, using the exact copy from `Homepage Copy V2.md` §02 (D-05) held in a content data module (not hardcoded in JSX), composed entirely from the `components/ui/` primitives (01-04-PLAN.md) and animated via `useScrollReveal` (01-05-PLAN.md), served through a React Router Data Mode route tree that reserves the `/case-study/:slug` shape Phase 2 will add (D-11), all wrapped in `MotionProvider` (01-03-PLAN.md) at the app root. This is the phase's capstone integration -- the first point every prior plan's output is proven to work together end-to-end.

Purpose: Prove the motion system and UI primitives actually compose into a real section, not just pass isolated unit tests -- this is what D-05 means by "not a throwaway test page."
Output: A running app whose `/` route renders the Hero with real content, real motion, real primitives.
</objective>

<execution_context>
@/Users/hazrafarhin/Desktop/Repositories/Portfolio/.claude/gsd-core/workflows/execute-plan.md
@/Users/hazrafarhin/Desktop/Repositories/Portfolio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-motion-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-motion-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-motion-infrastructure/01-UI-SPEC.md
@.planning/phases/01-foundation-motion-infrastructure/01-VALIDATION.md
</context>

## Artifacts this phase produces

- `src/content/hero.ts` — exports `HeroContent` interface + `heroContent` data object
- `src/router.tsx` — exports `router` (`createBrowserRouter` Data Mode route tree)
- `src/routes/home.tsx` — exports default `HomeRoute` component (the Hero section)
- `src/main.tsx` — finalized app entrypoint (`StrictMode` -> `MotionProvider` -> `RouterProvider`)
- `src/App.tsx` — finalized root layout (`Outlet` from `react-router`)

## Flagged Assumptions (spec-less probe fallback)

The edge-coverage probe run over QUAL-01/QUAL-02 (see the orchestrator's raw `edge-probe.cjs` report) surfaced one `unclassified` item for QUAL-01 that does not fit the edge taxonomy (it is a shape/data taxonomy; QUAL-01 is a scroll-behavior requirement). Per the spec-less fallback rules, this is recorded here explicitly rather than silently dropped: **the native-scroll/keyboard/anchor-link behavior QUAL-01 actually asks about is already covered by this plan's Task 3 `<human-check>` verification below and by 01-VALIDATION.md's "Manual-Only Verifications" table** -- it is not re-derived as a separate must-have here because jsdom cannot simulate real scroll physics or OS-level keyboard scroll, so the check can only be closed by the human-check step, not an automated one.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hero content module (D-05 verbatim copy)</name>
  <files>src/content/hero.ts</files>
  <read_first>Portfolio-Documentation/Homepage Copy V2.md (section: 02 / Hero), .planning/phases/01-foundation-motion-infrastructure/01-UI-SPEC.md (section: Copywriting Contract)</read_first>
  <action>
    Create `src/content/hero.ts` exporting `export interface HeroContent { eyebrow: string; statement: string; ctaLabel: string; ctaHref: string; metaDescription: string; metaStatus: string }` and `export const heroContent: HeroContent = { ... }` populated with these exact verbatim strings from `Homepage Copy V2.md` §02 (do not paraphrase, shorten, or reword): `eyebrow: 'Hazra Farhin · UX/UI Designer'`; `statement:` the full hero-statement paragraph beginning "Hazra designs solution oriented interfaces to minimize friction for products where the stakes are real..." and ending "...a team, including the developers building it, can run without her in the room."; `ctaLabel: 'View Selected Work →'`; `ctaHref: '#hero'` (Claude's Discretion: since Phase 1 ships Hero-only per D-06, there is no "Selected Work" section yet to link to -- `#hero` is a real, currently-resolvable in-page anchor pointing at the Hero section's own `id="hero"` set in Task 2, so the CTA is a genuine, testable anchor link today rather than a dead href; Phase 3 repoints this same field to `#selected-work` once that section exists); `metaDescription: 'Product design, systems, and front-end-ready UI for teams across the USA, the Gulf, and beyond, built around user needs and business outcomes together.'`; `metaStatus: 'Remote · CET to GST overlap · Available for new work'`.
  </action>
  <acceptance_criteria>
    - `src/content/hero.ts` exports `HeroContent` and `heroContent`
    - `grep -c "Hazra designs solution oriented interfaces" src/content/hero.ts` reports 1
    - `grep -c "View Selected Work" src/content/hero.ts` reports 1
    - `grep -c "Remote · CET to GST overlap · Available for new work" src/content/hero.ts` reports 1
    - Hero copy is consumed by reference (`heroContent.*` field access), not duplicated as a separate inline string literal in any `.tsx` file (verified via the data-driven-usage check in Task 2's acceptance criteria)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Hazra designs solution oriented interfaces" src/content/hero.ts</automated>
  </verify>
  <done>heroContent exports the exact verbatim copy from Homepage Copy V2.md §02, ready for src/routes/home.tsx to consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Router (Data Mode) + Home route Hero composition</name>
  <files>src/router.tsx, src/routes/home.tsx, src/routes/home.test.tsx</files>
  <read_first>.planning/phases/01-foundation-motion-infrastructure/01-RESEARCH.md (section: Architecture Patterns -> Pattern 3), src/content/hero.ts (from Task 1), src/components/ui/Button.tsx, src/components/ui/Card.tsx, src/components/ui/Typography.tsx (from 01-04-PLAN.md), src/motion/useScrollReveal.ts (from 01-05-PLAN.md)</read_first>
  <behavior>
    - Test 1: `HomeRoute` renders the eyebrow text (`heroContent.eyebrow`), the full statement text, the CTA button with `href="#hero"` and label `heroContent.ctaLabel`, and the meta card's `metaDescription` + `metaStatus` text -- all four sourced from `heroContent`, none hardcoded as separate literal strings in the test's expectations beyond what `heroContent` itself provides.
    - Test 2: The Hero section's root element has `id="hero"`, matching the CTA's `href="#hero"` target.
    - Test 3: The rendered CTA is composed via the `Button` primitive (not a raw `<a>`/`<button>` written directly in `home.tsx`) -- assert it has `Button`'s known base classes (e.g. `rounded-full`) rather than ad-hoc styling.
    - Test 4 (overflow/long-text UI consideration): the statement paragraph's wrapping container has a max-width utility class (e.g. `max-w-[62ch]` or equivalent) and no `truncate`/`overflow-hidden` class.
  </behavior>
  <action>
    Create `src/router.tsx` exporting `router = createBrowserRouter([{ path: '/', element: <App />, children: [{ index: true, element: <HomeRoute /> }] }])` using `createBrowserRouter` imported from `'react-router'` (not `'react-router-dom'`, removed in v8). Add a code comment reserving the shape Phase 2 will add as a sibling route object: `{ path: 'case-study/:slug', element: <CaseStudyRoute /> }` (do not implement this route -- Phase 2's job). Create `src/routes/home.tsx` exporting a default `HomeRoute` component: create a `heroRef = useRef<HTMLElement>(null)`, call `useScrollReveal(heroRef)`, and render a `<section id="hero" ref={heroRef}>` containing, in order: `<Label>{heroContent.eyebrow}</Label>`, `<Body className="max-w-[62ch]">{heroContent.statement}</Body>`, `<Button href={heroContent.ctaHref}>{heroContent.ctaLabel}</Button>`, and a `<Card><Body>{heroContent.metaDescription}</Body><Label>{heroContent.metaStatus}</Label></Card>`. Import `Label`, `Body` from `'../components/ui/Typography'`, `Button` from `'../components/ui/Button'`, `Card` from `'../components/ui/Card'`, `heroContent` from `'../content/hero'`, `useScrollReveal` from `'../motion/useScrollReveal'`. Do not add a second stubbed section (D-06 -- Hero-only scope this phase).
  </action>
  <acceptance_criteria>
    - `src/router.tsx` imports `createBrowserRouter` from `'react-router'` and contains the reserved `case-study/:slug` shape as a comment
    - `src/routes/home.tsx` renders Hero copy exclusively by field reference, not by re-typing it -- `grep -c 'heroContent\.statement' src/routes/home.tsx` reports at least 1, `grep -c 'heroContent\.eyebrow' src/routes/home.tsx` reports at least 1, and `grep -c 'heroContent\.metaDescription' src/routes/home.tsx` reports at least 1 (copy lives only in `src/content/hero.ts`, consumed here by reference)
    - `grep -c 'id="hero"' src/routes/home.tsx` reports 1
    - `grep -c "useScrollReveal(heroRef)" src/routes/home.tsx` reports 1
    - All 4 behaviors above have a corresponding passing test
    - `npx vitest run src/routes/home.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/routes/home.test.tsx</automated>
  </verify>
  <done>Home route composes the Hero entirely from components/ui/ primitives and heroContent data, calls useScrollReveal on the section root, reserves the /case-study/:slug route shape for Phase 2 -- all tests pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Finalize app entrypoint (MotionProvider + RouterProvider) and verify end-to-end</name>
  <files>src/main.tsx, src/App.tsx</files>
  <read_first>src/main.tsx, src/App.tsx (current placeholder state from 01-02-PLAN.md), src/motion/MotionProvider.tsx (from 01-03-PLAN.md), src/router.tsx (from Task 2), .planning/phases/01-foundation-motion-infrastructure/01-VALIDATION.md (section: Manual-Only Verifications)</read_first>
  <action>
    Replace `src/App.tsx`'s placeholder body with `export default function App() { return <Outlet />; }`, importing `Outlet` from `'react-router'`. Replace `src/main.tsx`'s render call with `createRoot(document.getElementById('root')!).render(<StrictMode><MotionProvider><RouterProvider router={router} /></MotionProvider></StrictMode>)`, importing `StrictMode` from `'react'`, `RouterProvider` from `'react-router/dom'`, `MotionProvider` from `'./motion/MotionProvider'`, and `router` from `'./router'`. Run the full test suite and a production build to confirm the phase's motion system, UI primitives, and Hero composition integrate without runtime errors.
  </action>
  <acceptance_criteria>
    - `src/main.tsx` wraps `RouterProvider` in `MotionProvider` inside `StrictMode`
    - `src/App.tsx` renders `Outlet` from `'react-router'`
    - `npm run build` exits 0
    - `npm test` (full suite across all phase test files) exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm test</automated>
    <human-check>
      Run `npm run dev`, open the Hero route in a browser. (1) Tab through the page's focusable elements (the CTA anchor) and confirm keyboard focus order is correct and a visible focus ring appears (QUAL-01, QUAL-04 groundwork). (2) Click the CTA (`href="#hero"`) and confirm the browser/Lenis-driven scroll lands correctly with no jump/desync (QUAL-01 native anchor-link behavior -- also closes the flagged spec-less-fallback assumption above). (3) Enable `prefers-reduced-motion: reduce` at the OS level, reload the route, and confirm the Hero's scroll-triggered reveal does NOT play while all content (eyebrow, statement, CTA, meta card) remains fully visible immediately (QUAL-02). (4) Disable the OS setting, reload again, and confirm the reveal DOES play this time.
    </human-check>
  </verify>
  <done>App renders the Hero at / with MotionProvider active at the root; production build and full test suite are green; native scroll/keyboard/anchor behavior and the OS-level reduced-motion toggle are confirmed correct by manual check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `src/content/hero.ts` -> rendered JSX | Static, developer-authored copy only -- no user input, no dynamic HTML injection |
| Browser URL -> React Router route matching | Client-side only; no server-rendered auth-gated routes exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `react-router` (already installed) | high | mitigate | Already covered by 01-01/01-02's checkpoint + lockfile; no new installs in this plan |
| T-01-11 | Tampering (XSS) | `heroContent` copy rendered via JSX | low | accept | Static, developer-authored TypeScript data, not user input; React's JSX escaping applies; no `dangerouslySetInnerHTML` used -- becomes relevant again only when Phase 2 renders user-facing Markdown content |
| T-01-12 | Spoofing | React Router client-side route matching (`/`, reserved `/case-study/:slug`) | low | accept | No server-rendered auth-gated routes exist; all routes are public static content -- route spoofing has no security consequence for a public portfolio |
| T-01-13 | Denial of Service | Duplicate `MotionProvider`/`useScrollReveal` registration if `main.tsx` accidentally wraps the tree twice | low | mitigate | Task 3's build+test verification catches a double-mount at the `npm run build`/`npm test` step; `StrictMode`'s phantom cycle is already proven safe by 01-03/01-05's cleanup tests |

</threat_model>

<verification>
`npm run build` and `npm test` both exit 0. Manual check (Task 3) confirms native scroll/keyboard/anchor behavior and the OS-level `prefers-reduced-motion` toggle both work as specified.
</verification>

<success_criteria>
The Hero route renders real content from src/content/hero.ts, composed entirely from components/ui/ primitives, animated via useScrollReveal, served through a React Router Data Mode tree that reserves Phase 2's route shape, wrapped in MotionProvider at the root -- production build and full test suite green, manual scroll/keyboard/anchor and reduced-motion checks confirmed.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-motion-infrastructure/01-06-SUMMARY.md` when done
</output>
