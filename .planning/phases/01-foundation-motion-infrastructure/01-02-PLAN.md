---
phase: 01-foundation-motion-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - index.html
  - .gitignore
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/test/setup.ts
  - src/lib/cn.ts
autonomous: true
requirements: [QUAL-01, QUAL-02]

must_haves:
  truths:
    - "The app builds (npm run build) and runs on Vite + React + TypeScript + Tailwind CSS v4 with a working dev server, using zero CDN <script>/<link> tags for Tailwind, GSAP, Lenis, or Lucide anywhere in index.html or src/"
    - "Tailwind v4 design tokens (7 color roles, 7 spacing steps, Inter font, 4 typography roles with paired line-heights) are declared once in src/index.css's @theme block and nowhere else, matching 01-UI-SPEC.md's Color/Spacing/Typography tables exactly"
    - "Phase 1 ships a single, dark-only token set -- no second (light) theme block and no theme-switching mechanism exist anywhere in vite.config.ts or src/index.css"
    - "Vitest runs with a configured jsdom environment and exits 0 with zero test files before any motion/UI test files exist (Wave 0 scaffold requirement)"
  artifacts:
    - path: "package.json"
      provides: "Pinned dependency manifest (vite 8.1.5, react/react-dom 19.2.8, @vitejs/plugin-react 6.0.4, typescript 7.0.2, tailwindcss 4.3.3, @tailwindcss/vite 4.3.3, gsap 3.15.0, @gsap/react 2.1.2, lenis 1.3.25, react-router 8.3.0, lucide-react 1.26.0, clsx 2.1.1, tailwind-merge 3.6.0; devDeps vitest 4.1.10, @testing-library/react 16.3.2, @testing-library/jest-dom 7.0.0, jsdom 29.1.1) + test script"
    - path: "vite.config.ts"
      provides: "Vite config with react() + tailwindcss() plugins and a Vitest test block (jsdom, setupFiles, globals, passWithNoTests)"
    - path: "src/index.css"
      provides: "Single Tailwind entry point; @theme token block; global prefers-reduced-motion CSS safety net"
    - path: "src/lib/cn.ts"
      provides: "cn() className helper (clsx + tailwind-merge)"
    - path: "src/test/setup.ts"
      provides: "@testing-library/jest-dom matcher registration for Vitest"
  key_links:
    - from: "vite.config.ts"
      to: "src/index.css"
      via: "@tailwindcss/vite plugin compiles the @theme block into utility classes at build time"
      pattern: "tailwindcss\\("
    - from: "vite.config.ts"
      to: "src/test/setup.ts"
      via: "test.setupFiles registers jest-dom matchers before every Vitest run"
      pattern: "setupFiles"
---

<objective>
Scaffold the from-scratch Vite + React + TypeScript app, install every pinned dependency from 01-RESEARCH.md's Standard Stack, lock the Axisform-derived (but not byte-for-byte-extracted, per D-02) design tokens into a Tailwind v4 CSS-first `@theme` block, and configure the Vitest + jsdom + Testing Library test harness every later plan's tests run against.

Purpose: Everything downstream (motion system, UI primitives, Hero) needs a working build pipeline, a single source of design tokens, and a running test harness to exist first — this is the phase's genuine Wave 0.
Output: A building, running Vite+React+TS+Tailwind v4 app with zero CDN tags, committed `package-lock.json`, a locked `@theme` token block, and a green (zero-test) Vitest run.
</objective>

<execution_context>
@/Users/hazrafarhin/Desktop/Repositories/Portfolio/.claude/gsd-core/workflows/execute-plan.md
@/Users/hazrafarhin/Desktop/Repositories/Portfolio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-motion-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-motion-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-motion-infrastructure/01-UI-SPEC.md
@.planning/phases/01-foundation-motion-infrastructure/01-VALIDATION.md
</context>

## Artifacts this phase produces

- `package.json`, `package-lock.json` — dependency manifest + lockfile
- `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json` — TypeScript project references (Vite react-ts scaffold defaults)
- `vite.config.ts` — Vite config: `react()` + `tailwindcss()` plugins, Vitest `test` block
- `index.html` — SPA shell, Inter Google Fonts `<link>` tags, no CDN script tags
- `src/main.tsx`, `src/App.tsx` — bare entrypoint/root shell (finalized with `MotionProvider`/`RouterProvider` in 01-06-PLAN.md)
- `src/index.css` — `@theme` design-token block + global `prefers-reduced-motion` CSS safety net
- `src/lib/cn.ts` — exported `cn()` helper
- `src/test/setup.ts` — Vitest/jest-dom setup file

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TS app and install pinned dependencies</name>
  <files>package.json, package-lock.json, tsconfig.json, tsconfig.app.json, tsconfig.node.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx, .gitignore</files>
  <read_first>.planning/phases/01-foundation-motion-infrastructure/01-RESEARCH.md (sections: Standard Stack, Installation), README.md, .gitignore</read_first>
  <action>
    Scaffold into an isolated temp directory first to avoid Vite's non-empty-directory prompt clobbering existing repo content (`.git`, `.planning/`, `Templates/`, `Portfolio-Documentation/`, `README.md`, `.gitignore`, `.env`): run `npm create vite@latest .vite-scaffold -- --template react-ts`. Move `.vite-scaffold/{package.json,tsconfig.json,tsconfig.app.json,tsconfig.node.json,vite.config.ts,index.html,src,public}` into the project root, then delete `.vite-scaffold/` entirely, including its own `.gitignore` and `README.md` (do NOT overwrite the repo's existing root `README.md` or `.gitignore` — merge instead of replace). Append the scaffold's ignore entries (`node_modules`, `dist`, `dist-ssr`, `*.local`, editor-directory entries) to the END of the existing root `.gitignore` beneath its current GSD-tooling entries; do not remove any existing line. Delete the scaffold's default `src/App.css` (not used — 01-06-PLAN.md composes the Home route directly with Tailwind utility classes and `components/ui/` primitives, no per-component CSS module). Set `package.json`'s `"name"` field to `"hazra-farhin-portfolio"` (the scaffold defaulted it to the temp folder name). Install the scaffold's own template deps first (`npm install`), then pin and install this phase's exact dependency set at the precise versions verified in 01-RESEARCH.md's Standard Stack table: `npm install vite@8.1.5 @vitejs/plugin-react@6.0.4 react@19.2.8 react-dom@19.2.8 gsap@3.15.0 @gsap/react@2.1.2 lenis@1.3.25 react-router@8.3.0 lucide-react@1.26.0 clsx@2.1.1 tailwind-merge@3.6.0 tailwindcss@4.3.3 @tailwindcss/vite@4.3.3`, then `npm install -D typescript@7.0.2 vitest@4.1.10 @testing-library/react@16.3.2 @testing-library/jest-dom@7.0.0 jsdom@29.1.1`. Do NOT install `react-router-dom` (removed in React Router v8) or `@studio-freight/react-lenis` (superseded, frozen package). Replace `src/main.tsx`'s render call with a minimal `createRoot(...).render(<App />)` (no router/motion wiring yet — that lands in 01-06-PLAN.md) and leave `src/App.tsx` as a minimal functional component returning a placeholder `<div>` (both finalized later). Commit `package-lock.json`.
  </action>
  <acceptance_criteria>
    - `package.json` `dependencies`/`devDependencies` list every package above at the exact pinned version, and do NOT list `react-router-dom` or `@studio-freight/react-lenis`
    - `package-lock.json` exists at the repo root
    - Root `README.md` content is unchanged from before this task (still the project's own README, not Vite's scaffold README)
    - Root `.gitignore` still contains its pre-existing GSD-tooling lines (`.env*`, `.agents/`, `.claude/`, `.codex/`, `.opencode/`, `.zcode/`) plus newly appended `node_modules`/`dist` entries
    - No `.vite-scaffold/` directory remains anywhere in the repo
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Vite+React+TS app scaffolded at repo root, all pinned dependencies installed, lockfile committed, existing README/.gitignore preserved, production build succeeds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Lock Tailwind v4 design tokens and load Inter (D-01/D-02/D-03/D-04)</name>
  <files>src/index.css, index.html, vite.config.ts</files>
  <read_first>.planning/phases/01-foundation-motion-infrastructure/01-UI-SPEC.md (sections: Spacing Scale, Typography, Color), .planning/phases/01-foundation-motion-infrastructure/01-RESEARCH.md (section: Code Examples -> Tailwind v4 CSS-first setup), vite.config.ts (current state from Task 1), src/index.css (current scaffold default)</read_first>
  <action>
    Register the `@tailwindcss/vite` plugin in `vite.config.ts`'s `plugins` array alongside `react()` (no `tailwind.config.js`, no PostCSS/autoprefixer config — Tailwind v4 is CSS-first). Per D-01, lock the real Axisform-derived visual tokens (dark background palette, type scale, spacing) into this Tailwind v4 `@theme` config now, in Phase 1 — not deferred to Phase 3. Replace `src/index.css`'s entire content with a single `@import "tailwindcss";` followed by one `@theme { }` block declaring exactly these tokens, matching 01-UI-SPEC.md verbatim: `--color-dominant: #0B0C0A`, `--color-secondary: #16171A`, `--color-accent: #FF6A33`, `--color-destructive: #E5484D`, `--color-foreground: #F2EFE6`, `--color-muted-foreground: rgba(242, 239, 230, 0.58)`, `--color-line: rgba(242, 239, 230, 0.10)`; `--spacing-xs: 4px`, `--spacing-sm: 8px`, `--spacing-md: 16px`, `--spacing-lg: 24px`, `--spacing-xl: 32px`, `--spacing-2xl: 48px`, `--spacing-3xl: 64px`; `--font-sans: 'Inter', system-ui, sans-serif`; `--text-label: 14px` + `--text-label--line-height: 1.4`, `--text-body: 18px` + `--text-body--line-height: 1.6`, `--text-heading: 40px` + `--text-heading--line-height: 1.15`, `--text-display: clamp(48px, 8vw, 88px)` + `--text-display--line-height: 1.0`. Use Tailwind's built-in `font-normal` (400) and `font-extrabold` (800) utilities for the two declared weights (no custom `--font-weight-*` tokens needed — Tailwind's default scale already maps these exactly). Use Tailwind's built-in `rounded-full` (pill, 999px) and `rounded-3xl` (24px) utilities for Button/Card radii in later plans (no custom `--radius-*` tokens needed — Tailwind v4 defaults already match the spec's 999px/24px exactly). Below the `@theme` block, add `body { @apply bg-dominant text-foreground font-sans; }` so the page background/foreground/font apply globally. Add a global CSS safety-net rule (defense-in-depth alongside the JS-driven `MotionProvider` gate landing in 01-03-PLAN.md): a `@media (prefers-reduced-motion: reduce)` block forcing `*, *::before, *::after { animation-duration: 0.001ms !important; animation-iteration-count: 1 !important; transition-duration: 0.001ms !important; scroll-behavior: auto !important; }`. Do NOT add a second `@theme` block, a `data-theme`/light-token variant, or any theme-switching mechanism (D-04: dark-only this phase, deferred). In `index.html`'s `<head>`, add `<link rel="preconnect" href="https://fonts.googleapis.com">`, `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>`, and `<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400..900&display=swap" rel="stylesheet">` (D-03: Inter is the primary typeface, loaded as a variable font via Google Fonts across weights 400-900 even though only 400/800 are consumed by this phase's Typography component in 01-04-PLAN.md) — no other CDN `<script>`/`<link>` tags (no Tailwind Play CDN, no cdnjs GSAP, no jsdelivr Lenis, no unpkg Lucide).
  </action>
  <acceptance_criteria>
    - `grep -c 'tailwindcss()' vite.config.ts` reports at least 1 match inside the `plugins` array
    - `grep -c -- "--color-accent: #FF6A33" src/index.css` reports 1
    - `grep -c -- "--text-display: clamp(48px, 8vw, 88px)" src/index.css` reports 1
    - `grep -c '@media (prefers-reduced-motion: reduce)' src/index.css` reports 1
    - `find . -maxdepth 1 -iname 'tailwind.config.*' | wc -l` reports 0 -- no legacy JS-based config file exists at the repo root
    - `grep -c 'cdn.tailwindcss.com\|cdnjs.cloudflare.com\|jsdelivr.net/npm/lenis\|unpkg.com/lucide' index.html` reports 0
    - `grep -c 'fonts.googleapis.com/css2?family=Inter' index.html` reports 1
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build && grep -c -- "--color-accent: #FF6A33" src/index.css</automated>
  </verify>
  <done>Single @theme token block matches 01-UI-SPEC.md exactly, Inter loads via Google Fonts link tags, no CDN script tags anywhere, no second theme/token set exists, production build succeeds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Configure Vitest + jsdom test harness and cn() helper</name>
  <files>vite.config.ts, src/test/setup.ts, src/lib/cn.ts, package.json</files>
  <read_first>.planning/phases/01-foundation-motion-infrastructure/01-VALIDATION.md (sections: Test Infrastructure, Wave 0 Requirements), .planning/phases/01-foundation-motion-infrastructure/01-RESEARCH.md (section: Validation Architecture), vite.config.ts (current state from Task 2)</read_first>
  <action>
    Change `vite.config.ts`'s `defineConfig` import from `'vite'` to `'vitest/config'` (Vitest's re-export merges Vite+Vitest config typing so the `test` key type-checks without a triple-slash reference). Add a `test` key to the config object: `{ environment: 'jsdom', setupFiles: ['./src/test/setup.ts'], globals: true, passWithNoTests: true }`. `passWithNoTests: true` is required now because no test files exist until 01-03-PLAN.md; without it, this task's own verify command would report a false failure. Create `src/test/setup.ts` containing exactly `import '@testing-library/jest-dom';` (side-effecting import that extends Vitest's global `expect` with jest-dom matchers). Create `src/lib/cn.ts` exporting `export function cn(...inputs: ClassValue[]): string` (import `ClassValue` from `clsx`), implemented as `twMerge(clsx(inputs))` using `clsx` from `'clsx'` and `twMerge` from `'tailwind-merge'`. Add a `"test": "vitest run"` script to `package.json`'s `scripts` (no watch-mode script — the phase's sampling rate always runs in `run` mode per 01-VALIDATION.md, never `--watch`).
  </action>
  <acceptance_criteria>
    - `grep -c "from 'vitest/config'" vite.config.ts` reports 1
    - `grep -c "environment: 'jsdom'" vite.config.ts` reports 1
    - `grep -c "passWithNoTests: true" vite.config.ts` reports 1
    - `src/test/setup.ts` contains `import '@testing-library/jest-dom';`
    - `src/lib/cn.ts` exports a function named `cn`
    - `grep -c '"test": "vitest run"' package.json` reports 1
    - `npm test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Vitest configured with jsdom + jest-dom matchers + passWithNoTests, cn() helper exported, npm test exits 0 with zero test files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local `node_modules` | This plan's install task (Task 1) is the point where every dependency actually lands on disk |
| Google Fonts CDN -> browser | `index.html`'s font `<link>` tags fetch Inter from `fonts.googleapis.com`/`fonts.gstatic.com` at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install` (Task 1, all 13 packages from 01-RESEARCH.md Standard Stack) | high | mitigate | Already gated by 01-01-PLAN.md's blocking human-verify checkpoint (must show "approved" before this plan runs); `package-lock.json` committed here pins exactly the reviewed versions |
| T-01-01 | Tampering | Google Fonts `<link>` tags (Inter) in `index.html` | low | accept | Same-origin trusted Google domains, static `<link>` tags only, no inline/dynamic script execution -- industry-standard, accepted risk |
| T-01-02 | Information Disclosure | none -- no user input, forms, or auth in this plan | n/a | not applicable | Build tooling and static CSS tokens only; no data-collection surface exists |
| T-01-03 | Elevation of Privilege | none -- no privilege boundary in a static SPA build step | n/a | not applicable | No auth/session/access-control surface exists anywhere in this phase |

</threat_model>

<verification>
`npm run build` and `npm test` both exit 0 after all three tasks complete. No CDN script/link tags for Tailwind, GSAP, Lenis, or Lucide appear anywhere in `index.html` or `src/`.
</verification>

<success_criteria>
Vite+React+TS+Tailwind v4 app builds and runs; design tokens locked in a single `@theme` block matching 01-UI-SPEC.md; Vitest+jsdom+Testing Library configured and green with zero tests; root `README.md`/`.gitignore` preserved, not overwritten.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-motion-infrastructure/01-02-SUMMARY.md` when done
</output>
