---
phase: 01-foundation-motion-infrastructure
plan: 05
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/motion/useScrollReveal.ts
  - src/motion/useScrollReveal.test.ts
autonomous: true
requirements: [QUAL-01, QUAL-02]

must_haves:
  truths:
    - "useScrollReveal(ref, options) registers a ScrollTrigger-driven gsap.from animation when motion is enabled"
    - "useScrollReveal creates zero ScrollTrigger instances when the reduced-motion context is true (no-op, content renders in its final visible state)"
    - "Multiple useScrollReveal() call sites reading the same MotionProvider context observe an identical reduced-motion boolean regardless of their mount order"
  artifacts:
    - path: "src/motion/useScrollReveal.ts"
      provides: "useScrollReveal(ref, options) -- the public motion-authoring hook every animated component uses, built on @gsap/react's useGSAP()"
  key_links:
    - from: "src/motion/useScrollReveal.ts"
      to: "src/motion/MotionProvider.tsx"
      via: "reads usePrefersReducedMotionContext() so callers never write their own reduced-motion check (D-09)"
      pattern: "usePrefersReducedMotionContext"
---

<objective>
Build `useScrollReveal(ref, options)` (D-09), the single public motion-authoring hook every animated component in this and future phases calls to opt into scroll-driven reveal motion. It must internally no-op when reduced motion is active -- callers never write their own `matchMedia` check -- and must be built on `@gsap/react`'s `useGSAP()` (not a hand-rolled `useEffect` + `gsap.context()`), inheriting StrictMode-safe cleanup for free.

Purpose: This is the concrete authoring API 01-06-PLAN.md's Hero section calls to prove success criterion #2 (real scroll-triggered reveal motion) end-to-end.
Output: `src/motion/useScrollReveal.ts` with a passing Vitest test file covering enabled/disabled/ordering behavior.
</objective>

<execution_context>
@/Users/hazrafarhin/Desktop/Repositories/Portfolio/.claude/gsd-core/workflows/execute-plan.md
@/Users/hazrafarhin/Desktop/Repositories/Portfolio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-motion-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-motion-infrastructure/01-RESEARCH.md
</context>

## Artifacts this phase produces

- `src/motion/useScrollReveal.ts` — exports `useScrollReveal(ref: RefObject<HTMLElement>, options?: { y?: number; duration?: number; start?: string }): void`
- `src/motion/useScrollReveal.test.ts` — Vitest unit tests

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useScrollReveal core implementation (enabled/disabled behavior)</name>
  <files>src/motion/useScrollReveal.ts, src/motion/useScrollReveal.test.ts</files>
  <read_first>.planning/phases/01-foundation-motion-infrastructure/01-RESEARCH.md (section: Architecture Patterns -> Pattern 2), src/motion/MotionProvider.tsx (exports from 01-03-PLAN.md)</read_first>
  <behavior>
    - Test 1: With `usePrefersReducedMotionContext` mocked to return `false` and a ref attached to a mounted DOM node, calling `useScrollReveal(ref)` results in `ScrollTrigger.getAll().length === 1` after render (one instance created, targeting `ref.current`).
    - Test 2: With `usePrefersReducedMotionContext` mocked to return `true`, calling `useScrollReveal(ref)` on the same kind of mounted node results in `ScrollTrigger.getAll().length === 0` -- no animation registered, no-op.
    - Test 3: Passing custom `options` (`{ y: 64, duration: 1.2, start: 'top 90%' }`) is honored -- assert the created `ScrollTrigger` instance's `start` matches, or that the underlying `gsap.from` call (spy on `gsap.from`) received the custom `y`/`duration` values instead of the defaults (`y: 32, duration: 0.8, start: 'top 85%'`).
  </behavior>
  <action>
    Export `useScrollReveal(ref: RefObject<HTMLElement>, options: { y?: number; duration?: number; start?: string } = {}): void` from `src/motion/useScrollReveal.ts`. Destructure `{ y = 32, duration = 0.8, start = 'top 85%' }` from `options`. Call `usePrefersReducedMotionContext()` (from `./MotionProvider`) to read the reduced-motion boolean. Wrap the effect in `@gsap/react`'s `useGSAP(() => { if (!ref.current || prefersReducedMotion) return; gsap.from(ref.current, { y, opacity: 0, duration, ease: 'power2.out', scrollTrigger: { trigger: ref.current, start } }); }, { scope: ref, dependencies: [prefersReducedMotion] })` -- the `dependencies` array is required so the effect reverts and re-runs if the reduced-motion preference flips mid-session (not just at first mount). In the test file, mock `src/motion/MotionProvider` via `vi.mock` to control `usePrefersReducedMotionContext`'s return value per test, and render a small harness component (`useRef` + a mounted `<div ref={ref}>`) that calls the hook, clearing `ScrollTrigger.getAll().forEach(t => t.kill())` in an `afterEach` so instance counts don't leak between tests.
  </action>
  <acceptance_criteria>
    - `src/motion/useScrollReveal.ts` exports `useScrollReveal`
    - Implementation is built on `useGSAP` from `@gsap/react` (not a raw `useEffect` + `gsap.context()`) -- `grep -c "from '@gsap/react'" src/motion/useScrollReveal.ts` reports 1
    - All 3 behaviors above have a corresponding passing test
    - `npx vitest run src/motion/useScrollReveal.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/motion/useScrollReveal.test.ts</automated>
  </verify>
  <done>useScrollReveal registers a ScrollTrigger-driven gsap.from when motion is enabled, no-ops with zero ScrollTrigger instances when reduced motion is on, and honors custom options -- all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ordering edge (multiple call sites) and unmount cleanup</name>
  <files>src/motion/useScrollReveal.ts, src/motion/useScrollReveal.test.ts</files>
  <read_first>src/motion/useScrollReveal.ts (from Task 1), .planning/phases/01-foundation-motion-infrastructure/01-RESEARCH.md (section: Common Pitfalls -> Pitfall 1)</read_first>
  <behavior>
    - Test 4 (ordering edge): Mount two independent harness components, each calling `useScrollReveal` on its own ref, in reversed relative order (harness B mounts before harness A within the same render) -- both harnesses read `usePrefersReducedMotionContext()` and record the boolean they observed; assert both recorded values are identical (`===`), proving no call site races another for a stale/different context read regardless of mount order.
    - Test 5 (unmount cleanup, Pitfall 1): Mounting a harness with motion enabled creates one `ScrollTrigger` instance; unmounting that harness (or simulating React StrictMode's phantom mount-unmount-mount cycle by wrapping in `<React.StrictMode>`) leaves `ScrollTrigger.getAll().length === 0` afterward -- `useGSAP`'s auto-revert on unmount is proven to actually kill the created trigger, not just rely on it being untested.
  </behavior>
  <action>
    No production-code change is required if Task 1's implementation already scopes the `useGSAP` call correctly (`{ scope: ref, dependencies: [prefersReducedMotion] }`); this task adds the two additional tests above to `useScrollReveal.test.ts` to prove those properties hold, and only touches `useScrollReveal.ts` if either test reveals a real defect (e.g. a missing `scope` option that lets StrictMode's phantom mount leak a `ScrollTrigger`).
  </action>
  <acceptance_criteria>
    - Both behaviors above have a corresponding passing test in `useScrollReveal.test.ts`
    - `npx vitest run src/motion/useScrollReveal.test.ts` exits 0 with at least 5 total passing tests across both tasks (3 from Task 1, 2 from Task 2)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/motion/useScrollReveal.test.ts</automated>
  </verify>
  <done>Multiple useScrollReveal call sites observe an identical reduced-motion value regardless of mount order, and unmount (including StrictMode's phantom cycle) leaves zero orphaned ScrollTrigger instances -- all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `MotionProvider`'s reduced-motion Context -> `useScrollReveal` call sites | Every animated component's motion-or-no-motion decision flows through this one read; a bug here is the exact failure mode QUAL-02 exists to prevent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `@gsap/react`'s `useGSAP` (already installed) | high | mitigate | Already covered by 01-01/01-02's checkpoint + lockfile; no new installs in this plan |
| T-01-09 | Denial of Service | Leaked `ScrollTrigger` instance from a call site that doesn't correctly scope/revert on unmount | medium | mitigate | Task 2's unmount-cleanup test directly asserts `ScrollTrigger.getAll().length === 0` post-unmount, including under StrictMode's phantom cycle |
| T-01-10 | Information Disclosure | none -- ref/options are internal React values, no external data | n/a | not applicable | No network/user-input surface exists in this hook |

</threat_model>

<verification>
`npx vitest run src/motion/useScrollReveal.test.ts` exits 0 with 5 passing tests.
</verification>

<success_criteria>
useScrollReveal exists, is built on useGSAP, correctly no-ops under reduced motion, honors custom options, is consistent across multiple call sites regardless of mount order, and cleans up correctly on unmount including StrictMode's phantom cycle.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-motion-infrastructure/01-05-SUMMARY.md` when done
</output>
