---
phase: 01-foundation-motion-infrastructure
plan: 07
type: execute
wave: 1
depends_on: ["01-03"]
files_modified:
  - src/motion/MotionProvider.tsx
  - src/motion/MotionProvider.test.tsx
autonomous: true
gap_closure: true
requirements: [QUAL-01, QUAL-02]

must_haves:
  truths:
    - "When prefersReducedMotion is true at mount, MotionProvider does not instantiate Lenis and does not register a gsap.ticker callback -- Lenis's JS-driven smooth-scroll easing never runs for reduced-motion users (closes ROADMAP Success Criterion #3 / 01-VERIFICATION.md Gap 1 / 01-REVIEW.md WR-01)"
    - "Toggling prefers-reduced-motion from false to true mid-session tears down the active Lenis instance (destroy() and gsap.ticker.remove() both fire); toggling back from true to false re-initializes a fresh Lenis instance, with no leaked ticker callback in either direction"
    - "The pre-existing GSAP scroll-reveal gating (useScrollReveal) and all 31 previously-passing tests across the phase continue to pass unmodified -- this is a narrow gating fix, not a rewrite"
    - "In a real browser, keyboard tab order reaches the CTA with a visible focus ring, clicking the CTA scrolls to #hero without jump/desync, and enabling OS-level prefers-reduced-motion now ALSO disables Lenis's scroll easing (not just the GSAP reveal) -- confirmed by human verification re-scoped to run against this fix (closes 01-06-PLAN.md Task 3's deferred human-check and 01-VERIFICATION.md's Human Verification Required item)"
  artifacts:
    - path: "src/motion/MotionProvider.tsx"
      provides: "Lenis-instantiation useEffect gated by an `if (prefersReducedMotion) return;` guard, with `prefersReducedMotion` added to the effect's dependency array"
    - path: "src/motion/MotionProvider.test.tsx"
      provides: "FakeMediaQueryList-based mockMatchMedia helper (mirroring usePrefersReducedMotion.test.ts) + 3 new tests proving Lenis/ticker registration is skipped under reduced motion (including under StrictMode) and correctly tears down/re-initializes on mid-session toggle"
  key_links:
    - from: "src/motion/MotionProvider.tsx (Lenis-instantiation effect)"
      to: "prefersReducedMotion (sourced from usePrefersReducedMotion via D-10, already exposed through ReducedMotionContext)"
      via: "if (prefersReducedMotion) return; guard + [prefersReducedMotion] dependency array -- the key link 01-VERIFICATION.md flagged as NOT WIRED"
      pattern: "if \\(prefersReducedMotion\\) return"
---

<objective>
Close the two gaps `01-VERIFICATION.md` found in Phase 1: (1) `MotionProvider`'s Lenis-instantiation effect runs unconditionally regardless of `prefersReducedMotion`, so Lenis's JS-driven smooth-scroll easing stays active for users who opted out of motion at the OS level -- this is the confirmed BLOCKER (ROADMAP Success Criterion #3, independently flagged as WR-01 in `01-REVIEW.md`); (2) the real-browser human-check deferred by `01-06-PLAN.md` Task 3 (keyboard focus order, CTA anchor-scroll, and the live reduced-motion symptom) has never been performed, and must now be re-scoped to run against the Gap 1 fix so it verifies the corrected behavior, not the broken one.

Purpose: Make the phase's "centralized, reduced-motion-safe motion system" claim actually true before Phase 2/3 build more motion-heavy sections on top of it.
Output: `MotionProvider.tsx`'s Lenis effect correctly gated on `prefersReducedMotion`, proven by 3 new unit tests plus a human-verified real-browser check. This plan does NOT mark QUAL-01/QUAL-02 complete in REQUIREMENTS.md -- that is the phase re-verification's decision after this plan executes.
</objective>

<execution_context>
@/Users/hazrafarhin/Desktop/Repositories/Portfolio/.claude/gsd-core/workflows/execute-plan.md
@/Users/hazrafarhin/Desktop/Repositories/Portfolio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-motion-infrastructure/01-VERIFICATION.md
@.planning/phases/01-foundation-motion-infrastructure/01-REVIEW.md
@.planning/phases/01-foundation-motion-infrastructure/01-CONTEXT.md
@src/motion/usePrefersReducedMotion.test.ts
</context>

## Artifacts this phase produces

- `src/motion/MotionProvider.tsx` (modified) -- the Lenis-instantiation `useEffect` inside `MotionProvider({ children })` gains an `if (prefersReducedMotion) return;` guard and `[prefersReducedMotion]` dependency array (was `[]`); no other line of the effect changes.
- `src/motion/MotionProvider.test.tsx` (modified) -- `mockMatchMedia` helper replaced with a `FakeMediaQueryList` class exposing `triggerChange(matches)`; 3 new test cases added to the `describe('MotionProvider', ...)` block; `act` added to the `@testing-library/react` import.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate Lenis instantiation on prefersReducedMotion + re-scoped human verification</name>
  <files>src/motion/MotionProvider.tsx, src/motion/MotionProvider.test.tsx</files>
  <read_first>src/motion/MotionProvider.tsx (current state -- the unconditional Lenis effect, lines 29-50), src/motion/MotionProvider.test.tsx (current state -- 4 passing tests, the existing mockMatchMedia stub), src/motion/usePrefersReducedMotion.test.ts (the FakeMediaQueryList pattern to mirror), .planning/phases/01-foundation-motion-infrastructure/01-VERIFICATION.md (Gap 1 exact description, lines 8-16 and 111-119), .planning/phases/01-foundation-motion-infrastructure/01-REVIEW.md (WR-01, the exact proposed fix)</read_first>
  <behavior>
    - Test A (new): "does not instantiate Lenis or register a gsap.ticker callback when prefersReducedMotion is true at mount" -- call `mockMatchMedia(true)`, spy `const addSpy = vi.spyOn(gsap.ticker, 'add')`, render `<MotionProvider><div>content</div></MotionProvider>`, assert `expect(Lenis).not.toHaveBeenCalled()` and `expect(addSpy).not.toHaveBeenCalled()`.
    - Test B (new, StrictMode-safe variant): "creates no Lenis instance or ticker callback under StrictMode when prefersReducedMotion is true" -- call `mockMatchMedia(true)`, spy on `gsap.ticker.add`, render the same tree wrapped in `<StrictMode>`, assert `expect(Lenis).not.toHaveBeenCalled()` and the ticker-add spy was never called, then call `unmount()` and confirm it does not throw.
    - Test C (new): "tears down Lenis when prefersReducedMotion toggles to true mid-session, and re-initializes it when toggled back to false" -- call `const mql = mockMatchMedia(false)`, render `<MotionProvider><div>content</div></MotionProvider>`, assert `expect(Lenis).toHaveBeenCalledTimes(1)`, capture `const firstInstance = (Lenis as unknown as ReturnType<typeof vi.fn>).mock.results[0].value`, wrap `mql.triggerChange(true)` in `act(() => { ... })` and assert `expect(firstInstance.destroy).toHaveBeenCalledTimes(1)` plus `expect(Lenis).toHaveBeenCalledTimes(1)` (no new instance created while reduced motion is on), then wrap `mql.triggerChange(false)` in a second `act(() => { ... })` and assert `expect(Lenis).toHaveBeenCalledTimes(2)` (a fresh instance created after toggling back off) with `expect((Lenis as unknown as ReturnType<typeof vi.fn>).mock.results[1].value.destroy).not.toHaveBeenCalled()`.
    - All 4 pre-existing tests in `MotionProvider.test.tsx` must continue to pass unmodified (they all mount with `prefersReducedMotion: false` via the `beforeEach(() => mockMatchMedia(false))`, so the new guard/dependency array does not change their behavior).
  </behavior>
  <action>
    In `src/motion/MotionProvider.tsx`, modify the Lenis-instantiation `useEffect` (currently lines 29-50) by adding a single early-return guard as its first statement, before the `new Lenis(...)` call: `if (prefersReducedMotion) return;`. Change the effect's dependency array from `[]` to `[prefersReducedMotion]` so toggling the OS-level setting mid-session re-runs the effect -- the existing cleanup function (`gsap.ticker.remove(update)`, `lenis.destroy()`) already tears down the prior instance correctly on re-run; do not modify the cleanup function, the Lenis constructor options, the `lenis.on('scroll', ScrollTrigger.update)` call, or the `gsap.ticker.add`/`lagSmoothing(0)` calls -- this is a narrow gating fix, not a rewrite.

    In `src/motion/MotionProvider.test.tsx`: add `act` to the existing `@testing-library/react` import (alongside `render`, `renderHook`). Replace the current `mockMatchMedia` helper -- a `vi.fn().mockReturnValue({ matches, addEventListener: vi.fn(), removeEventListener: vi.fn() })` stub that discards the registered change listener -- with a `FakeMediaQueryList` class matching the one already in `src/motion/usePrefersReducedMotion.test.ts`: a `matches: boolean` field set in the constructor, `addEventListener = vi.fn((_type: string, listener: EventListener) => { this.listener = listener; })`, `removeEventListener = vi.fn()`, a `listener: EventListener | undefined` field, and a `triggerChange(matches: boolean)` method that sets `this.matches = matches` and calls `this.listener?.(new Event('change'))`. Update `mockMatchMedia(initialMatches: boolean)` to construct `const mql = new FakeMediaQueryList(initialMatches)`, call `vi.stubGlobal('matchMedia', vi.fn().mockReturnValue(mql))`, and `return mql`. Add Tests A, B, and C described above inside the existing `describe('MotionProvider', ...)` block, alongside (not replacing) the 4 pre-existing tests.
  </action>
  <acceptance_criteria>
    - `grep -c "if (prefersReducedMotion) return;" src/motion/MotionProvider.tsx` reports 1
    - `grep -c "\[prefersReducedMotion\]" src/motion/MotionProvider.tsx` reports 1 (the effect's dependency array)
    - `grep -c "triggerChange" src/motion/MotionProvider.test.tsx` reports at least 2 (the method definition plus at least one call site in Test C)
    - `npx vitest run src/motion/MotionProvider.test.tsx` exits 0 and reports 7 passed tests (4 pre-existing + 3 new: Test A, Test B, Test C)
    - `npm test` (full suite) exits 0 and reports 34 passed tests across 7 test files (the prior 31 plus this task's 3 new ones)
    - `npm run build` exits 0
    - No other line inside the Lenis-instantiation effect body changed besides the added guard and the dependency array (diff review)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/motion/MotionProvider.test.tsx && npm test && npm run build</automated>
    <human-check>
      With this task's fix applied, run `npm run dev` and open the Hero route (`/`) in a real browser. This re-scopes and closes 01-06-PLAN.md's Task 3 deferred human-check and 01-VERIFICATION.md's "Human Verification Required" item -- now run against the fixed code, not the pre-fix code. (a) Tab through the page's focusable elements and confirm the CTA anchor receives keyboard focus in the correct order with a visible focus-visible ring. (b) Click the CTA (`href="#hero"`) and confirm the browser/Lenis-driven scroll lands correctly with no jump or desync. (c) Enable `prefers-reduced-motion: reduce` at the OS level and reload the route -- confirm BOTH that the GSAP scroll-triggered reveal does not play (content fully visible immediately, as already unit-verified) AND that Lenis's smooth-scroll easing is now also disabled (scrolling should feel native/instant, not eased/physics-based, unlike before this fix). (d) Disable the OS setting and reload again -- confirm BOTH the GSAP reveal plays again AND Lenis's eased smooth-scroll is restored.
    </human-check>
  </verify>
  <done>The Lenis-instantiation effect in MotionProvider.tsx is gated by an `if (prefersReducedMotion) return;` guard with `prefersReducedMotion` in its dependency array; 3 new tests confirm no Lenis/ticker registration occurs under reduced motion (including under StrictMode) and that toggling mid-session correctly tears down and re-initializes Lenis; the full 34-test suite and production build remain green; and a human has confirmed in a real browser that keyboard focus, CTA anchor-scroll, and the OS-level reduced-motion toggle (now covering both the GSAP reveal and Lenis's smooth-scroll) all behave correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS accessibility setting -> browser `matchMedia` -> React Context -> Lenis instantiation | The exact channel this plan repairs: previously the reduced-motion preference crossed into Context correctly but never gated the Lenis side-effect, silently un-gating scroll-easing motion for users who opted out |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-14 | Denial of Service (reliability) | Lenis instantiation now re-runs on every `prefersReducedMotion` toggle via `[prefersReducedMotion]` deps | medium | mitigate | Test C directly asserts symmetric `destroy()`/re-instantiation behavior on toggle, so no leaked Lenis instance or duplicate ticker callback can accumulate across repeated toggles |
| T-01-15 | Information Disclosure | `prefersReducedMotion` boolean crossing the OS->Context boundary | n/a | not applicable | UI preference only, no PII; already accepted in 01-03-PLAN.md's threat model, unchanged by this fix |
| T-01-SC | Tampering | `gsap`, `lenis` packages (already installed, no new installs) | high | mitigate | Already covered by 01-01-PLAN.md's approved legitimacy checkpoint and committed lockfile; this plan modifies only application/test code, zero new `package.json` entries |

</threat_model>

<verification>
`npx vitest run src/motion/MotionProvider.test.tsx` exits 0 with 7 passing tests. `npm test` exits 0 with 34 passing tests across 7 files. `npm run build` exits 0. The embedded human-check confirms both the GSAP reveal and Lenis's smooth-scroll are disabled under OS-level reduced motion, and that keyboard focus / CTA anchor-scroll behave correctly.
</verification>

<success_criteria>
MotionProvider's Lenis-instantiation effect is gated on prefersReducedMotion (guard + dependency array), proven by 3 new unit tests covering mount-time gating, StrictMode-safe mount-time gating, and mid-session toggle mount/teardown behavior. All pre-existing tests and the production build remain green. A human has verified in a real browser that reduced motion now disables Lenis's smooth-scroll (not just the GSAP reveal) and that keyboard focus/CTA anchor-scroll behave correctly. REQUIREMENTS.md is NOT modified by this plan -- QUAL-01/QUAL-02 completion is decided by the subsequent phase re-verification.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-motion-infrastructure/01-07-SUMMARY.md` when done
</output>
