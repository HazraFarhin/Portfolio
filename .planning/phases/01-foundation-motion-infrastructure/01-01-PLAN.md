---
phase: 01-foundation-motion-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [QUAL-01, QUAL-02]

must_haves:
  truths:
    - "Every npm package flagged SUS/ASSUMED in 01-RESEARCH.md's Package Legitimacy Audit has been human-reviewed and explicitly approved before any install task in 01-02-PLAN.md runs"
  artifacts: []
  key_links: []
---

<objective>
Gate the phase's very first install step behind a human review of every npm package the Package Legitimacy Audit (01-RESEARCH.md) flagged `SUS` under the "too-new heuristic". This is a security control, not a UI/functional check — it runs before any code exists and before `npm install` executes, so it cannot be deferred to end-of-phase UAT the way `workflow.human_verify_mode: end-of-phase` normally defers verification (that setting governs functional/visual checks; a pre-install supply-chain gate structurally cannot be deferred, since deferring it means the packages — and anything a compromised `postinstall` script does — are already on disk by the time anyone looks).

Purpose: Confirm no package in the Standard Stack is a slopsquatting/typosquat risk before it touches this machine, per the mandatory Package Legitimacy Gate protocol (goal_backward instructions: "legitimacy checkpoints are never auto-approvable — `workflow.auto_advance` ignored").
Output: A human "approved" confirmation, unblocking 01-02-PLAN.md's install task. No files are created by this plan.
</objective>

<execution_context>
@/Users/hazrafarhin/Desktop/Repositories/Portfolio/.claude/gsd-core/workflows/execute-plan.md
@/Users/hazrafarhin/Desktop/Repositories/Portfolio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-motion-infrastructure/01-RESEARCH.md
</context>

## Artifacts this phase produces

None — this plan is a pure human checkpoint with zero file output.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm package legitimacy before any install runs</name>
  <files>none — pure checkpoint, no files created or modified</files>
  <what-built>
    01-RESEARCH.md's "Package Legitimacy Audit" table ran `gsd-tools query package-legitimacy check --ecosystem npm` against every package the phase installs (13 runtime/dev packages) and flagged 10 of them `SUS` purely on a "too-new heuristic" (latest patch/minor release within the last 0-30 days) — NOT because any package name, source repo, or download count looked suspicious. The researcher already cross-checked each flagged package's registry download count, GitHub source repo identity, and publish cadence, and recorded an explicit "Approved — override" rationale per row. This task asks a human to spot-confirm that override before `npm install` runs in 01-02-PLAN.md, since no automated tool can substitute final human judgment on supply-chain risk.
  </what-built>
  <action>
    Present the Package Legitimacy Audit table from 01-RESEARCH.md to the human for review, and block on an explicit "approved" response before allowing 01-02-PLAN.md's install task to run. Do not proceed to 01-02-PLAN.md until this checkpoint resolves.
  </action>
  <how-to-verify>
    Review the Package Legitimacy Audit table in `.planning/phases/01-foundation-motion-infrastructure/01-RESEARCH.md` (section "## Package Legitimacy Audit"). For each row marked `SUS (too-new heuristic)`, the table already lists: registry, latest publish date, weekly download count, and source repo URL. Spot-check at minimum:
    1. `lenis` — https://www.npmjs.com/package/lenis (lowest download count of the flagged rows at ~1.14M weekly; confirm the repo link resolves to `github.com/darkroomengineering/lenis` and the README matches the smooth-scroll library described)
    2. `react-router` — https://www.npmjs.com/package/react-router (confirm `github.com/remix-run/react-router`, v8.3.0, and that it is NOT `react-router-dom`, which is intentionally excluded per the v7→v8 migration)
    3. `tailwindcss` / `@tailwindcss/vite` — https://www.npmjs.com/package/tailwindcss (confirm `github.com/tailwindlabs/tailwindcss`)
    Confirm zero packages in the table carry a `[SLOP]` verdict (the audit reports none did).
  </how-to-verify>
  <resume-signal>Type "approved" to unblock 01-02-PLAN.md's install task, or describe which package(s) need further investigation before proceeding.</resume-signal>
  <read_first>.planning/phases/01-foundation-motion-infrastructure/01-RESEARCH.md (section: Package Legitimacy Audit)</read_first>
  <acceptance_criteria>
    - Human has explicitly typed "approved" (or equivalent affirmative) in response to this checkpoint
    - No package in the audit table carries an unresolved `[SLOP]` verdict
    - Response is recorded before 01-02-PLAN.md's Task 1 (npm install) begins execution
  </acceptance_criteria>
  <verify>
    <human-check>Human types "approved" (or flags specific packages for further review) before execution proceeds to 01-02-PLAN.md.</human-check>
  </verify>
  <done>Human has responded "approved" to this checkpoint; no unresolved SLOP-verdict packages remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local `node_modules` | Every dependency this phase installs crosses this boundary; a malicious/typosquatted package here executes arbitrary code on this machine via install scripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm install (all 13 packages in 01-RESEARCH.md Standard Stack) | high | mitigate | This checkpoint: human reviews the Package Legitimacy Audit's `SUS`-flagged rows (download counts, source repo identity, publish cadence) and explicitly approves before 01-02-PLAN.md's `npm install` runs. Lockfile (`package-lock.json`) committed in 01-02 pins exactly what was reviewed. |

</threat_model>

<verification>
Human types "approved" in response to the checkpoint above. No automated command applies — this plan produces no files.
</verification>

<success_criteria>
Human has explicitly approved the package legitimacy audit; 01-02-PLAN.md is unblocked to run `npm install`.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-motion-infrastructure/01-01-SUMMARY.md` when done
</output>
