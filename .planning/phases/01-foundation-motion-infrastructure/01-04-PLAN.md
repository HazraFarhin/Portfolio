---
phase: 01-foundation-motion-infrastructure
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/components/ui/Button.tsx
  - src/components/ui/Button.test.tsx
  - src/components/ui/Card.tsx
  - src/components/ui/Card.test.tsx
  - src/components/ui/Typography.tsx
  - src/components/ui/Typography.test.tsx
autonomous: true
requirements: [QUAL-01, QUAL-02]

must_haves:
  truths:
    - "Button, Card, and Typography components exist in src/components/ui/ and are the only implementations of these visual patterns in the codebase -- no per-section redefinition of button/card/typography styling elsewhere"
    - "Primary CTA button label renders in full at all viewport widths with no truncation or ellipsis"
  artifacts:
    - path: "src/components/ui/Button.tsx"
      provides: "Button -- primary/ghost variants, pill shape, motion-safe hover lift, polymorphic <a>/<button> render via optional href prop"
    - path: "src/components/ui/Card.tsx"
      provides: "Card -- glass variant (backdrop-blur-lg, rounded-3xl, low-opacity secondary background)"
    - path: "src/components/ui/Typography.tsx"
      provides: "Label / Body / Heading / Display typography primitives mapping 1:1 to the four declared type roles"
  key_links:
    - from: "src/components/ui/Button.tsx"
      to: "src/lib/cn.ts"
      via: "merges base + variant + caller-supplied className via cn()"
      pattern: "cn\\("
  prohibitions:
    - requirement_id: QUAL-02
      category: safety
      status: unverified
      flagged: true
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT leave CSS-only hover/lift motion ungated by prefers-reduced-motion -- Button's hover lift/translate effect must use a motion-safe: Tailwind variant (or equivalent CSS media guard) so it also disables under the OS setting, not just GSAP-driven ScrollTrigger reveals built in 01-05-PLAN.md."
    - requirement_id: QUAL-02
      category: safety
      status: unverified
      flagged: true
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT let 'disable non-essential motion' be interpreted broadly enough to also suppress necessary state feedback -- Button's focus-visible ring and hover color/border change must remain visible and functional even when prefers-reduced-motion is enabled; reduced motion removes decorative movement, not state feedback."
---

<objective>
Build the three UI primitives locked in scope by D-08 (exactly Button, Card, Typography -- no Container/Section wrapper this phase) styled to the Axisform aesthetic now per D-07 (glass-morphism cards, pill dark-chrome buttons, oversized editorial type), consuming the `@theme` tokens locked in 01-02-PLAN.md.

Purpose: Every later phase (2, 3) composes these three primitives rather than redefining button/card/typography styling per section -- this plan is what makes success criterion #4 concretely reachable.
Output: `Button.tsx`, `Card.tsx`, `Typography.tsx` in `src/components/ui/`, each with a passing Vitest + Testing Library test file.
</objective>

<execution_context>
@/Users/hazrafarhin/Desktop/Repositories/Portfolio/.claude/gsd-core/workflows/execute-plan.md
@/Users/hazrafarhin/Desktop/Repositories/Portfolio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-motion-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-motion-infrastructure/01-UI-SPEC.md
</context>

## Artifacts this phase produces

- `src/components/ui/Button.tsx` — exports `Button({ variant, href, className, children, ...rest })`
- `src/components/ui/Card.tsx` — exports `Card({ variant, className, children })`
- `src/components/ui/Typography.tsx` — exports `Label`, `Body`, `Heading`, `Display`
- corresponding `*.test.tsx` files for each

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Button primitive (primary/ghost, pill, motion-safe hover lift)</name>
  <files>src/components/ui/Button.tsx, src/components/ui/Button.test.tsx</files>
  <read_first>.planning/phases/01-foundation-motion-infrastructure/01-UI-SPEC.md (sections: Component Inventory, Surface & Elevation Notes, Copywriting Contract), src/lib/cn.ts (from 01-02-PLAN.md)</read_first>
  <behavior>
    - Test 1: With no `href` prop, `Button` renders a native `<button type="button">` element.
    - Test 2: With an `href` prop set, `Button` renders a native `<a>` element with that `href` attribute (not a `<button>`).
    - Test 3: `variant="ghost"` (vs. the default `"primary"`) produces a different className string -- assert the two variants' rendered `className` are not equal.
    - Test 4: A long CTA string (e.g. the exact copy `"View Selected Work →"`) renders as full, untruncated text content -- assert `screen.getByText('View Selected Work →')` matches and the rendered element's className contains no `truncate`/`overflow-hidden`/`text-ellipsis` utility.
    - Test 5 (QUAL-02 prohibition): the rendered className contains `motion-safe:hover:-translate-y-0.5` (or the exact hover-lift utility chosen), AND does not contain that same translate utility without the `motion-safe:` prefix (i.e. no unguarded `hover:-translate-y` substring exists outside the `motion-safe:`-prefixed one).
  </behavior>
  <action>
    Define `interface ButtonProps { variant?: 'primary' | 'ghost'; href?: string; className?: string; children: ReactNode }` (plus rest-spread of native `ButtonHTMLAttributes<HTMLButtonElement>` or `AnchorHTMLAttributes<HTMLAnchorElement>` depending on branch). Export `Button({ variant = 'primary', href, className, children, ...rest }: ButtonProps)`. When `href` is defined, render `<a href={href} className={cn(base, variantClasses[variant], className)} {...rest}>{children}</a>`; otherwise render `<button type="button" className={cn(base, variantClasses[variant], className)} {...rest}>{children}</button>`. Base classes: `inline-flex items-center justify-center gap-2 rounded-full px-lg py-md min-h-11 text-label font-extrabold tracking-wide transition-colors motion-safe:transition-transform motion-safe:hover:-translate-y-0.5 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent`. `variant="primary"` adds `border border-accent/40 text-foreground hover:border-accent hover:bg-accent/10`. `variant="ghost"` adds `border border-line text-foreground hover:border-foreground/40`. Use `cn()` from `src/lib/cn.ts` to merge base + variant + caller `className` (never string-concatenate).
  </action>
  <acceptance_criteria>
    - `src/components/ui/Button.tsx` exports `Button`
    - All 5 behaviors above have a corresponding passing test
    - `grep -c 'motion-safe:hover:-translate-y' src/components/ui/Button.tsx` reports at least 1
    - `grep -E 'hover:-translate-y' src/components/ui/Button.tsx | grep -v 'motion-safe:' | wc -l` reports 0 (no unguarded hover-lift utility)
    - `npx vitest run src/components/ui/Button.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/ui/Button.test.tsx</automated>
  </verify>
  <done>Button renders as <a> or <button> depending on href, applies distinct primary/ghost styling, never truncates its label, and gates hover-lift behind motion-safe: -- all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Card primitive (glass variant)</name>
  <files>src/components/ui/Card.tsx, src/components/ui/Card.test.tsx</files>
  <read_first>.planning/phases/01-foundation-motion-infrastructure/01-UI-SPEC.md (section: Surface & Elevation Notes), src/lib/cn.ts (from 01-02-PLAN.md)</read_first>
  <behavior>
    - Test 1: `Card` renders its `children` inside a single wrapping element whose className contains `rounded-3xl` and `backdrop-blur-lg`.
    - Test 2 (overflow/long-text UI consideration): rendering a long child paragraph (150+ characters) produces no `overflow-hidden`/`truncate`/fixed-`h-` (fixed height) class on the wrapper -- content-driven height, not clipped.
    - Test 3: a caller-supplied `className` prop is merged into (not replacing) the base glass styling -- assert the rendered className contains both the base `rounded-3xl` token and the caller's custom class.
  </behavior>
  <action>
    Define `interface CardProps { variant?: 'glass'; className?: string; children: ReactNode }` (only `'glass'` is a valid value this phase per D-08 -- no second variant, no Container/Section wrapper component in this file or anywhere else in `src/components/ui/`). Export `Card({ variant = 'glass', className, children }: CardProps)` rendering a `<div className={cn('rounded-3xl border border-line bg-secondary/55 backdrop-blur-lg shadow-[0_24px_80px_rgba(0,0,0,0.5)] p-lg', className)}>{children}</div>`.
  </action>
  <acceptance_criteria>
    - `src/components/ui/Card.tsx` exports `Card`
    - All 3 behaviors above have a corresponding passing test
    - No `Container` or `Section` export exists anywhere in `src/components/ui/` after this task
    - `npx vitest run src/components/ui/Card.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/ui/Card.test.tsx</automated>
  </verify>
  <done>Card renders the glass surface (24px radius, 16px blur), never clips long content, merges caller className -- all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Typography primitives (Label / Body / Heading / Display)</name>
  <files>src/components/ui/Typography.tsx, src/components/ui/Typography.test.tsx</files>
  <read_first>.planning/phases/01-foundation-motion-infrastructure/01-UI-SPEC.md (section: Typography)</read_first>
  <behavior>
    - Test 1: `Label` renders a `<span>` by default with className containing `text-label` and `uppercase`.
    - Test 2: `Body` renders a `<p>` by default with className containing `text-body`.
    - Test 3: `Heading` renders an `<h2>` by default with className containing `text-heading` and `font-extrabold`.
    - Test 4: `Display` renders with className containing `text-display` and `font-extrabold`.
    - Test 5: each of the four accepts an `as` prop overriding the rendered tag (e.g. `<Heading as="h1">` renders an `<h1>`, not `<h2>`).
    - Test 6 (long-text UI consideration): a 400+ character string passed as `Body`'s children renders in full with no truncation class present.
  </behavior>
  <action>
    Define a shared `interface TypographyProps { as?: ElementType; className?: string; children: ReactNode }`. Export four components: `Label({ as: Tag = 'span', className, children, ...rest })` with base classes `text-label font-normal uppercase tracking-[0.14em] text-muted-foreground`; `Body({ as: Tag = 'p', ... })` with `text-body font-normal text-foreground`; `Heading({ as: Tag = 'h2', ... })` with `text-heading font-extrabold tracking-tight text-foreground`; `Display({ as: Tag = 'p', ... })` with `text-display font-extrabold tracking-tight text-foreground`. Each renders `<Tag className={cn(baseClasses, className)} {...rest}>{children}</Tag>`. Use `cn()` from `src/lib/cn.ts` for class merging in every component.
  </action>
  <acceptance_criteria>
    - `src/components/ui/Typography.tsx` exports `Label`, `Body`, `Heading`, `Display`
    - All 6 behaviors above have a corresponding passing test
    - `npx vitest run src/components/ui/Typography.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/ui/Typography.test.tsx</automated>
  </verify>
  <done>All four typography roles render with their locked size/weight/line-height token classes, support tag override via `as`, never truncate long content -- all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-supplied `className`/`children` props -> rendered DOM | Every primitive accepts free-form React children and an optional className string from its caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | none new -- no packages installed in this plan | n/a | not applicable | Already covered by 01-01/01-02's checkpoint + lockfile; this plan only consumes `clsx`/`tailwind-merge` already installed |
| T-01-07 | Tampering (XSS-adjacent) | `children`/`className` passed through to JSX | low | accept | React's JSX escaping means `children` text is never interpreted as HTML; no `dangerouslySetInnerHTML` is used anywhere in these three components -- standard React behavior, no bespoke risk introduced |
| T-01-08 | Information Disclosure | none -- purely presentational components, no data fetched or stored | n/a | not applicable | No data source exists in this plan |

</threat_model>

<verification>
`npx vitest run src/components/ui/Button.test.tsx src/components/ui/Card.test.tsx src/components/ui/Typography.test.tsx` exits 0.
</verification>

<success_criteria>
Button, Card, and Typography exist in `src/components/ui/`, each independently tested, styled to the Axisform-derived tokens locked in 01-02-PLAN.md, with the motion-safe hover-lift and focus-visible prohibitions satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-motion-infrastructure/01-04-SUMMARY.md` when done
</output>
