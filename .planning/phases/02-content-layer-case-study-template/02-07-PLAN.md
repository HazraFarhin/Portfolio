---
phase: 02-content-layer-case-study-template
plan: 7
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - src/components/case-study/DraftBadge.tsx
  - src/components/case-study/DraftBadge.test.tsx
  - src/components/case-study/Overview.tsx
  - src/components/case-study/Overview.test.tsx
autonomous: true
requirements: [CASE-02, CASE-03]

must_haves:
  truths:
    - "Overview row order (Client, Industry, My Role, Team, Timeline, Links) is a fixed, hardcoded template order written directly in Overview's JSX (D-11) -- never derived from object key iteration order or prop-declaration order"
    - "Overview's Links row renders the muted 'Coming soon' Label when external_link is absent, and two ghost-variant Buttons labeled 'Prototype' and 'Live Site' side by side when present -- never a broken/dead link"
    - "Overview renders inside a single glass Card, with property-name cells styled as Label (uppercase, tracked, muted) and value cells styled as Body, per the Overview Table Styling contract"
    - "DraftBadge renders when status === 'Draft', renders nothing (null) when status === 'Published' -- visibility toggles purely on the status field with no component change required (D-12)"
    - "DraftBadge's rendered className never references the destructive/red color token or any warning-style treatment -- it is a quiet, neutral status marker"
  artifacts:
    - src/components/case-study/Overview.tsx
    - src/components/case-study/DraftBadge.tsx
  key_links:
    - "Overview/DraftBadge both accept frontmatter fields typed against Plan 02-03's schema.ts CaseStudyFrontmatter shape (client/industry/role/team/timeline/external_link for Overview, status for DraftBadge) -- Plan 02-09's CaseStudyPage passes these directly from the loaded CaseStudy object without remapping field names"
    - "Overview is the CASE-03 load-bearing component: role and outcome must be reachable without scrolling, which depends on Plan 02-09 rendering Overview immediately after title/summary/draft-badge and before every other section"
---

<objective>
Build `Overview` (the frontmatter-driven, skimmable metadata block satisfying CASE-03's "role/outcome visible without scrolling" requirement, per D-08) and `DraftBadge` (D-03's unobtrusive draft-content marker), the two components that read frontmatter fields directly rather than parsed markdown-body sections.

Purpose: Overview is the single component this phase builds specifically to satisfy CASE-03 -- no other plan in Phase 2 addresses "role/outcome skimmable near the top" directly. Per D-08, the Overview table (client/industry/role/team/timeline/links) must render directly below the title and one-line summary, before any other section -- this component's own structure is what makes that placement possible; Plan 02-09 is responsible for actually positioning it there in the full page. DraftBadge is D-03's locked visual requirement, rendered immediately below the title per the template's layout order. Both are pure, frontmatter-driven presentational components with no markdown parsing involved, making them independently testable against fixture prop objects.
Output: `Overview.tsx` and `DraftBadge.tsx`, both composed entirely from Phase 1's `Button`/`Card`/`Typography`/`cn()` primitives and typed against Plan 02-03's `CaseStudyFrontmatter` schema type.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-layer-case-study-template/02-UI-SPEC.md
@.planning/phases/02-content-layer-case-study-template/02-RESEARCH.md
@.planning/phases/02-content-layer-case-study-template/02-PATTERNS.md
@src/components/ui/Typography.tsx
@src/components/ui/Card.tsx
@src/components/ui/Button.tsx
@src/lib/cn.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DraftBadge component</name>
  <files>src/components/case-study/DraftBadge.tsx, src/components/case-study/DraftBadge.test.tsx</files>
  <read_first>.planning/phases/02-content-layer-case-study-template/02-UI-SPEC.md (Draft Content Badge section, Color section's accent-reservation note excluding the draft badge), src/content/case-studies/schema.ts (once Plan 02-03 has executed -- import the `CaseStudyFrontmatter` type from there rather than redeclaring the status union locally)</read_first>
  <behavior>
    - `status="Draft"` -> renders an element containing the exact text "Draft content — pending final copy"
    - `status="Published"` -> renders null (no DOM node at all, not an empty/hidden element)
    - The rendered badge's className never contains "destructive" or any red/warning-style utility class
  </behavior>
  <action>Implement `DraftBadge` accepting props `{ status: Pick<CaseStudyFrontmatter, 'status'>['status'] }` (import `CaseStudyFrontmatter` from `'../../content/case-studies/schema'`). Return `null` immediately when `status !== 'Draft'`. Otherwise render a `span` with classes composed via `cn()`: `inline-flex items-center gap-xs rounded-full border border-line bg-secondary/40 px-sm py-xs`, containing a `Label` with the exact text "Draft content — pending final copy". Do not apply `--color-destructive`, `bg-destructive`, `text-destructive`, `border-destructive`, or any Tailwind red-family utility class anywhere in this component -- per UI-SPEC's explicit exclusion, this must read as a quiet, neutral marker reusing the same visual register as the existing `metaStatus` Label already shipped in Hero's Card (Phase 1 precedent).</action>
  <acceptance_criteria>
    <source>Component file exports a named `DraftBadge` function accepting exactly `{ status }`</source>
    <behavior>Rendering with `status="Published"` produces no DOM output; rendering with `status="Draft"` produces the exact copy text</behavior>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/case-study/DraftBadge.test.tsx</automated>
  </verify>
  <done>DraftBadge.tsx and DraftBadge.test.tsx both exist; the badge renders only for Draft status with the exact locked copy and no destructive/warning styling; the test file passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Overview component</name>
  <files>src/components/case-study/Overview.tsx, src/components/case-study/Overview.test.tsx</files>
  <read_first>.planning/phases/02-content-layer-case-study-template/02-UI-SPEC.md (Overview Table Styling section), src/components/ui/Card.tsx, src/components/ui/Button.tsx, src/content/case-studies/schema.ts (once Plan 02-03 has executed)</read_first>
  <behavior>
    - Given `client`, `industry`, `role`, `team`, `timeline` all populated and `external_link` present, renders 6 Label property-name cells in this exact DOM order: "Client", "Industry", "My Role", "Team", "Timeline", "Links" -- and the Links row renders two ghost-variant Buttons labeled "Prototype" and "Live Site", both with `href` equal to `external_link` (both buttons share the one `external_link` field -- this is UI-SPEC's exact locked contract, not a mismatch to fix)
    - Given `external_link` is `undefined`, the Links row renders the muted Label text "Coming soon" instead of any Button
    - The whole Overview block is wrapped in exactly one `Card` element (its rendered className contains `rounded-3xl`, matching `Card`'s own classes)
    - Value cells (client/industry/role/team/timeline) render via `Body`, property-name cells render via `Label`
  </behavior>
  <action>Implement `Overview` accepting props `{ client, industry, role, team, timeline, external_link }: Pick<CaseStudyFrontmatter, 'client' | 'industry' | 'role' | 'team' | 'timeline' | 'external_link'>` (import `CaseStudyFrontmatter` from `'../../content/case-studies/schema'`). Wrap everything in a single `<Card>`. Inside, render a responsive definition grid (`grid grid-cols-1 md:grid-cols-[auto_1fr] gap-x-md gap-y-sm` via `cn()`) containing exactly 6 hardcoded rows, written literally in this order in the JSX -- never generated by mapping over `Object.entries(props)` or any other data-driven iteration, since row order must be guaranteed by source-code structure alone: (1) `Label`"Client" + `Body`{client}, (2) `Label`"Industry" + `Body`{industry}, (3) `Label`"My Role" + `Body`{role}, (4) `Label`"Team" + `Body`{team}, (5) `Label`"Timeline" + `Body`{timeline}, (6) `Label`"Links" + either (`external_link` present) two `Button` elements side-by-side (`variant="ghost"`, `href={external_link}`, labeled "Prototype" and "Live Site" respectively -- both pointing at the same single `external_link` value, since the schema has only one link field) or (`external_link` absent) a `Label` reading exactly "Coming soon".</action>
  <acceptance_criteria>
    <source>Component file exports a named `Overview` function accepting the 6 named frontmatter fields as props</source>
    <behavior>Querying all rendered Label elements' text content in DOM order returns exactly `['Client', 'Industry', 'My Role', 'Team', 'Timeline', 'Links']` (or `['Client', 'Industry', 'My Role', 'Team', 'Timeline']` plus the Links row's conditional content) regardless of prop values</behavior>
    <behavior>Rendering with `external_link` undefined never produces a Button element in the Links row; rendering with it defined never produces the "Coming soon" text</behavior>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/case-study/Overview.test.tsx</automated>
  </verify>
  <done>Overview.tsx and Overview.test.tsx both exist; the component renders all 6 rows in fixed template order inside one Card, correctly branches the Links row on external_link presence, and the test file passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Validated frontmatter field values -> Overview's rendered `<a href>` (via Button) | `external_link` has already passed Plan 02-03's `.refine()`-hardened `.url()` check restricting it to http/https before it ever reaches this component -- Overview trusts the value it receives, it does not re-validate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-11 | Tampering | Overview rendering `external_link` into two `<a href>` elements | low | accept | The value has already been schema-validated and scheme-restricted by Plan 02-03 before reaching this component; Overview performs no additional escaping/validation of its own, consistent with the "single validation gate" design already established |
| T-02-12 | Spoofing (structural) | Overview row order accidentally depending on iteration/object-key order | low | mitigate | Rows are written as 6 literal, hardcoded JSX elements in fixed order, never generated via `Object.entries()`/mapping over props -- guarantees CASE-03's skimmable, predictable layout regardless of how props are constructed upstream |

</threat_model>

<verification>
`npx vitest run src/components/case-study/DraftBadge.test.tsx src/components/case-study/Overview.test.tsx` -- all tests green.
</verification>

<success_criteria>
Overview and DraftBadge are ready for Plan 02-09's capstone composition; Overview's fixed row order and Card wrapping satisfy CASE-03's skimmability requirement structurally, independent of viewport-specific visual verification (which remains a manual UAT check owned by Plan 02-09).
</success_criteria>

<output>
Create `.planning/phases/02-content-layer-case-study-template/02-07-SUMMARY.md` when done
</output>
