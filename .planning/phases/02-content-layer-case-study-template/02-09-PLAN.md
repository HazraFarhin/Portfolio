---
phase: 02-content-layer-case-study-template
plan: 9
type: execute
wave: 4
depends_on: ["02-04", "02-05", "02-06", "02-07", "02-08"]
files_modified:
  - src/components/case-study/Challenge.tsx
  - src/components/case-study/Challenge.test.tsx
  - src/components/case-study/LearningsReflections.tsx
  - src/components/case-study/LearningsReflections.test.tsx
  - src/components/case-study/NextProject.tsx
  - src/components/case-study/NextProject.test.tsx
  - src/routes/not-found.tsx
  - src/routes/not-found.test.tsx
  - src/routes/case-study.tsx
  - src/routes/case-study.test.tsx
  - src/router.tsx
autonomous: true
requirements: [CASE-01, CASE-02, CASE-03, CASE-04]

must_haves:
  truths:
    - "Slugs containing dots (astrosure.ai, adreport.io) are matched as literal, single path segments against React Router's :slug param -- no split/truncation at the dot; verified by an explicit test route for these two slugs."
    - "An empty/missing :slug segment does not match the /case-study/:slug route pattern at all -- falls through to the app's catch-all/404 route, not a crash in CaseStudyPage."
    - "Fixed inter-section spacing (py-xl on mobile, py-2xl on desktop, per UI-SPEC) applies uniformly to every one of the 8 body sections regardless of that section's content length -- sections never visually merge/collide."
    - "Section order (Overview, Tools Used, Outcome & Impact, The Challenge, Process, Solution, Learnings & Reflections, Next Project) is a fixed, hardcoded sequence written directly in CaseStudyPage's JSX -- never derived from markdown heading order in the source file."
    - "Overview (containing role) renders immediately after title/summary/draft-badge and before every other section, for all 6 real case-study files."
    - "An unmatched or deferred slug (e.g. /case-study/riyaah, a typo, or any slug not among the 6 loaded files) renders the 'Case study not found.' fallback inside CaseStudyPage itself, not a crash."
    - statement: "Title + one-line summary + Overview fit within the first viewport without scrolling on both a 1440x900 desktop viewport and a 375x812 mobile viewport, using the longest actual title/summary among the 6 real placeholder files -- requires the manual visual UAT check already specified in 02-VALIDATION.md's Manual-Only Verifications table, not a DOM-order assertion alone."
      verification: backstop
    - "Next Project always resolves to a real next case study for all 6 real slugs via Plan 02-08's circular getNextCaseStudy() -- NextProject itself never recomputes this logic, it only renders the already-computed nextTitle/nextSlug props."
    - "No hardcoded slug literal (cad, verzion-cloud-migration, tata-capital-ai-interface, mashreq, astrosure.ai, adreport.io) appears anywhere in src/routes/case-study.tsx or src/router.tsx source code (CASE-04's content-only-change guarantee)."
  artifacts:
    - src/components/case-study/Challenge.tsx
    - src/components/case-study/LearningsReflections.tsx
    - src/components/case-study/NextProject.tsx
    - src/routes/not-found.tsx
    - src/routes/case-study.tsx
  key_links:
    - "case-study.tsx composes Overview/DraftBadge (02-07), ToolsUsed/OutcomeImpact/ImagePlaceholder (02-04), Process (02-06), Solution (02-05), Challenge/LearningsReflections/NextProject (this plan), and getCaseStudyBySlug/getNextCaseStudy (02-08) -- it is the single point where every Phase 2 artifact converges"
    - "router.tsx's case-study/:slug and catch-all '*' routes are the only two entries this plan adds to Phase 1's route tree; no other file in the app ever needs to know about case-study routing"
---

<objective>
Build the 3 remaining leaf section/nav components (Challenge, Learnings & Reflections, Next Project), the app-level not-found fallback route (filling the small routing gap Phase 1 left open -- no catch-all route existed yet), and the `CaseStudyPage` route component itself: the capstone integration that, per D-07, composes every Phase 2 artifact into the literal, fixed document order D-11 requires -- reusing Phase 1's `Button`/`Card`/`Typography` primitives rather than introducing new one-off styling -- wired into `router.tsx` at `/case-study/:slug`.

Purpose: This is the phase's capstone plan. Every other Wave 1-3 plan built an isolated, independently-tested piece (schema, content files, section components, loader); this plan is where they are proven to compose correctly as a whole page satisfying CASE-01 (navigable per-slug pages, including the two dotted slugs), CASE-02 (all 7 sections in template order), CASE-03 (role/outcome above the fold), and CASE-04 (zero hardcoded slug literals in the routing/page layer). Per D-09, every section subcomponent this phase builds (including this plan's Challenge/LearningsReflections/NextProject) stays internal to the case-study template -- Phase 3's homepage build consumes only Plan 02-08's loader/metadata exports (`caseStudies`, `getCaseStudyBySlug`), never these components directly; that reuse decision is explicitly left to Phase 3, not locked here.
Output: `CaseStudyPage` mounted at `/case-study/:slug`, a sibling catch-all `*` route rendering a generic not-found page, and the 3 remaining section/nav components that only this integration point needed.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-layer-case-study-template/02-CONTEXT.md
@.planning/phases/02-content-layer-case-study-template/02-UI-SPEC.md
@.planning/phases/02-content-layer-case-study-template/02-RESEARCH.md
@.planning/phases/02-content-layer-case-study-template/02-VALIDATION.md
@src/router.tsx
@src/routes/home.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Challenge and Learnings & Reflections section components</name>
  <files>src/components/case-study/Challenge.tsx, src/components/case-study/Challenge.test.tsx, src/components/case-study/LearningsReflections.tsx, src/components/case-study/LearningsReflections.test.tsx</files>
  <read_first>.planning/phases/02-content-layer-case-study-template/02-04-PLAN.md (ToolsUsed.tsx/OutcomeImpact.tsx's established simple-markdown-section pattern -- follow the same shape), .planning/phases/02-content-layer-case-study-template/02-UI-SPEC.md (UI Considerations row: Challenge pull-quote is the template's one explicitly-optional sub-element), src/components/case-study/ImagePlaceholder.tsx (once Plan 02-04 has executed)</read_first>
  <behavior>
    - Challenge: given a non-empty `content` string, renders an h2 `Heading` reading "The Challenge" followed by the markdown-rendered body
    - Challenge: given a `content` string containing a markdown blockquote (`> ...`), renders that blockquote distinctly styled (e.g. an italic `Body` with a left accent border) as the template's optional pull-quote; given a `content` string with no blockquote, renders no empty blockquote shell at all
    - LearningsReflections: given a non-empty `content` string, renders an h2 `Heading` reading "Learnings & Reflections" followed by the markdown-rendered body
    - Both components: `img` markup embedded in `content` remaps to `ImagePlaceholder` (defensive, per D-02 -- neither section has a structural image slot of its own per D-11, only Process's 3 sub-stages and Solution's 2 centerpieces do)
    - Neither component ever renders a native image element
  </behavior>
  <action>Implement `Challenge` accepting `{ content: string }`: a `<section>` with an h2 `Heading` "The Challenge", then a `Markdown` block rendering `content` with `components` remapping `p`/`li` to `Body`, `img` to `ImagePlaceholder` (`caption="Challenge artifact — pending" size="stage"`), and `blockquote` to a `Body` styled with `cn('italic border-l-2 border-line pl-md')` (the pull-quote treatment -- renders only when the source markdown actually contains a blockquote; react-markdown simply emits nothing for this tag when absent, so no extra conditional logic is needed). Implement `LearningsReflections` accepting `{ content: string }` following the exact same structural pattern as `ToolsUsed`/`OutcomeImpact` (Plan 02-04): a `<section>` with an h2 `Heading` "Learnings & Reflections", then a `Markdown` block remapping `p`/`li` to `Body` and `img` to `ImagePlaceholder` (`caption="Reflection artifact — pending" size="stage"`). Neither component defines a bespoke empty-state fallback string -- UI-SPEC's Copywriting Contract only specifies empty-state copy for Tools Used, Outcome & Impact, and Overview's Links row; Challenge and Learnings & Reflections have no listed fallback because D-01 guarantees all 6 authored files populate them, so a plain markdown render of an empty string (rendering nothing) is the correct, spec-accurate behavior here, not an oversight. No `<Card>` wrapper on either component.</action>
  <acceptance_criteria>
    <source>Both files export named functions (`Challenge`, `LearningsReflections`) accepting exactly `{ content: string }`</source>
    <behavior>Rendering Challenge with a `content` string containing `> A quote.` shows that quote text styled distinctly from regular body paragraphs (a different className)</behavior>
    <behavior>Rendering either component with a `content` string containing no blockquote produces no blockquote element in the output</behavior>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/case-study/Challenge.test.tsx src/components/case-study/LearningsReflections.test.tsx</automated>
  </verify>
  <done>Challenge.tsx/test.tsx and LearningsReflections.tsx/test.tsx all exist; both render their markdown content with the correct heading, the pull-quote renders only when present in source content, and both test files pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NextProject and the app-level not-found route</name>
  <files>src/components/case-study/NextProject.tsx, src/components/case-study/NextProject.test.tsx, src/routes/not-found.tsx, src/routes/not-found.test.tsx</files>
  <read_first>.planning/phases/02-content-layer-case-study-template/02-UI-SPEC.md (Copywriting Contract's Primary CTA and Empty state rows), src/components/ui/Button.tsx, src/routes/home.tsx</read_first>
  <behavior>
    - NextProject: given `nextTitle="Verzion Cloud Migration"` and `nextSlug="verzion-cloud-migration"`, renders an h2 `Heading` reading "Next Project" and a primary-variant `Button` with `href="/case-study/verzion-cloud-migration"` whose accessible name is exactly "Verzion Cloud Migration →"
    - NotFoundRoute (`src/routes/not-found.tsx`, no props): renders the text "Page not found." and a ghost-variant `Button` with `href="/"` and accessible name containing "Back to home"
  </behavior>
  <action>Implement `NextProject` accepting `{ nextTitle: string; nextSlug: string }`: a `<section>` with an h2 `Heading` "Next Project" and a `Button` (`variant="primary"`, `href={`/case-study/${nextSlug}`}`) whose children is the literal template string `${nextTitle} →`. This component performs no lookup/computation of its own -- the caller (Task 3's `CaseStudyPage`) is responsible for calling Plan 02-08's `getNextCaseStudy()` and passing the result's already-resolved title/slug down as plain string props. Implement `NotFoundRoute` as a default-exported, no-props route component mirroring `HomeRoute`'s plain-function shape (`src/routes/home.tsx`): centered `Body` reading "Page not found.", a second `Body` reading "This page doesn't exist. Let's get you back to the homepage.", and a `Button` (`variant="ghost"`, `href="/"`) reading "← Back to home". This is the catch-all destination Task 3 wires into `router.tsx` at `path: '*'` -- it fills the small routing gap Phase 1 intentionally left open (no catch-all route existed before this phase), needed for the empty/missing-slug must-have below to hold true.</action>
  <acceptance_criteria>
    <source>NextProject.tsx exports a named `NextProject` function accepting exactly `{ nextTitle, nextSlug }`; not-found.tsx default-exports a no-props `NotFoundRoute` function</source>
    <behavior>NextProject's rendered link href and accessible name both derive purely from its two string props, with no internal case-study data lookup</behavior>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/case-study/NextProject.test.tsx src/routes/not-found.test.tsx</automated>
  </verify>
  <done>NextProject.tsx/test.tsx and not-found.tsx/test.tsx all exist; NextProject is a pure presentational component with no data-lookup logic of its own; NotFoundRoute renders the generic page-level fallback copy; both test files pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CaseStudyPage integration and router wiring</name>
  <files>src/routes/case-study.tsx, src/routes/case-study.test.tsx, src/router.tsx</files>
  <read_first>src/router.tsx (the exact commented reserved slot to fill in), .planning/phases/02-content-layer-case-study-template/02-UI-SPEC.md (Case-Study Page Layout & Composition -- the full 12-item literal document order, Section rhythm, Above-the-fold budget, Empty state copy), .planning/phases/02-content-layer-case-study-template/02-RESEARCH.md (Common Pitfalls 2 and 4 -- dotted-slug and unmatched-slug handling), src/content/case-studies/loader.ts (once Plan 02-08 has executed), src/components/case-study/{Overview,DraftBadge,ToolsUsed,OutcomeImpact,ImagePlaceholder,Process,Solution,Challenge,LearningsReflections,NextProject}.tsx (once their respective plans have executed)</read_first>
  <behavior>
    - Rendering `CaseStudyPage` for a real, fully-populated slug (e.g. `mashreq`) produces, in this exact DOM order, h2 headings reading: "Overview", "Tools Used", "Outcome & Impact", "The Challenge", "Process", "Solution", "Learnings & Reflections", "Next Project" -- and the case study's `role` text appears in the DOM before the "Tools Used" heading
    - Rendering `CaseStudyPage` for `astrosure.ai` resolves and displays that exact case study's title (not `astrosure`'s, not a blank page) -- proving no dot-truncation bug; same for `adreport.io`
    - Rendering `CaseStudyPage` for an unmatched slug (`riyaah` -- a real deferred slug per D-10, not among the 6 loaded files) renders the text "Case study not found." and a "Back to home" link, without throwing
    - Rendering the app's route tree (case-study/:slug sibling with a catch-all `*`) at an empty slug path (`/case-study/`) does NOT render CaseStudyPage's "Case study not found." copy -- it renders the catch-all's distinct "Page not found." copy instead, proving the empty segment falls through to the 404 route rather than matching `:slug`
    - Every one of the 7 non-Overview section wrappers (Tools Used through Next Project) has a className containing both a mobile (`py-xl`) and a desktop (`md:py-2xl`) spacing utility
  </behavior>
  <action>Implement `CaseStudyPage` (default export) in `src/routes/case-study.tsx`: read `const { slug } = useParams<{ slug: string }>()` from `'react-router'`; call `const caseStudy = slug ? getCaseStudyBySlug(slug) : undefined` (imported from `'../content/case-studies/loader'`). When `!caseStudy`, return the case-study-specific not-found fallback: a `Body` reading "Case study not found.", a `Body` reading "This project isn't available yet. Browse the case studies that are live today.", and a `Button` (`variant="ghost"`, `href="/"`) reading "← Back to home" -- distinct copy from the app-level `NotFoundRoute` (Task 2), since this is a data-lookup 404 for a slug that matched the route pattern, not a route-pattern-mismatch 404. When `caseStudy` is found, render this literal, hardcoded JSX sequence (never a `.map()` over a section-descriptor array or anything derived from markdown heading order) -- (1) `ImagePlaceholder` (`size="banner"`, `caption="Cover image — pending"`), (2) `Heading` (`as="h1"`) reading `caseStudy.title`, (3) `Body` (`className="max-w-[62ch]"`) reading `caseStudy.summary`, (4) `DraftBadge` (`status={caseStudy.status}`), then 8 sections each wrapped in its own `<section className={cn('py-xl', 'md:py-2xl')}>`: (5) `Overview` (spreading `client`/`industry`/`role`/`team`/`timeline`/`external_link` from `caseStudy`), (6) `ToolsUsed` (`content={caseStudy.sections.toolsUsed}`), (7) `OutcomeImpact` (`content={caseStudy.sections.outcomeImpact}`), (8) `Challenge` (`content={caseStudy.sections.challenge}`), (9) `Process` (`stages={caseStudy.sections.process}`), (10) `Solution` (`content={caseStudy.sections.solution}`), (11) `LearningsReflections` (`content={caseStudy.sections.learnings}`), (12) `NextProject`, computed from `const next = getNextCaseStudy(caseStudy.slug)` (imported from the loader) -- guard with `{next && <NextProject nextTitle={next.title} nextSlug={next.slug} />}` (defensive `if`, not a non-null assertion, so a hypothetical future edge case with 0-1 loaded case studies degrades gracefully instead of crashing, even though Plan 02-08's circular wraparound guarantees `next` is always defined for any of today's 6 real slugs). Do not write any of the 6 slug strings literally anywhere in this file -- the only slug-shaped value in this component is the `slug` variable read from `useParams()`. Then modify `src/router.tsx`: import `CaseStudyPage from './routes/case-study'` and `NotFoundRoute from './routes/not-found'`; replace the commented reserved slot with `{ path: 'case-study/:slug', element: <CaseStudyPage /> }`; add `{ path: '*', element: <NotFoundRoute /> }` as the last child in the same route array. In `case-study.test.tsx`, build the DOM-order/section/dotted-slug/not-found assertions using `MemoryRouter` + `Routes` + `Route` (`import { MemoryRouter, Routes, Route } from 'react-router'`) at `initialEntries={['/case-study/<slug>']}` wrapping `<Route path="case-study/:slug" element={<CaseStudyPage />} />` -- this exercises `useParams()` against the real content loaded via Plan 02-02's actual `.md` files (no mocking of the loader needed, since all 6 real files already exist). For the empty-slug-falls-to-catch-all case specifically, register both routes together in the same test-local `Routes` tree (`<Route path="case-study/:slug" .../>` sibling to `<Route path="*" element={<NotFoundRoute />} />`) at `initialEntries={['/case-study/']}`, and assert `NotFoundRoute`'s "Page not found." text renders, not `CaseStudyPage`'s "Case study not found." text.</action>
  <acceptance_criteria>
    <source>case-study.tsx default-exports `CaseStudyPage`; router.tsx's route array includes both `{ path: 'case-study/:slug', ... }` and `{ path: '*', ... }`</source>
    <test-command>npx vitest run src/routes/case-study.test.tsx</test-command>
    <behavior>All 5 behavior cases in the <behavior> block above pass, including the dotted-slug and empty-slug-falls-to-catch-all cases</behavior>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/routes/case-study.test.tsx && npm test</automated>
  </verify>
  <done>case-study.tsx composes all 12 layout items in fixed document order; router.tsx wires both the case-study route and the catch-all 404 route; astrosure.ai/adreport.io resolve correctly; an unmatched slug renders the in-page not-found fallback; an empty slug segment falls through to the app-level NotFoundRoute; the full test suite (`npm test`) passes.</done>
</task>

</tasks>

## Artifacts This Phase Produces

Consolidated list of every symbol/file created across all 9 Phase 2 plans, for downstream phases (Phase 3's homepage build) to reference without re-reading every SUMMARY:

| Plan | File(s) | Key Exports |
|------|---------|--------------|
| 02-01 | `package.json`, `package-lock.json` | `zod`, `js-yaml`, `react-markdown` (deps); `@types/js-yaml` (devDep) |
| 02-02 | `src/content/case-studies/{cad,verzion-cloud-migration,tata-capital-ai-interface,mashreq,astrosure.ai,adreport.io}.md` | 6 content files (data only, no code exports) |
| 02-03 | `src/content/case-studies/schema.ts`, `schema.test.ts`, `parse.ts`, `parse.test.ts` | `CaseStudyFrontmatterSchema`, `CaseStudyFrontmatter`, `CaseStudy` (types); `parseCaseStudyFile`, `splitBodyIntoSections` (functions) |
| 02-04 | `src/components/case-study/ImagePlaceholder.tsx`, `ToolsUsed.tsx`, `OutcomeImpact.tsx` (+ their `.test.tsx`) | `ImagePlaceholder`, `ToolsUsed`, `OutcomeImpact` |
| 02-05 | `src/components/case-study/Solution.tsx` (+ `.test.tsx`) | `Solution` |
| 02-06 | `src/components/case-study/Process.tsx` (+ `.test.tsx`) | `Process`, `ProcessStages` (type) |
| 02-07 | `src/components/case-study/Overview.tsx`, `DraftBadge.tsx` (+ their `.test.tsx`) | `Overview`, `DraftBadge` |
| 02-08 | `src/content/case-studies/loader.ts` (+ `.test.ts`) | `caseStudies`, `getCaseStudyBySlug`, `getNextCaseStudy`, `loadCaseStudiesFromRawFiles` |
| 02-09 | `src/components/case-study/Challenge.tsx`, `LearningsReflections.tsx`, `NextProject.tsx` (+ their `.test.tsx`); `src/routes/not-found.tsx` (+ `.test.tsx`); `src/routes/case-study.tsx` (+ `.test.tsx`); `src/router.tsx` (modified) | `Challenge`, `LearningsReflections`, `NextProject`, `NotFoundRoute`, `CaseStudyPage` |

Phase 3's homepage build should import `caseStudies`/`getCaseStudyBySlug` from `src/content/case-studies/loader.ts` for its "Selected Work" grid (per 02-CONTEXT.md D-09) -- not any of the section components above, which remain internal to the case-study template per D-09.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| URL path segment (`:slug`) -> `getCaseStudyBySlug()` lookup -> rendered page | The one place in this phase where an arbitrary browser-navigable string (not just first-party content) reaches the app -- a user (or a bookmarked/typed URL) can request any slug, including deferred or nonexistent ones |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-15 | Denial of Service | `CaseStudyPage` crashing on an unmatched/deferred slug | high | mitigate | `getCaseStudyBySlug(slug)` returning `undefined` is explicitly branched before any field access (`caseStudy.title`, etc.) -- the not-found fallback renders instead of a `TypeError` on `undefined.title`, per RESEARCH.md Pitfall 4 |
| T-02-16 | Denial of Service | An empty/missing `:slug` route segment reaching no matching route and rendering a blank page or crash | medium | mitigate | A sibling catch-all `{ path: '*' }` route renders `NotFoundRoute` for any path (including a trailing-slash/empty `case-study/` segment) that doesn't match `case-study/:slug` -- this app-level 404 route did not exist before this plan and is added specifically to close this gap |
| T-02-17 | Tampering (structural) | Section order accidentally depending on markdown heading order or object-key iteration instead of source-code structure | low | mitigate | `CaseStudyPage`'s 8 body sections are written as literal, hardcoded JSX elements in fixed order -- never a `.map()` over a section-descriptor array derived from parsed content, guaranteeing CASE-02's template-fidelity requirement (D-11) regardless of what order headings happen to appear in a given `.md` file |

</threat_model>

<verification>
`npm test` -- the full suite (Phase 1's existing tests plus all of Phase 2's new tests across Plans 02-03 through 02-09) is green. Additionally, the manual code-review check from 02-VALIDATION.md's Per-Task Verification Map, scoped to exclude test files (which legitimately reference real slugs like `mashreq`/`astrosure.ai`/`riyaah` as test fixtures -- only non-test source is checked for CASE-04's content-only-change guarantee): `grep -rn "cad\|verzion-cloud-migration\|tata-capital-ai-interface\|mashreq\|astrosure\.ai\|adreport\.io" src/routes src/components src/router.tsx --include='*.ts' --include='*.tsx' | grep -v '\.test\.'` returns zero matches.
</verification>

<success_criteria>
Every one of the 6 featured case studies (including the two dotted slugs) is reachable at `/case-study/:slug` and renders all 7 CASE-02 sections plus the Next Project footer in the exact D-11 document order; role/outcome are structurally reachable before any other section (CASE-03); an unmatched slug and an empty slug segment both degrade gracefully instead of crashing; zero hardcoded slug literals exist in the routing/page layer (CASE-04). The above-the-fold viewport fit (item 7 in must_haves) remains a manual UAT check per 02-VALIDATION.md, to be confirmed during `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/02-content-layer-case-study-template/02-09-SUMMARY.md` when done
</output>
