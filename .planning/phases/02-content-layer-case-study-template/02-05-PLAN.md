---
phase: 02-content-layer-case-study-template
plan: 5
type: execute
wave: 3
depends_on: ["02-04"]
files_modified:
  - src/components/case-study/Solution.tsx
  - src/components/case-study/Solution.test.tsx
autonomous: true
requirements: [CASE-02]

must_haves:
  truths:
    - "Solution renders exactly 2 centerpiece-sized ImagePlaceholder blocks alongside its paragraph content, per the authoritative template's fixed image slots (D-11) -- this count never varies with the amount of authored prose"
    - "Solution never renders a real, native image element regardless of markdown body content (D-02) -- any image markup embedded in the body is remapped to ImagePlaceholder, same as ToolsUsed/OutcomeImpact"
    - "Solution's section heading renders as an h2 reading exactly 'Solution'"
  artifacts:
    - src/components/case-study/Solution.tsx
  key_links:
    - "Solution is imported by Plan 02-09's CaseStudyPage as the 10th section in the fixed document order (D-11), rendered from caseStudy.sections.solution"
---

<objective>
Build the `Solution` case-study section component: the largest visual "centerpiece" section per the authoritative template, rendering the solution narrative paragraph via `react-markdown` plus 2 fixed `centerpiece`-sized `ImagePlaceholder` blocks (D-11's own structural image slots for this section, distinct from any image markup that might appear inside the authored markdown body).

Purpose: Solution is one of the 7 required CASE-02 sections and the one place the template calls for its largest, most visually prominent placeholder imagery (UI-SPEC's "largest placeholder size on the page"). Building it now (Wave 3, parallel to Process/Overview/loader) means Plan 02-09's capstone integration has every section component ready to compose without blocking on anything but Wave 3's completion.
Output: `Solution.tsx`, a presentational component composed entirely from Phase 1's `Typography`/`cn()` primitives and Plan 02-04's `ImagePlaceholder`, plus its Vitest test file.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-layer-case-study-template/02-UI-SPEC.md
@.planning/phases/02-content-layer-case-study-template/02-RESEARCH.md
@.planning/phases/02-content-layer-case-study-template/02-PATTERNS.md
@src/components/ui/Typography.tsx
@src/lib/cn.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Solution section component</name>
  <files>src/components/case-study/Solution.tsx, src/components/case-study/Solution.test.tsx</files>
  <read_first>.planning/phases/02-content-layer-case-study-template/02-UI-SPEC.md (Case-Study Page Layout & Composition list item 10, Placeholder Image Blocks "centerpiece" sizing), .planning/phases/02-content-layer-case-study-template/02-04-PLAN.md (ImagePlaceholder's exact prop shape: `{ caption: string; size: 'banner' | 'stage' | 'centerpiece' }`), src/components/ui/Typography.tsx, src/lib/cn.ts, src/components/case-study/ImagePlaceholder.tsx (once Plan 02-04 has executed -- read its exported prop names before importing)</read_first>
  <behavior>
    - Given a non-empty `content` string, renders a `<section>` with an `h2` `Heading` reading "Solution", the markdown-rendered paragraph text, and exactly 2 `ImagePlaceholder` elements with `size="centerpiece"`
    - Given an empty/whitespace-only `content` string, still renders exactly 2 `ImagePlaceholder` elements with `size="centerpiece"` (the 2 image slots are structural per D-11, never conditional on body content)
    - `img` markup embedded inside the markdown `content` itself is remapped to `ImagePlaceholder` (defensive; the authored placeholder content for all 6 files never embeds one, but the remap must exist per D-02)
    - No native image element is ever rendered, regardless of prop values
  </behavior>
  <action>Implement `Solution` accepting props `{ content: string }` (the `sections.solution` string parse.ts produces per Plan 02-03). Render a `<section>` wrapping: an `h2` `Heading` reading "Solution"; a `Markdown` (react-markdown) block rendering `content`, with `components` remapping `p`/`li` to `Body` and `img` to `ImagePlaceholder` (props `caption="Solution detail — pending" size="stage"` for any embedded markdown image -- this is the defensive fallback path, distinct from the 2 structural placeholders below); then, unconditionally (not derived from `content` at all), 2 `ImagePlaceholder` elements with `size="centerpiece"`, laid out in a `grid grid-cols-1 md:grid-cols-2 gap-md` wrapper below the paragraph, captions `"Final solution — pending"` and `"Final solution detail — pending"` respectively (distinct captions for accessibility, both conveying the same "pending" meaning per D-02). No `<Card>` wrapper -- per UI-SPEC's layout rule, only Overview is Card-wrapped. Compose all one-off classNames via `cn()` from `'../../lib/cn'`, matching the established convention in `ToolsUsed.tsx`/`OutcomeImpact.tsx` (Plan 02-04).</action>
  <acceptance_criteria>
    <source>Component file exports a named `Solution` function accepting exactly `{ content: string }`</source>
    <behavior>Rendering with any `content` value (including an empty string) always produces exactly 2 elements matching the `centerpiece` ImagePlaceholder variant</behavior>
    <behavior>No native image element exists anywhere in the rendered output for any prop value</behavior>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/case-study/Solution.test.tsx</automated>
  </verify>
  <done>Solution.tsx and Solution.test.tsx both exist; the component always renders 2 centerpiece-sized ImagePlaceholder blocks regardless of content, renders the markdown paragraph correctly, and the test file passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Parsed markdown-body section string -> react-markdown -> DOM | Content is first-party-authored (D-12), rendered without `dangerouslySetInnerHTML`, same boundary already documented in Plan 02-04's threat model |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-09a | Tampering | react-markdown rendering of the `sections.solution` string | low | accept | Same shared mitigation documented in Plan 02-04 (T-02-06): react-markdown renders to React elements with no raw-HTML plugin added; content is author-controlled today (D-12), not user-submitted |

</threat_model>

<verification>
`npx vitest run src/components/case-study/Solution.test.tsx` -- all tests green.
</verification>

<success_criteria>
Solution is ready for Plan 02-09's capstone composition, always rendering its 2 fixed centerpiece image slots regardless of authored content length.
</success_criteria>

<output>
Create `.planning/phases/02-content-layer-case-study-template/02-05-SUMMARY.md` when done
</output>
