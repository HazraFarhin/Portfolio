---
phase: 02-content-layer-case-study-template
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/case-studies/cad.md
  - src/content/case-studies/mashreq.md
  - src/content/case-studies/astrosure.ai.md
  - src/content/case-studies/adreport.io.md
  - src/content/case-studies/tata-capital-ai-interface.md
  - src/content/case-studies/verzion-cloud-migration.md
autonomous: true
requirements: [CASE-02]
gap_closure: true
gap_ids: [G-02-7]

must_haves:
  truths:
    - "Each of the 6 case-study content files' Challenge section contains a markdown blockquote line (`> ...`), so the case study genuinely exercises Challenge.tsx's already-correct pull-quote styling (italic + left border) instead of the pull-quote path being unreachable with current content."
    - "The blockquote is a real, content-appropriate pull-quote sentence summarizing the core tension of that project's challenge -- not filler or lorem-ipsum text -- consistent with the rest of each file's authored copy."
    - "Every existing bullet point in each Challenge section is preserved; the blockquote is an addition, not a replacement of existing content."
  artifacts:
    - src/content/case-studies/cad.md
    - src/content/case-studies/mashreq.md
    - src/content/case-studies/astrosure.ai.md
    - src/content/case-studies/adreport.io.md
    - src/content/case-studies/tata-capital-ai-interface.md
    - src/content/case-studies/verzion-cloud-migration.md
  key_links:
    - "Challenge.tsx's blockquote component (already wired per 02-09, unmodified by this plan) now has real markdown blockquote content to render on all 6 live case-study pages -- closing UAT gap G-02-7 without touching any component code"
    - "splitBodyIntoSections (src/content/case-studies/parse.ts) splits body content by top-level `## ` headings only -- a `> ` line inside the Challenge section's content range is untouched by that split logic, so this content-only addition cannot break section parsing (verified by the existing parse.test.ts / loader.test.ts suites, which use synthetic fixtures unaffected by these files)"
---

<objective>
Add a genuine markdown pull-quote (`> ...`) to the Challenge section of all 6 case-study content files, so Challenge.tsx's already-correct blockquote styling (italic text + left border, built and tested in Plan 02-09) is actually exercised and visible on every live case-study page.

Purpose: Closes UAT gap G-02-7. The root cause is content-only, not a code bug: Challenge.tsx's blockquote handling is correctly implemented and covered by Challenge.test.tsx, but all 6 authored case-study files write their Challenge section as a plain bullet list with no blockquote line, so the styled pull-quote path was unreachable and rendered as plain text -- exactly what the user observed during UAT. This plan does not modify Challenge.tsx or any other component; it adds one pull-quote sentence per file, each summarizing the core tension already described in that file's existing Challenge bullets, matching the project's existing copy voice and "--" em-dash convention.
Output: All 6 `src/content/case-studies/*.md` files' Challenge sections updated with one blockquote line each, preserving all existing bullet content.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-layer-case-study-template/02-UAT.md
@src/components/case-study/Challenge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Challenge pull-quotes to cad, mashreq, astrosure.ai</name>
  <files>src/content/case-studies/cad.md, src/content/case-studies/mashreq.md, src/content/case-studies/astrosure.ai.md</files>
  <action>
    In each file's `## The Challenge` section, keep the existing bullet list exactly as-is and append a blank line followed by one blockquote line (`> ...`) before the `## Process` heading. The blockquote is a single sentence that summarizes the core tension already described in that file's own Challenge bullets -- written in the same plain, first-person-adjacent copy voice and "--" em-dash convention already used elsewhere in these files (see `astrosure.ai.md` lines 44/64 and `cad.md` line 64 for the established "--" usage).

    Use these exact pull-quotes (already tension-matched to each file's existing bullets):
    - cad.md: "> The redesign had to make the workflow easier to learn without making it any less powerful for the people who already knew it inside out."
    - mashreq.md: "> The interface needed to feel like one coherent product, not five different tools stitched together under one login."
    - astrosure.ai.md: "> Customers weren't struggling with insurance -- they were struggling with how it was being explained to them."

    Do not modify any frontmatter, any other section, or any existing bullet line in these 3 files.
  </action>
  <verify>
    <automated>for f in cad mashreq astrosure.ai; do n=$(awk '/^## The Challenge/{f=1;next} /^## /{f=0} f' "src/content/case-studies/$f.md" | grep -c '^> '); if [ "$n" -lt 1 ]; then echo "FAIL: $f missing Challenge blockquote"; exit 1; fi; done; echo "PASS: cad, mashreq, astrosure.ai all have a Challenge blockquote"</automated>
  </verify>
  <done>cad.md, mashreq.md, and astrosure.ai.md each have exactly one blockquote line inside their Challenge section, all existing bullets preserved, and the verify command above prints PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Add Challenge pull-quotes to adreport.io, tata-capital-ai-interface, verzion-cloud-migration</name>
  <files>src/content/case-studies/adreport.io.md, src/content/case-studies/tata-capital-ai-interface.md, src/content/case-studies/verzion-cloud-migration.md</files>
  <action>
    Same pattern as Task 1: in each file's `## The Challenge` section, keep the existing bullet list exactly as-is and append a blank line followed by one blockquote line (`> ...`) before the `## Process` heading, matching each file's existing copy voice and "--" em-dash convention (see `tata-capital-ai-interface.md` lines 44/64 and `verzion-cloud-migration.md` line 64 for the established usage).

    Use these exact pull-quotes:
    - adreport.io.md: "> The metrics campaign managers needed were always in the data -- they just weren't ever where managers were looking."
    - tata-capital-ai-interface.md: "> Trust in an AI recommendation starts with understanding it, not just seeing it."
    - verzion-cloud-migration.md: "> IT teams didn't need more information about the migration -- they needed to stop hunting for it across five different screens."

    Do not modify any frontmatter, any other section, or any existing bullet line in these 3 files.
  </action>
  <verify>
    <automated>for f in adreport.io tata-capital-ai-interface verzion-cloud-migration; do n=$(awk '/^## The Challenge/{f=1;next} /^## /{f=0} f' "src/content/case-studies/$f.md" | grep -c '^> '); if [ "$n" -lt 1 ]; then echo "FAIL: $f missing Challenge blockquote"; exit 1; fi; done; echo "PASS: adreport.io, tata-capital-ai-interface, verzion-cloud-migration all have a Challenge blockquote"</automated>
  </verify>
  <done>adreport.io.md, tata-capital-ai-interface.md, and verzion-cloud-migration.md each have exactly one blockquote line inside their Challenge section, all existing bullets preserved, and the verify command above prints PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan only adds first-party, author-written Markdown text to already-trusted content files that pass through the same `parseCaseStudyFile` -> `splitBodyIntoSections` -> `react-markdown` pipeline every existing case-study body already uses. No new input source, no user-supplied or externally-sourced content is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-18 | Tampering | Markdown content files (`src/content/case-studies/*.md`) | low | accept | Content is first-party, author-written, version-controlled copy -- identical trust level to the bullet lines already present in these same files. `react-markdown` (already in use since Plan 02-03) escapes rendered output by default, so no new XSS surface is introduced by adding a `> ` blockquote line versus the existing `- ` bullet lines. |

</threat_model>

<verification>
`npm test` -- the full suite (all 407+ existing tests) remains green, confirming the added blockquote lines don't break `parseCaseStudyFile`/`splitBodyIntoSections`'s heading-based section splitting or any existing component/loader test (all of which use synthetic fixtures, not these real files, so a regression here would only surface via a full-suite run). Additionally, run both tasks' automated verify commands together to confirm all 6 files now have a Challenge blockquote:

```
for f in cad mashreq astrosure.ai adreport.io tata-capital-ai-interface verzion-cloud-migration; do n=$(awk '/^## The Challenge/{f=1;next} /^## /{f=0} f' "src/content/case-studies/$f.md" | grep -c '^> '); if [ "$n" -lt 1 ]; then echo "FAIL: $f"; exit 1; fi; done; echo "PASS: all 6 case studies have a Challenge blockquote"
```
</verification>

<success_criteria>
All 6 case-study content files' Challenge sections contain a genuine, content-appropriate blockquote line in addition to their existing bullets. Loading any of the 6 case-study pages (e.g. `/case-study/cad`) now shows the Challenge section's pull-quote rendered with the italic + left-border styling already built in Challenge.tsx (Plan 02-09) -- closing UAT gap G-02-7 with a content-only change, zero component code touched, and the full test suite still green.
</success_criteria>

<output>
Create `.planning/phases/02-content-layer-case-study-template/02-10-SUMMARY.md` when done
</output>
