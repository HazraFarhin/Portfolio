---
phase: 02-content-layer-case-study-template
plan: 6
type: execute
wave: 3
depends_on: ["02-04"]
files_modified:
  - src/components/case-study/Process.tsx
  - src/components/case-study/Process.test.tsx
autonomous: true
requirements: [CASE-02]

must_haves:
  truths:
    - "Process always renders exactly 5 fixed sub-stages in this order: Discovery & Research, Define, Ideate & Wireframe, Design & Prototype, Test & Iterate -- a structural requirement (D-11), not a variable-length list, and unaffected by which prop values are populated"
    - "Exactly 3 of the 5 sub-stages (Discovery & Research, Ideate & Wireframe, Design & Prototype) render an ImagePlaceholder(size='stage') directly below their paragraph; Define and Test & Iterate render no image slot at all -- no image slot is invented for the 2 stages the authoritative template doesn't give one to"
    - "Each sub-stage renders a numbered eyebrow Label (e.g. '01 -- Discovery & Research') as an h3, reusing the Hero eyebrow+statement pairing rather than repeating a 40px Heading five times"
  artifacts:
    - src/components/case-study/Process.tsx
  key_links:
    - "Process is imported by Plan 02-09's CaseStudyPage as the 9th section; its `stages` prop field names (discoveryResearch/define/ideateWireframe/designPrototype/testIterate) must exactly match the keys Plan 02-03's parse.ts splitBodyIntoSections() produces for the process section, so CaseStudyPage can pass `caseStudy.sections.process` through unchanged"
---

<objective>
Build the `Process` case-study section component: the fixed, 5-sub-stage breakdown (Discovery & Research, Define, Ideate & Wireframe, Design & Prototype, Test & Iterate) with 3 of the 5 stages additionally rendering a `stage`-sized `ImagePlaceholder`, per the authoritative template's own structure (D-11).

Purpose: Process is one of the 7 required CASE-02 sections and the only one with an internal fixed-length sub-collection. Building it as a hardcoded 5-item structure (not derived from whatever keys happen to be present on its props) guarantees the "always exactly 5, never variable-length" UI consideration holds even if upstream parsing ever produces a partial or malformed sections object.
Output: `Process.tsx`, a presentational component exporting its own `ProcessStages` prop type (5 required string fields) and composed entirely from Phase 1's `Typography`/`cn()` primitives plus Plan 02-04's `ImagePlaceholder`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-layer-case-study-template/02-UI-SPEC.md
@.planning/phases/02-content-layer-case-study-template/02-RESEARCH.md
@.planning/phases/02-content-layer-case-study-template/02-PATTERNS.md
@src/components/ui/Typography.tsx
@src/lib/cn.ts
@src/routes/home.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Process section component (5 fixed sub-stages)</name>
  <files>src/components/case-study/Process.tsx, src/components/case-study/Process.test.tsx</files>
  <read_first>.planning/phases/02-content-layer-case-study-template/02-UI-SPEC.md (Process Component section), .planning/phases/02-content-layer-case-study-template/02-03-PLAN.md (Task 2's behavior: the 5 stage key names produced by splitBodyIntoSections' process sub-split -- discoveryResearch, define, ideateWireframe, designPrototype, testIterate, in that fixed order), src/routes/home.tsx (lines 19-21, the existing Label-as-eyebrow + Body-statement pairing pattern to reuse), src/components/case-study/ImagePlaceholder.tsx (once Plan 02-04 has executed)</read_first>
  <behavior>
    - Given all 5 stage strings populated, renders exactly 5 numbered eyebrow labels in this fixed order and wording: "01 — Discovery & Research", "02 — Define", "03 — Ideate & Wireframe", "04 — Design & Prototype", "05 — Test & Iterate"
    - Renders exactly 3 ImagePlaceholder elements with `size="stage"`, positioned immediately after the Discovery & Research, Ideate & Wireframe, and Design & Prototype paragraphs only
    - Renders 0 ImagePlaceholder elements after the Define and Test & Iterate paragraphs
    - Given one or more stage strings are empty (defensive case -- e.g. a source file omitted a sub-heading), still renders all 5 stage slots with their eyebrow labels; only the paragraph text is empty for that stage, the stage itself is never dropped
    - No native image element is ever rendered
  </behavior>
  <action>Export a `ProcessStages` interface with 5 required `string` fields: `discoveryResearch`, `define`, `ideateWireframe`, `designPrototype`, `testIterate` (field names and order must exactly match Plan 02-03's parse.ts output so Plan 02-09 can pass `caseStudy.sections.process` straight through). Implement `Process` accepting props `{ stages: ProcessStages }`. Inside the component, define a fixed, hardcoded array of exactly 5 stage descriptors -- `[{ key: 'discoveryResearch', label: 'Discovery & Research', hasImage: true }, { key: 'define', label: 'Define', hasImage: false }, { key: 'ideateWireframe', label: 'Ideate & Wireframe', hasImage: true }, { key: 'designPrototype', label: 'Design & Prototype', hasImage: true }, { key: 'testIterate', label: 'Test & Iterate', hasImage: false }]` -- and `.map()` over this fixed array (never `Object.keys(stages)` or `Object.entries(stages)`, which would make the rendered count/order depend on whatever keys happen to exist on the prop object) to render each stage: a `Label` (`as="h3"`) reading `${String(index + 1).padStart(2, '0')} — ${label}`, then a `Body` paragraph reading `stages[key]`, then -- only when `hasImage` is true -- an `ImagePlaceholder` (`size="stage"`, `caption={`${label} artifact — pending`}`) directly below. Stack all 5 stages vertically with `gap-xl` (32px) between them via `cn()`. Wrap the whole thing in a `<section>` with an `h2` `Heading` reading "Process" above the 5-stage list. No `<Card>` wrapper.</action>
  <acceptance_criteria>
    <source>Component file exports a named `Process` function accepting `{ stages: ProcessStages }`, and exports the `ProcessStages` interface itself</source>
    <behavior>Rendering with any `stages` prop value (including one with empty-string fields) always produces exactly 5 numbered eyebrow labels in the fixed order and exactly 3 `stage`-sized ImagePlaceholder elements at the fixed positions (after Discovery & Research, Ideate & Wireframe, Design & Prototype)</behavior>
    <behavior>No native image element exists anywhere in the rendered output for any prop value</behavior>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/case-study/Process.test.tsx</automated>
  </verify>
  <done>Process.tsx and Process.test.tsx both exist; the component always renders 5 fixed sub-stages and exactly 3 stage-image slots at their fixed positions regardless of the stages prop's actual string content; the test file passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Parsed markdown-body sub-stage strings -> Body paragraph -> DOM | Content is first-party-authored (D-12); this component renders plain text via `Body`, not `react-markdown`, since each sub-stage is 1-2 sentences of prose per the template's own authoring guidance, not structured markdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-10 | Tampering (structural integrity) | A malformed/partial `stages` prop causing a variable-length or reordered render | low | mitigate | Rendering iterates a hardcoded 5-item descriptor array, never `Object.keys(stages)`/`Object.entries(stages)` -- the component's own structure guarantees exactly 5 stages in fixed order regardless of what the `stages` prop actually contains |

</threat_model>

<verification>
`npx vitest run src/components/case-study/Process.test.tsx` -- all tests green.
</verification>

<success_criteria>
Process is ready for Plan 02-09's capstone composition, always rendering its 5 fixed sub-stages and 3 fixed image slots regardless of upstream content variance.
</success_criteria>

<output>
Create `.planning/phases/02-content-layer-case-study-template/02-06-SUMMARY.md` when done
</output>
