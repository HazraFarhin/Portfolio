---
phase: 02-content-layer-case-study-template
plan: 4
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/case-study/ImagePlaceholder.tsx
  - src/components/case-study/ImagePlaceholder.test.tsx
  - src/components/case-study/ToolsUsed.tsx
  - src/components/case-study/ToolsUsed.test.tsx
  - src/components/case-study/OutcomeImpact.tsx
  - src/components/case-study/OutcomeImpact.test.tsx
autonomous: true
requirements: [CASE-02]

must_haves:
  truths:
    - "Every image slot in the template renders ImagePlaceholder, never a real <img> pointed at a nonexistent path (D-02)"
    - "ToolsUsed renders 'Tools list pending.' when its section string is empty/blank, otherwise renders the authored tool list"
    - "OutcomeImpact renders the 'Outcome details pending...' italic fallback when its section string is empty/blank, otherwise renders the authored outcome list"
    - "Tools Used chip/list content wraps (flex-wrap) rather than scrolling or clipping at high volume"
  artifacts:
    - src/components/case-study/ImagePlaceholder.tsx
    - src/components/case-study/ToolsUsed.tsx
    - src/components/case-study/OutcomeImpact.tsx
  key_links:
    - "ImagePlaceholder is imported by Plan 02-05 (Solution), Plan 02-06 (Process), and Plan 02-09 (cover banner) -- it must exist before those plans compose it"
---

<objective>
Build the shared `ImagePlaceholder` component (D-02's styled placeholder block, used everywhere the template calls for an image) plus the two simplest markdown-body section components, `ToolsUsed` and `OutcomeImpact`, which render `react-markdown` output with a defensive empty-state fallback.

Purpose: `ImagePlaceholder` is a foundational dependency for 3 later plans (Solution's centerpiece images, Process's stage images, and the page-level cover banner) -- building it first unblocks parallel work. `ToolsUsed`/`OutcomeImpact` establish the react-markdown-with-component-remapping pattern every other body-section component (Plan 02-05) will repeat.
Output: 3 presentational components composed entirely from Phase 1's `Typography`/`cn()` primitives and the newly-installed `lucide-react`/`react-markdown` packages.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-layer-case-study-template/02-UI-SPEC.md
@.planning/phases/02-content-layer-case-study-template/02-RESEARCH.md
@.planning/phases/02-content-layer-case-study-template/02-PATTERNS.md
@src/components/ui/Typography.tsx
@src/lib/cn.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ImagePlaceholder component</name>
  <files>src/components/case-study/ImagePlaceholder.tsx, src/components/case-study/ImagePlaceholder.test.tsx</files>
  <read_first>.planning/phases/02-content-layer-case-study-template/02-UI-SPEC.md (Placeholder Image Blocks section), src/components/ui/Typography.tsx, src/lib/cn.ts</read_first>
  <behavior>
    - Renders a caption's text (via Label) beneath a centered lucide-react ImageOff icon
    - `size="banner"` applies a 21:9 aspect ratio (16:9 on mobile) with `max-height: clamp(140px, 20vh, 220px)`
    - `size="stage"` applies a 16:9 aspect ratio with `max-height: 200px`
    - `size="centerpiece"` applies a 4:3 aspect ratio with `max-height: 360px`
    - Never renders a native `<img>` element regardless of props
  </behavior>
  <action>Per this task's `tdd="true"` marker, write `ImagePlaceholder.test.tsx` first (using `@testing-library/react`'s `render()`), asserting: each of the 3 `size` values produces a distinct className string containing that size's aspect-ratio/max-height classes, and `container.querySelector('img')` returns `null` for every prop combination. Then implement `ImagePlaceholder.tsx` to make the tests pass: accept props `{ caption: string; size: 'banner' | 'stage' | 'centerpiece' }`. Root element is a `div` with classes composed via `cn()`: base classes `rounded-2xl border border-line bg-gradient-to-br from-secondary to-dominant flex flex-col items-center justify-center gap-sm`, plus a per-`size` class string (banner: `aspect-[21/9] max-h-[clamp(140px,20vh,220px)]`, stage: `aspect-video max-h-[200px]`, centerpiece: `aspect-[4/3] max-h-[360px]`). Render lucide-react's `ImageOff` icon with `className="text-muted-foreground"`, then a `Label` containing the `caption` prop text below it. No `<img>` tag anywhere in this component, per D-02.</action>
  <acceptance_criteria>
    <source>Component file exports a named `ImagePlaceholder` function accepting exactly `{ caption, size }` props</source>
    <behavior>Rendering with each of the 3 `size` values produces a distinct className string containing that size's aspect-ratio and max-height classes</behavior>
    <behavior>No `<img>` element exists anywhere in the rendered output for any prop combination</behavior>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/case-study/ImagePlaceholder.test.tsx && grep -c '<img' src/components/case-study/ImagePlaceholder.tsx</automated>
  </verify>
  <done>ImagePlaceholder.tsx and ImagePlaceholder.test.tsx both exist; the component renders no `<img>` tag; the 3 size variants each apply visually distinct sizing classes per UI-SPEC; the test file passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ToolsUsed section component</name>
  <files>src/components/case-study/ToolsUsed.tsx, src/components/case-study/ToolsUsed.test.tsx</files>
  <read_first>.planning/phases/02-content-layer-case-study-template/02-UI-SPEC.md (Copywriting Contract, UI Considerations), .planning/phases/02-content-layer-case-study-template/02-RESEARCH.md (Pattern 4)</read_first>
  <behavior>
    - Given a non-empty markdown section string (e.g. a bullet list of tools), renders it via react-markdown with `img` remapped to ImagePlaceholder
    - Given an empty or whitespace-only section string, renders the muted `Label` text "Tools list pending." instead of an empty list
    - Section heading "Tools Used" renders as an `h2` via the `Heading` primitive
    - The rendered tool list uses `flex flex-wrap gap-sm` so it wraps rather than scrolling/clipping at high item counts
  </behavior>
  <action>Per this task's `tdd="true"` marker, write `ToolsUsed.test.tsx` first covering both acceptance-criteria behaviors below. Then implement `ToolsUsed` accepting props `{ content: string }` (the `sections.toolsUsed` string from parse.ts's output). Render a `<section>` wrapping a `Heading` (`as="h2"`) reading "Tools Used", followed by: if `content.trim()` is empty, a `Label` reading exactly "Tools list pending."; otherwise, a `react-markdown` (`Markdown` import) block wrapped in a `div` with `flex flex-wrap gap-sm`, with `components` remapping `li` to a chip-styled span (small rounded pill using `cn()` with `Body`-derived text classes) and `img` unconditionally to `ImagePlaceholder` (props `caption="Tool icon — pending" size="stage"`) per D-02. No `<Card>` wrapper -- per UI-SPEC's layout rule, only Overview (Plan 02-07) is Card-wrapped.</action>
  <acceptance_criteria>
    <behavior>Rendering with `content=""` shows the text "Tools list pending." and no react-markdown output</behavior>
    <behavior>Rendering with a non-empty `content` string containing an `- Figma` bullet shows "Figma" in the rendered output</behavior>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/case-study/ToolsUsed.test.tsx</automated>
  </verify>
  <done>ToolsUsed.tsx and ToolsUsed.test.tsx both exist; the component renders the empty-state fallback for blank content and the real markdown-rendered tool list otherwise, matching UI-SPEC's Copywriting Contract exactly; the test file passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OutcomeImpact section component</name>
  <files>src/components/case-study/OutcomeImpact.tsx, src/components/case-study/OutcomeImpact.test.tsx</files>
  <read_first>.planning/phases/02-content-layer-case-study-template/02-UI-SPEC.md (Copywriting Contract)</read_first>
  <behavior>
    - Given a non-empty section string, renders it via react-markdown with `img` remapped to ImagePlaceholder
    - Given an empty/whitespace-only section string, renders the italic `Body` fallback: "Outcome details pending — qualitative summary of impact and stakeholder reception to follow."
    - Section heading "Outcome & Impact" renders as an `h2` via `Heading`
  </behavior>
  <action>Per this task's `tdd="true"` marker, write `OutcomeImpact.test.tsx` first covering both acceptance-criteria behaviors below. Then implement `OutcomeImpact` accepting props `{ content: string }` (the `sections.outcomeImpact` string). Structure mirrors ToolsUsed: a `<section>` with a `Heading` (`as="h2"`) reading "Outcome & Impact", then either the italic `Body` fallback text (exact string: "Outcome details pending — qualitative summary of impact and stakeholder reception to follow.", with `className="italic"`) when `content.trim()` is empty, or a `Markdown` block remapping `img` to `ImagePlaceholder` and `p`/`li` to `Body` otherwise. No `<Card>` wrapper.</action>
  <acceptance_criteria>
    <behavior>Rendering with `content=""` shows the exact fallback sentence with an italic className</behavior>
    <behavior>Rendering with non-empty content shows that content's text, not the fallback</behavior>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/case-study/OutcomeImpact.test.tsx</automated>
  </verify>
  <done>OutcomeImpact.tsx and OutcomeImpact.test.tsx both exist; the component renders the exact fallback copy for empty content and markdown-rendered content otherwise; the test file passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Parsed markdown-body section string -> react-markdown -> DOM | Content is first-party-authored (D-12), rendered without `dangerouslySetInnerHTML` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-06 | Tampering | react-markdown rendering of ToolsUsed/OutcomeImpact section strings | low | accept | react-markdown's default behavior renders to React elements, never executing embedded raw HTML (no `rehype-raw` plugin is added); content is author-controlled today (D-12), not user-submitted -- documented here as the shared mitigation every later markdown-rendering component (Plan 02-05, 02-06) inherits by using the same `Markdown` import with no raw-HTML plugin |

</threat_model>

<verification>
`npx vitest run src/components/case-study/` — all new component tests green; `grep -c '<img' src/components/case-study/ImagePlaceholder.tsx` returns 0.
</verification>

<success_criteria>
ImagePlaceholder is ready for reuse by Plans 02-05/02-06/02-09; ToolsUsed and OutcomeImpact correctly handle both populated and empty section content per UI-SPEC's Copywriting Contract.
</success_criteria>

<output>
Create `.planning/phases/02-content-layer-case-study-template/02-04-SUMMARY.md` when done
</output>
