---
phase: 02-content-layer-case-study-template
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements: [CASE-04]

must_haves:
  truths:
    - "zod, js-yaml, and react-markdown are installed in package.json dependencies and resolvable via node_modules"
    - "@types/js-yaml is installed in devDependencies"
    - "js-yaml's [SUS]-flagged legitimacy verdict has been human-reconfirmed before install, per the Package Legitimacy Gate"
  artifacts:
    - package.json (zod, js-yaml, react-markdown, @types/js-yaml added)
  key_links:
    - "Plans 02-03 through 02-09 all import zod/js-yaml/react-markdown and cannot compile until this plan's install completes"
---

<objective>
Install the three new runtime dependencies this phase requires (zod, js-yaml, react-markdown) plus their dev-only type package, after a human re-confirms the one package (js-yaml) that 02-RESEARCH.md's Package Legitimacy Audit flagged with a raw [SUS] verdict (a documented false positive, but the gate requires human confirmation regardless of the research doc's own reasoning).

Purpose: Every subsequent Phase 2 plan (schema validation, frontmatter/body parsing, markdown-body rendering) imports one of these three packages. Front-loading the install in a single Wave 1 plan means no later plan touches package.json/package-lock.json, avoiding file-conflict/wave-ordering problems across the 8 remaining plans.
Output: Updated package.json/package-lock.json with zod, js-yaml, react-markdown (dependencies) and @types/js-yaml (devDependency) installed and importable.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-layer-case-study-template/02-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Re-confirm js-yaml package legitimacy before install</name>
  <what-built>02-RESEARCH.md's Package Legitimacy Audit table flags `js-yaml` with a raw verdict of [SUS] ("too-new"), triggered because its most recent *patch* release published 2026-07-23 (the day before research ran). The audit's own note explains this is a date-heuristic false positive: `npm view js-yaml time.created` shows the package's actual first release was 2011-11-02 (15 years old), it has ~272M weekly downloads, and it is maintained by the long-established `nodeca` GitHub org. No code has been installed yet — this checkpoint exists because the Package Legitimacy Gate treats every [SUS]-flagged package as requiring human confirmation before its install task runs, regardless of the research doc's own reasoning.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/js-yaml and confirm: publisher/maintainer org is `nodeca`, weekly download count is in the hundreds of millions, and the "Repository" link points to github.com/nodeca/js-yaml.
    2. Optionally run `npm view js-yaml time.created` locally and confirm the printed date is 2011-11-02 (not 2026).
    3. Confirm you are comfortable installing `js-yaml` as a runtime dependency for parsing case-study frontmatter YAML (used only for parsing content this project's own author writes into `.md` files — not for parsing any third-party or user-submitted input).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install in Task 2, or describe any concern that should block it.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install zod, js-yaml, react-markdown and @types/js-yaml</name>
  <files>package.json, package-lock.json</files>
  <read_first>package.json, .planning/phases/02-content-layer-case-study-template/02-RESEARCH.md</read_first>
  <action>Run `npm install zod js-yaml react-markdown` to add all three as runtime dependencies, then run `npm install -D @types/js-yaml` to add its type declarations as a devDependency (react-markdown and zod ship their own types; js-yaml does not). Do not install `remark-gfm`, `gray-matter`, or `front-matter` — `remark-gfm` is only needed if body content uses GitHub-Flavored-Markdown syntax (tables/strikethrough/task-lists), which this phase's placeholder content does not use per 02-RESEARCH.md's Standard Stack table; `gray-matter`/`front-matter` are explicitly rejected per 02-RESEARCH.md's Common Pitfalls (Pitfall 1) because they call `Buffer.isBuffer`/`toBuffer` internally and throw `ReferenceError: Buffer is not defined` when bundled by Vite for the browser. Do not pin exact versions manually beyond what npm resolves — 02-RESEARCH.md's verified versions (zod 4.4.3, js-yaml 5.2.2, react-markdown 10.1.0) are current-latest as of the research date, so an unconstrained `npm install <pkg>` will resolve to them or a compatible newer patch.</action>
  <acceptance_criteria>
    <source>package.json "dependencies" includes "zod", "js-yaml", and "react-markdown"; "devDependencies" includes "@types/js-yaml"</source>
    <cli>`npm ls zod js-yaml react-markdown @types/js-yaml` exits 0 and prints all four as installed with no "invalid"/"missing" markers</cli>
  </acceptance_criteria>
  <verify>
    <automated>npm ls zod js-yaml react-markdown @types/js-yaml</automated>
  </verify>
  <done>package.json/package-lock.json updated; `npm ls` for all four packages reports them installed; no other dependency in package.json is removed or downgraded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry -> local node_modules | Supply-chain trust boundary crossed at install time; this plan's sole purpose is to cross it safely for 3 new packages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-SC | Tampering | npm install (zod, js-yaml, react-markdown, @types/js-yaml) | high | mitigate | 02-RESEARCH.md's Package Legitimacy Audit ran for all four packages before this plan existed; the one [SUS]-flagged package (js-yaml) gets a blocking `checkpoint:human-verify` (Task 1) before install, never auto-approved regardless of `workflow.auto_advance`. zod/react-markdown/@types/js-yaml carry an "OK" verdict in the audit and do not require a checkpoint. |

</threat_model>

<verification>
`npm ls zod js-yaml react-markdown @types/js-yaml` reports all four installed; `npm run build` still completes (nothing yet imports the new packages, so this only confirms the install didn't corrupt the existing build).
</verification>

<success_criteria>
Human has explicitly confirmed js-yaml's legitimacy; all three runtime packages plus the one type package are present in package.json and node_modules, ready for Plan 02-03 (schema/parser) and Plan 02-04 (markdown-rendering components) to import.
</success_criteria>

<output>
Create `.planning/phases/02-content-layer-case-study-template/02-01-SUMMARY.md` when done
</output>
