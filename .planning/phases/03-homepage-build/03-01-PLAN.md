---
phase: 03-homepage-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/nav.ts
  - src/components/home/Nav.tsx
  - src/components/home/Nav.test.tsx
  - src/content/proof-strip.ts
  - src/components/home/ProofStrip.tsx
  - src/components/home/ProofStrip.test.tsx
  - src/content/field-archive.ts
  - src/components/home/FieldArchive.tsx
  - src/components/home/FieldArchive.test.tsx
autonomous: true
requirements: [HOME-02]

must_haves:
  truths:
    - "navContent exposes exactly 4 nav links (Work, Method, Skills, Contact) pointing at #selected-work, #how-i-work, #skills-tools, #contact-footer per D-06 clarified"
    - "Nav is not mounted in App.tsx and does not call useScrollReveal (persistent chrome, homepage-only per D-06 clarified, Open Question 2 resolved to homepage-only)"
    - "Proof strip renders all 4 concrete stats (~4 years, 8+ industries, 3 regions, 30% reduction) sourced verbatim from Homepage Copy V2.md §05 (HOME-02)"
    - "Proof Strip's 4 stat numbers are the only new accent-colored (text-accent) elements introduced this phase, per UI-SPEC's 10%-accent-budget discipline"
    - "Field Archive is a native overflow-x-auto horizontally-scrollable region (role=region, aria-label, tabIndex=0) with zero pin:true/containerAnimation ScrollTrigger config anywhere in the file (D-04, REQUIREMENTS.md's explicit anti-scroll-jacking ban)"
    - "Field Archive gallery scroll never clips/blocks native mouse/touch/keyboard scroll (overflow, UI-SPEC UI Considerations #7, covered)"
    - statement: "Nav bar fits without a hamburger menu at 320-375px mobile viewports with the actual rendered link labels"
      verification: backstop
    - statement: "Proof Strip's 2x2 mobile stat grid captions don't overlap/truncate at the smallest supported viewport"
      verification: backstop
  artifacts:
    - src/content/nav.ts
    - src/components/home/Nav.tsx
    - src/content/proof-strip.ts
    - src/components/home/ProofStrip.tsx
    - src/content/field-archive.ts
    - src/components/home/FieldArchive.tsx
  key_links:
    - "Nav.tsx href targets -> section ids owned by Plans 03-02/03-03/03-06 (#selected-work, #how-i-work, #skills-tools, #contact-footer) -- these ids only resolve after Plan 03-07 mounts everything into home.tsx"
    - "FieldArchive.tsx -> ImagePlaceholder (existing, Phase 2, 'stage' size, unmodified)"
---

<objective>
Build the persistent Nav bar (D-06), the Proof Strip stats section (HOME-02), and the Field Archive gallery (D-04) as three independent, self-contained homepage sections -- each a content module + rendering component + test, following the exact `src/content/hero.ts` / `src/routes/home.tsx` pattern established in Phase 1.

Purpose: These three sections have no dependency on any other new Phase 3 file (Nav's link targets are plain `#id` strings that don't need the target sections to exist yet to be unit-tested; Field Archive only needs the existing, unmodified `ImagePlaceholder`). Building them first, in parallel with the other Wave 1 plans, keeps Phase 3's dependency graph flat.
Output: `src/content/nav.ts`, `src/components/home/Nav.tsx` (+test), `src/content/proof-strip.ts`, `src/components/home/ProofStrip.tsx` (+test), `src/content/field-archive.ts`, `src/components/home/FieldArchive.tsx` (+test).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-build/03-CONTEXT.md
@.planning/phases/03-homepage-build/03-UI-SPEC.md
@Portfolio-Documentation/Homepage Copy V2.md
@src/content/hero.ts
@src/routes/home.tsx
@src/components/ui/Button.tsx
@src/components/ui/Card.tsx
@src/components/ui/Typography.tsx
@src/lib/cn.ts
@src/motion/useScrollReveal.ts
@src/components/case-study/ImagePlaceholder.tsx
@src/components/case-study/Overview.tsx
@src/components/case-study/Process.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Nav bar -- content module + component + test (D-06)</name>
  <files>src/content/nav.ts, src/components/home/Nav.tsx, src/components/home/Nav.test.tsx</files>
  <read_first>src/content/hero.ts, src/routes/home.tsx, src/components/ui/Typography.tsx, src/components/ui/Button.tsx, src/lib/cn.ts, .planning/phases/03-homepage-build/03-UI-SPEC.md (Nav Bar section)</read_first>
  <behavior>
    - navContent.links has exactly 4 entries in order: Work(#selected-work), Method(#how-i-work), Skills(#skills-tools), Contact(#contact-footer)
    - Nav.tsx renders one `<nav>` with the wordmark link (href="#hero") plus 4 nav links, each an `<a>` matching navContent 1:1
    - Nav.tsx never calls useScrollReveal and is never imported by App.tsx
  </behavior>
  <action>
    Create `src/content/nav.ts` exporting `interface NavLink { label: string; href: string }`, `interface NavContent { wordmark: string; wordmarkHref: string; links: NavLink[] }`, and `export const navContent: NavContent = { wordmark: 'Hazra Farhin', wordmarkHref: '#hero', links: [ { label: 'Work', href: '#selected-work' }, { label: 'Method', href: '#how-i-work' }, { label: 'Skills', href: '#skills-tools' }, { label: 'Contact', href: '#contact-footer' } ] }`. Mirror `src/content/hero.ts`'s exact `export interface X` + `export const xContent: X = {...}` shape, with a JSDoc citing "D-06, Homepage Copy V2.md §01 Navigation (4-link cadence, 'Engage' swapped for 'Skills' since Engage is cut per D-05, per UI-SPEC Copywriting Contract)". Skip the source doc's optional wordmark tagline -- UI-SPEC's Typography table calls the wordmark a "quiet identity mark," not a tagline carrier.

    Create `src/components/home/Nav.tsx`: a function component `Nav()` (no props) rendering `<nav className="fixed inset-x-0 top-0 z-50 h-16 bg-dominant/80 backdrop-blur-lg border-b border-line flex items-center justify-between px-md md:px-lg">` containing `<Label as="a" href={navContent.wordmarkHref}>{navContent.wordmark}</Label>` on the left and `<div className="flex gap-md">` on the right wrapping one `<Label as="a" href={link.href} key={link.href}>{link.label}</Label>` per `navContent.links` entry (`.map()` is fine here -- Nav's own link list is inherently variable-shaped chrome, not a locked-count content section like ProofStrip/HowIWork). Use plain `#id` hrefs throughout -- never react-router `Link`, never `to="/#..."` (D-06 clarified: homepage-only scope, Open Question 2 resolved). Do NOT call `useScrollReveal` on Nav (persistent chrome, not a reveal-on-scroll section). Do NOT import `Nav` into `src/App.tsx` -- it is wired into `home.tsx` only, by Plan 03-07.
  </action>
  <verify>
    <automated>npm run test -- src/components/home/Nav.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `navContent.links` has exactly 4 entries with labels `Work`, `Method`, `Skills`, `Contact` in that order, and hrefs `#selected-work`, `#how-i-work`, `#skills-tools`, `#contact-footer` respectively
    - `Nav.tsx` renders exactly one `<nav>` element containing 5 `<a>` elements total (1 wordmark + 4 nav links), each `href` exactly matching `navContent`'s corresponding value
    - `Nav.tsx` contains zero occurrences of `useScrollReveal` and zero occurrences of `to="/#` (grep -c both == 0)
    - `src/App.tsx` remains byte-identical to its current 5-line `Outlet`-only implementation (unmodified by this task)
  </acceptance_criteria>
  <done>Nav renders a fixed wordmark + 4-link bar wired to the 4 in-page section ids, scoped to home.tsx only, verified by Nav.test.tsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Proof Strip -- content module + component + test (HOME-02)</name>
  <files>src/content/proof-strip.ts, src/components/home/ProofStrip.tsx, src/components/home/ProofStrip.test.tsx</files>
  <read_first>src/content/hero.ts, src/components/case-study/Overview.tsx (fixed-row discipline), src/components/case-study/Process.tsx (numbered literal-sequence discipline), .planning/phases/03-homepage-build/03-UI-SPEC.md (Typography + Color sections)</read_first>
  <behavior>
    - proofStripContent.stats has exactly 4 entries: ~4/8+/3/30% in Homepage Copy V2.md §05's order
    - ProofStrip.tsx renders all 4 values and all 4 captions, with the 4 value elements carrying the text-accent class
    - ProofStrip.tsx never derives the 4 stats via `.stats.map(` -- 4 literal, indexed JSX blocks instead
  </behavior>
  <action>
    Create `src/content/proof-strip.ts` exporting `interface ProofStat { value: string; caption: string }`, `interface ProofStripContent { label: string; heading: string; stats: ProofStat[]; supportingLine: string }`, and `export const proofStripContent: ProofStripContent = { label: 'In Practice', heading: 'Design, In Numbers', stats: [ { value: '~4', caption: 'years in product design' }, { value: '8+', caption: 'industries shipped across -- FinTech, e-commerce, telecom, martech, beauty, sports tech' }, { value: '3', caption: 'regions designed for -- India, UAE, Saudi Arabia, Canada, USA' }, { value: '30%', caption: 'avg. reduction in reporting/task time on shipped projects' } ], supportingLine: "Working across industries and directly with founders, product leads, and engineering teams has shown me that user needs and business goals rarely conflict. They just need someone translating between design, product, and engineering." }` -- sourced verbatim from `Portfolio-Documentation/Homepage Copy V2.md` §05 Telemetry.

    Create `src/components/home/ProofStrip.tsx`: `<section id="proof-strip" ref={ref}>` with `useScrollReveal(ref)`. Render `<Label>{proofStripContent.label}</Label>`, `<Heading as="h2">{proofStripContent.heading}</Heading>`, then a `grid grid-cols-2 md:grid-cols-4 gap-lg` containing exactly 4 literal, hardcoded `<div>` blocks (index 0..3 into `proofStripContent.stats`, each preceded by a `{/* Stat N: <label> */}` numbered comment mirroring `Process.tsx`'s discipline -- NEVER `proofStripContent.stats.map(...)`), each block: `<Heading as="p" className="text-accent">{proofStripContent.stats[N].value}</Heading>` then `<Label as="p">{proofStripContent.stats[N].caption}</Label>`. Below the grid, render `<Body>{proofStripContent.supportingLine}</Body>`.
  </action>
  <verify>
    <automated>npm run test -- src/components/home/ProofStrip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `proofStripContent.stats` is exactly `['~4','8+','3','30%']` (by `value`) in that order, matching Homepage Copy V2.md §05
    - ProofStrip.tsx renders `<section id="proof-strip">`, and all 4 stat values plus all 4 captions appear in the rendered DOM
    - All 4 stat-value elements carry the `text-accent` class (grep -c "text-accent" src/components/home/ProofStrip.tsx >= 4)
    - `grep -c '\.stats\.map(' src/components/home/ProofStrip.tsx` returns 0 (confirms literal-sequence discipline, not iteration)
  </acceptance_criteria>
  <done>Proof Strip renders all 4 real stats from Homepage Copy V2.md §05 with accent-colored figures, verified by ProofStrip.test.tsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Field Archive -- content module + component + test (D-04)</name>
  <files>src/content/field-archive.ts, src/components/home/FieldArchive.tsx, src/components/home/FieldArchive.test.tsx</files>
  <read_first>src/components/case-study/ImagePlaceholder.tsx, src/motion/useScrollReveal.ts, .planning/phases/03-homepage-build/03-RESEARCH.md (Pattern 4, Pitfall 2), .planning/phases/03-homepage-build/03-UI-SPEC.md (Field Archive section)</read_first>
  <behavior>
    - fieldArchiveContent.captions has exactly 6 entries verbatim from Homepage Copy V2.md §06
    - FieldArchive.tsx's scroll container has role="region", aria-label="Field Archive gallery", tabIndex={0}, overflow-x-auto
    - FieldArchive.tsx contains zero pin:true / containerAnimation ScrollTrigger config
  </behavior>
  <action>
    Create `src/content/field-archive.ts` exporting `interface FieldArchiveContent { label: string; copy: string; captions: string[] }` and `export const fieldArchiveContent: FieldArchiveContent = { label: 'Field Archive', copy: 'Fragments from research boards, wireframes, and shipped interfaces, before the system takes final shape.', captions: [ 'Early flow mapping, digital banking self-service', 'Wireframe iteration, cloud migration dashboard', 'Component states, RTL-optimised commerce UI', 'Usability test notes, AI-native agent tooling', 'Stakeholder review notes, requirement changes tracked', 'Handoff spec, production-ready HTML/CSS' ] }` -- sourced verbatim from `Portfolio-Documentation/Homepage Copy V2.md` §06.

    Create `src/components/home/FieldArchive.tsx` following 03-RESEARCH.md's Pattern 4 sketch exactly: `<section id="field-archive" ref={sectionRef}>` with `useScrollReveal(sectionRef)` as the section's ONLY GSAP involvement (decorative fade/rise entrance, never scroll-position-linked horizontal motion). Render `<Label>{fieldArchiveContent.label}</Label>` and `<Body>{fieldArchiveContent.copy}</Body>` above an inner `<div className="flex gap-md overflow-x-auto snap-x snap-proximity" role="region" aria-label="Field Archive gallery" tabIndex={0}>` that `.map()`s over `fieldArchiveContent.captions` (variable-length gallery content, `.map()` is correct here per RESEARCH.md's own authoritative sketch), each item `<div key={caption} className="snap-start shrink-0"><ImagePlaceholder caption={caption} size="stage" /></div>`. CRITICAL constraint: this file must never configure `ScrollTrigger` with `pin: true` or `containerAnimation` -- the section's only motion call is the single `useScrollReveal(sectionRef)` line (REQUIREMENTS.md's explicit anti-scroll-jacking ban, RESEARCH.md Pitfall 2).
  </action>
  <verify>
    <automated>npm run test -- src/components/home/FieldArchive.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `fieldArchiveContent.captions` has exactly 6 entries matching Homepage Copy V2.md §06 verbatim, in that order
    - FieldArchive.tsx's scroll container element has `role="region"`, `aria-label="Field Archive gallery"`, `tabIndex={0}`, and an `overflow-x-auto` class
    - `grep -c -E 'pin:|containerAnimation' src/components/home/FieldArchive.tsx` returns 0
    - Renders exactly 6 `ImagePlaceholder` instances, one per caption, each with `size="stage"`
  </acceptance_criteria>
  <done>Field Archive renders a native, keyboard-focusable horizontally-scrollable gallery of the 6 sourced captions with zero scroll-jacking config, verified by FieldArchive.test.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Nav, Proof Strip, and Field Archive render only developer-authored static TypeScript content-module strings -- no user input, no external navigation, no dynamic HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Denial of Service (accessibility) | FieldArchive.tsx scroll container | medium | mitigate | Native `overflow-x-auto` + `role="region"`/`aria-label`/`tabIndex={0}` keeps keyboard/screen-reader/native scroll intact; zero `pin`/`containerAnimation` config, per REQUIREMENTS.md's explicit anti-scroll-jacking ban -- enforced by this task's own grep acceptance criterion |
| T-03-03 | Information Disclosure / Tampering | Nav.tsx, ProofStrip.tsx, FieldArchive.tsx | low | accept | All rendered strings are typed TS literals in `src/content/*.ts`, never `dangerouslySetInnerHTML` or markdown-rendered user content -- no injection surface exists in these 3 sections |
</threat_model>

<verification>
- `npm run test -- src/components/home/Nav.test.tsx src/components/home/ProofStrip.test.tsx src/components/home/FieldArchive.test.tsx` all pass
- `npm run test` (full suite) still passes with no regressions to any Phase 1/2 test file
</verification>

<success_criteria>
Nav, Proof Strip, and Field Archive each exist as an independent content module + component + passing test, ready to be mounted into `home.tsx` by Plan 03-07. No file this plan touches is later modified by any other Wave 1 plan.
</success_criteria>

<output>
Create `.planning/phases/03-homepage-build/03-01-SUMMARY.md` when done
</output>
