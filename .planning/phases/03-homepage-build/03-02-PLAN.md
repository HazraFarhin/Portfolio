---
phase: 03-homepage-build
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/selected-work.ts
  - src/content/case-studies/deferred.ts
  - src/components/home/SelectedWork.tsx
  - src/components/home/SelectedWork.test.tsx
  - src/content/how-i-work.ts
  - src/components/home/HowIWork.tsx
  - src/components/home/HowIWork.test.tsx
autonomous: true
requirements: [HOME-03, HOME-04, HOME-05]

must_haves:
  truths:
    - "Selected Work renders exactly the 6 real case studies from src/content/case-studies/loader.ts's caseStudies export, in IA order (cad, verzion-cloud-migration, tata-capital-ai-interface, mashreq, astrosure.ai, adreport.io), on initial render (HOME-03)"
    - "Clicking the 'See more' toggle reveals the 5 deferred case studies (riyaah, icici-bank-atm-kiosk, ambit, northernarc, citrus) appended into the SAME grid as coming-soon cards; clicking again collapses back to 6; the toggle itself never disappears in either state (HOME-04, D-09)"
    - "Selected Work grid handles the 6-to-11 population change in one grid, never a separate list/section (zero-one-many, UI-SPEC UI Considerations #1, covered)"
    - "Deferred case-study cards render as full-looking Cards carrying a visible 'Coming soon' tag, never a blank/name-only chip (empty state, UI-SPEC UI Considerations #2, covered)"
    - "Selected Work card summaries are 2-line CSS-clamped regardless of the real summary length differences across the 6 case studies (long-text, UI-SPEC UI Considerations #4, covered)"
    - "How I Work renders the 6 Studio Method action-words (listen/align/reduce/design./prototype/systemize) and the 5-step Operating Loop (Understand->Align->Structure->Design->Transfer) together in ONE section, never as two separate sections (HOME-05, D-02)"
  prohibitions:
    - "MUST NOT render Selected Work using Homepage Copy V2.md §07's superseded fictional project copy (e.g. 'AI-Native Banking Interface', 'Cloud Migration Platform', 'Luxury Beauty Commerce Platform') when real case-study loader data exists -- this would misrepresent Hazra's actual portfolio to a recruiter (D-08)"
    - "MUST NOT render the 5 deferred-slug coming-soon cards in a way that is visually indistinguishable from the 6 real, live case-study cards -- the 'Coming soon' tag must always be present and visible so a recruiter is never misled into treating unfinished work as a shipped case study (D-10)"
  artifacts:
    - src/content/selected-work.ts
    - src/content/case-studies/deferred.ts
    - src/components/home/SelectedWork.tsx
    - src/content/how-i-work.ts
    - src/components/home/HowIWork.tsx
  key_links:
    - "SelectedWork.tsx -> caseStudies (src/content/case-studies/loader.ts, existing Phase 2 export, read-only)"
    - "SelectedWork.tsx -> deferredCaseStudies (this plan's own src/content/case-studies/deferred.ts)"
    - "SelectedWork.tsx deferred-card hrefs -> /case-study/<slug> routes built by Plan 03-04 (ComingSoonRoute)"
---

<objective>
Build Selected Work (HOME-03/HOME-04, D-08/D-09/D-10) -- the homepage's real-data grid of the 6 featured case studies with an inline "see more" toggle revealing 5 deferred coming-soon cards -- and How I Work (HOME-05, D-02) -- the combined Studio Method action-words + 5-step Operating Loop section.

Purpose: Selected Work is this phase's only genuinely new interaction pattern (stateful toggle over real loader data + a small deferred-slug stub dataset); How I Work is the fixed-sequence combination the user explicitly locked as ONE section, not two. Both are independent of every other Wave 1 plan's files.
Output: `src/content/selected-work.ts`, `src/content/case-studies/deferred.ts`, `src/components/home/SelectedWork.tsx` (+test), `src/content/how-i-work.ts`, `src/components/home/HowIWork.tsx` (+test).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-build/03-CONTEXT.md
@.planning/phases/03-homepage-build/03-UI-SPEC.md
@Portfolio-Documentation/Homepage Copy V2.md
@src/content/case-studies/loader.ts
@src/content/case-studies/schema.ts
@src/components/ui/Button.tsx
@src/components/ui/Card.tsx
@src/components/ui/Typography.tsx
@src/components/case-study/ImagePlaceholder.tsx
@src/components/case-study/Process.tsx
@src/motion/useScrollReveal.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Selected Work -- real 6 + deferred 5 toggle (HOME-03, HOME-04, D-08/D-09/D-10)</name>
  <files>src/content/selected-work.ts, src/content/case-studies/deferred.ts, src/components/home/SelectedWork.tsx, src/components/home/SelectedWork.test.tsx</files>
  <read_first>src/content/case-studies/loader.ts, src/content/case-studies/schema.ts, src/routes/case-study.tsx (defensive-lookup style, Button href precedent for internal routes), .planning/phases/03-homepage-build/03-RESEARCH.md (Pattern 2), .planning/phases/03-homepage-build/03-UI-SPEC.md (Selected Work section)</read_first>
  <behavior>
    - Initial render: exactly 6 cards, in caseStudies' order-sorted order, zero "Coming soon" labels
    - Click toggle: 5 more cards appear (11 total), each with a "Coming soon" label and href /case-study/<deferred-slug>
    - Click toggle again: back to 6 cards; toggle button remains present in both states
    - Toggle label reads "See more" collapsed, "See less" expanded
  </behavior>
  <action>
    Create `src/content/selected-work.ts` exporting `interface SelectedWorkContent { label: string; heading: string; supportingCopy: string; footnote: string; seeMoreLabel: string; seeLessLabel: string }` and `export const selectedWorkContent: SelectedWorkContent = { label: 'Selected Work', heading: 'Interfaces Built to Hold Up', supportingCopy: "Every project here followed the same loop: understand the problem and the business goal behind it, align with stakeholders and developers on what's actually feasible, structure a solution, design to remove friction, then hand off something reusable. The sectors changed. The process didn't.", footnote: 'Design Systems · Multi-Market UX · Dashboards · Conversational & AI Interfaces · Commerce · Business Requirement Translation · Developer Handoff', seeMoreLabel: 'See more', seeLessLabel: 'See less' }` -- sourced verbatim from `Portfolio-Documentation/Homepage Copy V2.md` §07, EXCLUDING the 5 old per-project copy blocks (D-08 explicitly bans that copy since it doesn't match the real case studies).

    Create `src/content/case-studies/deferred.ts` exporting `interface DeferredCaseStudy { slug: string; title: string }` and `export const deferredCaseStudies: DeferredCaseStudy[] = [ { slug: 'riyaah', title: 'Riyaah' }, { slug: 'icici-bank-atm-kiosk', title: 'ICICI Bank ATM Kiosk' }, { slug: 'ambit', title: 'Ambit' }, { slug: 'northernarc', title: 'NorthernArc' }, { slug: 'citrus', title: 'Citrus' } ]` in exactly this order (matches `Information Architecture.md`'s slug order). Add a JSDoc block flagging these titles as `[ASSUMED]` humanized display copy (RESEARCH.md Assumptions Log A1) -- not sourced from any case-study content file, since none exist for these 5 slugs (Phase 2 D-10). Do NOT modify `src/content/case-studies/loader.ts` or `parse.ts` (RESEARCH.md Anti-Patterns).

    Create `src/components/home/SelectedWork.tsx`: `<section id="selected-work" ref={ref}>` with `useScrollReveal(ref)`. Render `selectedWorkContent.label`, `.heading`, `.supportingCopy`. Add `const [expanded, setExpanded] = useState(false)`. Render one `<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-lg">` containing: (a) `caseStudies.map((cs) => ...)` (imported from `../../content/case-studies/loader`) -- each a `<Card key={cs.slug}>` with `<ImagePlaceholder caption={`${cs.title} cover -- pending`} size="stage" />`, `<Heading as="h3">{cs.title}</Heading>`, `<Label>{cs.client}</Label>`, `<Body className="line-clamp-2">{cs.summary}</Body>`, `<Button variant="ghost" href={`/case-study/${cs.slug}`}>View case study →</Button>`; (b) when `expanded` is true, `deferredCaseStudies.map((dcs) => ...)` (imported from `../../content/case-studies/deferred`) -- visually identical `<Card key={dcs.slug}>` structure (same `ImagePlaceholder size="stage"`, same `Heading`/`Button` shape) PLUS `<Label>Coming soon</Label>`, `href={`/case-study/${dcs.slug}`}` (resolved by Plan 03-04's literal routes). Below the grid: `<Button variant="ghost" onClick={() => setExpanded((e) => !e)} aria-expanded={expanded}>{expanded ? selectedWorkContent.seeLessLabel : selectedWorkContent.seeMoreLabel}</Button>` -- persistent, never conditionally unmounted (D-09's locked persistent-toggle choice). Render `selectedWorkContent.footnote` below the toggle.
  </action>
  <verify>
    <automated>npm run test -- src/components/home/SelectedWork.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - On initial render, exactly 6 case-study cards render in `caseStudies`' order (cad, verzion-cloud-migration, tata-capital-ai-interface, mashreq, astrosure.ai, adreport.io), and zero "Coming soon" labels exist
    - Clicking the toggle appends exactly 5 more cards (11 total), with hrefs `/case-study/riyaah`, `/case-study/icici-bank-atm-kiosk`, `/case-study/ambit`, `/case-study/northernarc`, `/case-study/citrus`, each carrying a "Coming soon" label
    - Clicking the toggle again returns to exactly 6 cards; the toggle button element persists in the DOM in both states (never removed/unmounted)
    - The toggle button's visible label is `See more` collapsed and `See less` expanded
    - `SelectedWork.tsx` imports `caseStudies` from `'../../content/case-studies/loader'` and contains zero occurrences of any of the superseded fictional project titles from Homepage Copy V2.md §07 (`AI-Native Banking Interface`, `Cloud Migration Platform`, `Luxury Beauty Commerce Platform`, `AI-Based Astrology Platform`, `Analytics Dashboards`) -- grep -c for each returns 0
  </acceptance_criteria>
  <done>Selected Work renders the real 6 loader-sourced case studies in IA order and toggles in the 5 deferred coming-soon cards without ever using the superseded fictional copy, verified by SelectedWork.test.tsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: How I Work -- Studio Method + Operating Loop, combined (HOME-05, D-02)</name>
  <files>src/content/how-i-work.ts, src/components/home/HowIWork.tsx, src/components/home/HowIWork.test.tsx</files>
  <read_first>src/components/case-study/Process.tsx, src/content/hero.ts, .planning/phases/03-homepage-build/03-CONTEXT.md (D-02), .planning/phases/03-homepage-build/03-UI-SPEC.md ("How I Work (Method)" section)</read_first>
  <behavior>
    - actionWords has exactly 6 entries: listen, align, reduce, design., prototype, systemize
    - loopSteps has exactly 5 entries: Understand, Align, Structure, Design, Transfer, each with a description
    - Both beats render inside the SAME section, never split across two sections
  </behavior>
  <action>
    Create `src/content/how-i-work.ts` exporting `interface LoopStep { name: string; description: string }`, `interface HowIWorkContent { label: string; heading: string; methodIntro: string; actionWords: string[]; loopSteps: LoopStep[] }`, and `export const howIWorkContent: HowIWorkContent = { label: 'How I Work', heading: 'The Loop', methodIntro: "The method doesn't change per industry, only the constraints do. Understand what's actually breaking for the user and what the business needs to move forward. Align with clients and developers early on what's feasible, not after the designs are done. Cut the interface back to what earns its place on screen, then build it in a way the next designer or developer can pick up without a briefing.", actionWords: ['listen', 'align', 'reduce', 'design.', 'prototype', 'systemize'], loopSteps: [ { name: 'Understand', description: "Research the product, the users, and the real constraint, technical, business, or regulatory, that's actually shaping the problem." }, { name: 'Align', description: "Sit down with clients and developers early to confirm what's actually needed and what's actually buildable, before it's baked into a design nobody can ship." }, { name: 'Structure', description: 'Turn findings into information architecture and flows the whole team, including developers and stakeholders, can agree on before a single pixel is final.' }, { name: 'Design', description: 'Build high-fidelity screens and interactive prototypes tested against real content and edge cases, not idealised data.' }, { name: 'Transfer', description: 'Document components, states, and logic, and hand off production-ready screens, so the system keeps working without me.' } ] }` -- action words + methodIntro sourced verbatim from Homepage Copy V2.md §03, loopSteps sourced verbatim from §09, combined per D-02.

    Create `src/components/home/HowIWork.tsx`: `<section id="how-i-work" ref={ref}>` with `useScrollReveal(ref)`. Render `howIWorkContent.label`, `.heading`, `.methodIntro`. Then the 6 action-words as a literal, hardcoded `<div className="flex flex-wrap gap-md">` of exactly 6 `<Heading as="span">{howIWorkContent.actionWords[N]}</Heading>` blocks (N = 0..5, literal indices with `{/* Action word N */}` numbered comments -- NEVER `.actionWords.map(...)`). Then the 5 Loop steps as a literal, hardcoded `<div className="flex flex-col gap-xl">` of exactly 5 blocks (N = 0..4, `{/* Loop step N */}` comments), each `<Label as="h3">{`${String(N + 1).padStart(2, '0')} — ${howIWorkContent.loopSteps[N].name}`}</Label>` + `<Body>{howIWorkContent.loopSteps[N].description}</Body>` -- mirrors `Process.tsx`'s numbered-eyebrow pattern.
  </action>
  <verify>
    <automated>npm run test -- src/components/home/HowIWork.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `howIWorkContent.actionWords` equals exactly `['listen','align','reduce','design.','prototype','systemize']`
    - `howIWorkContent.loopSteps` has exactly 5 entries named `Understand, Align, Structure, Design, Transfer` in that order
    - HowIWork.tsx renders `<section id="how-i-work">` containing all 6 action words and all 5 loop step names + descriptions in the DOM
    - `grep -c -E '\.actionWords\.map\(|\.loopSteps\.map\(' src/components/home/HowIWork.tsx` returns 0
  </acceptance_criteria>
  <done>How I Work renders both the 6 Studio Method action-words and the 5-step Operating Loop as one combined section per D-02, verified by HowIWork.test.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SelectedWork.tsx -> internal routes | Every card's href is a same-origin `/case-study/<slug>` path -- no external navigation, no user input drives the href |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Information Disclosure | deferred.ts's [ASSUMED] humanized titles | low | accept | Purely cosmetic display copy, flagged in code as `[ASSUMED]`; no real client/industry/summary data is fabricated for the 5 deferred slugs (RESEARCH.md Pitfall 3) -- corrective edits are a single-file change |
| T-03-05 | Tampering | SelectedWork.tsx toggle state | low | accept | Local `useState` UI toggle only, no persisted state, no user input crosses a trust boundary |
</threat_model>

<verification>
- `npm run test -- src/components/home/SelectedWork.test.tsx src/components/home/HowIWork.test.tsx` both pass
- `npm run test` (full suite) still passes with no regressions to `src/content/case-studies/loader.test.ts` or any Phase 2 test
</verification>

<success_criteria>
Selected Work correctly composes real loader data with the deferred stub dataset behind a persistent toggle; How I Work renders both required content beats as one section. Both ready to mount into `home.tsx` by Plan 03-07.
</success_criteria>

<output>
Create `.planning/phases/03-homepage-build/03-02-SUMMARY.md` when done
</output>
