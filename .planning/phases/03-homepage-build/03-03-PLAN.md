---
phase: 03-homepage-build
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/skills-tools.ts
  - src/components/home/SkillsTools.tsx
  - src/components/home/SkillsTools.test.tsx
  - src/content/about.ts
  - src/components/home/About.tsx
  - src/components/home/About.test.tsx
  - src/components/case-study/ImagePlaceholder.tsx
  - src/components/case-study/ImagePlaceholder.test.tsx
autonomous: true
requirements: [HOME-06, HOME-07]

must_haves:
  truths:
    - "Skills & Tools renders the 5 Operating Stack capability cards (Discovery & Research; UX Strategy & IA; UI Design & Prototyping; Stakeholder & Developer Alignment; Design Systems & Dev Handoff) per D-03 (HOME-06)"
    - "Skills & Tools renders a tool-chip row computed from the real case-study frontmatter tags field (10 distinct alphabetically-sorted values), never an invented or hand-typed tool list (HOME-06, D-14 clarified)"
    - "Skills & Tools tool-chip row wraps via flex-wrap and never clips/scrolls horizontally regardless of chip count (overflow, UI-SPEC UI Considerations #6, covered)"
    - "About renders a compact, fresh 2-4 sentence bio distinct from the Hero's longer statement, alongside a portrait-sized headshot placeholder (HOME-07, D-12, D-13)"
    - "About's bio always renders in full with no truncation, since the 2-4 sentence cap is a copy-authoring constraint, not a CSS clamp (long-text, UI-SPEC UI Considerations #5, covered)"
    - "ImagePlaceholder's size union gains a new 'portrait' token additively (aspect 3/4, max-h-[320px]) with zero regression to the existing banner/stage/centerpiece sizes already used across all 6 case-study pages (RESEARCH.md Pitfall 5)"
  artifacts:
    - src/content/skills-tools.ts
    - src/components/home/SkillsTools.tsx
    - src/content/about.ts
    - src/components/home/About.tsx
    - "src/components/case-study/ImagePlaceholder.tsx (modified: new 'portrait' ImagePlaceholderSize token)"
  key_links:
    - "skills-tools.ts's skillTags -> caseStudies.flatMap(tags) (src/content/case-studies/loader.ts, existing Phase 2 export, read-only)"
    - "About.tsx -> ImagePlaceholder's new 'portrait' size (this plan's own extension to the Phase 2 component)"
---

<objective>
Build Skills & Tools (HOME-06, D-03/D-14) -- the 5 Operating Stack capability cards plus a real, computed tool-chip row -- and About (HOME-07, D-12/D-13) -- a compact fresh bio with a headshot placeholder, which requires additively extending Phase 2's shared `ImagePlaceholder` component with a new `portrait` size token.

Purpose: Both sections are self-contained (no dependency on any other new Phase 3 file). Skills & Tools' tool-chip aggregation must stay honest to real case-study data rather than an invented list (D-14 clarified). About requires a small, backward-compatible addition to a shared Phase 2 component, scoped and verified in this plan so no other plan needs to touch `ImagePlaceholder.tsx`.
Output: `src/content/skills-tools.ts`, `src/components/home/SkillsTools.tsx` (+test), `src/content/about.ts`, `src/components/home/About.tsx` (+test), `src/components/case-study/ImagePlaceholder.tsx` extended with `portrait`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-build/03-CONTEXT.md
@.planning/phases/03-homepage-build/03-UI-SPEC.md
@Portfolio-Documentation/Homepage Copy V2.md
@src/content/case-studies/loader.ts
@src/content/case-studies/schema.ts
@src/content/hero.ts
@src/components/case-study/Overview.tsx
@src/components/case-study/ToolsUsed.tsx
@src/components/case-study/ImagePlaceholder.tsx
@src/components/ui/Card.tsx
@src/components/ui/Typography.tsx
@src/motion/useScrollReveal.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Skills &amp; Tools -- 5 capability cards + computed tool chips (HOME-06, D-03, D-14 clarified)</name>
  <files>src/content/skills-tools.ts, src/components/home/SkillsTools.tsx, src/components/home/SkillsTools.test.tsx</files>
  <read_first>src/content/case-studies/loader.ts, src/content/case-studies/schema.ts, src/components/case-study/Overview.tsx, src/components/case-study/ToolsUsed.tsx (chip styling precedent), .planning/phases/03-homepage-build/03-CONTEXT.md (D-03, D-14 clarified)</read_first>
  <behavior>
    - skillsToolsContent.cards has exactly 5 entries with D-03's exact titles
    - skillTags is computed (not hand-typed) from caseStudies.flatMap(tags), deduplicated and alphabetically sorted, equal to the 10 real values
    - Tool-chip row wraps (flex-wrap), never horizontally scrolls or clips
  </behavior>
  <action>
    Create `src/content/skills-tools.ts` exporting `interface SkillCard { title: string; description: string }`, `interface SkillsToolsContent { label: string; heading: string; leadLine: string; cards: SkillCard[]; closingNote: string }`, and `export const skillsToolsContent: SkillsToolsContent = { label: 'What I Do', heading: 'Operating Stack', leadLine: 'I design the interface layer around a product: research, structure, screens, and the system that holds them together, shaped by conversations with the people who have to approve it and the people who have to build it.', cards: [ { title: 'Discovery & Research', description: "Understanding real user needs and real business goals before a single screen gets drawn, so the two aren't discovered to be in conflict halfway through the project." }, { title: 'UX Strategy & IA', description: 'Turning ambiguous problems into structured flows and wireframes that hold up against edge cases, technical limits, and what the business actually needs the product to do.' }, { title: 'UI Design & Prototyping', description: 'High-fidelity interfaces and interactive prototypes built to a standard that survives contact with real developers and real users, not just a review meeting.' }, { title: 'Stakeholder & Developer Alignment', description: 'Working directly with clients, product leads, and engineering teams to turn business requirements and technical constraints into design decisions everyone has actually agreed to, not just signed off on.' }, { title: 'Design Systems & Dev Handoff', description: "Reusable components and production-ready screens that shorten the distance between design and shipped product, so quality doesn't erode at handoff." } ], closingNote: 'I focus on what to minimize before planning out the additions, and on getting the business, the user, and the dev team reading from the same brief.' }` -- the 5 card titles use D-03's exact wording; descriptions sourced verbatim from `Homepage Copy V2.md` §04; `heading: 'Operating Stack'` is a planner-derived label matching the source doc's own "04/Operating Stack" section name (no explicit `Heading:` field exists in the source for this section).

    In the same file, add `import { caseStudies } from './case-studies/loader';` and `export const skillTags: string[] = [...new Set(caseStudies.flatMap((cs) => cs.tags))].sort();` -- per D-14 clarified: the literal frontmatter `tags` field only (NOT the unstructured `Tools Used` Markdown body), alphabetically sorted for a stable display order independent of `Set` iteration order. This MUST be a computed expression, never a hand-written array literal (RESEARCH.md Don't Hand-Roll) -- it stays honest to real case-study data as those files are edited.

    Create `src/components/home/SkillsTools.tsx`: `<section id="skills-tools" ref={ref}>` with `useScrollReveal(ref)`. Render `label`/`heading`/`leadLine`. Then exactly 5 literal `Card` blocks (index 0..4 into `skillsToolsContent.cards`, `{/* Card N */}` numbered comments, NEVER `.cards.map(...)`) inside a `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-lg`, each `<Card><Heading as="h3">{title}</Heading><Body>{description}</Body></Card>`. Then render `closingNote` as a `<Body>`. Then a tool-chip row `<div className="flex flex-wrap gap-sm">` that DOES `.map()` over the imported `skillTags` (variable-length, data-driven -- the intentional exception to the fixed-sequence rule), each chip `<Label as="span" className="rounded-full border border-line px-md py-xs">{tag}</Label>`.
  </action>
  <verify>
    <automated>npm run test -- src/components/home/SkillsTools.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `skillsToolsContent.cards` has exactly 5 entries with titles `Discovery & Research`, `UX Strategy & IA`, `UI Design & Prototyping`, `Stakeholder & Developer Alignment`, `Design Systems & Dev Handoff` in that order
    - `skillTags` equals `['AI Interface','Banking','Cloud','Dashboard','Design System','Enterprise','InsurTech','Mobile','UI','UX']`
    - `src/content/skills-tools.ts` contains `caseStudies.flatMap` and contains zero occurrence of a hardcoded 10-item string array literal duplicating `skillTags`'s values (i.e. `skillTags` is not reassigned to a literal array anywhere in the file)
    - SkillsTools.tsx renders all 5 capability cards and all 10 tool-chip labels in the DOM, inside a `flex flex-wrap` container (no `overflow-x-auto` class on the chip row)
    - `grep -c '\.cards\.map(' src/components/home/SkillsTools.tsx` returns 0
  </acceptance_criteria>
  <done>Skills & Tools renders the 5 Operating Stack cards plus all 10 real, computed tool chips, verified by SkillsTools.test.tsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: About -- fresh bio + headshot placeholder, ImagePlaceholder 'portrait' extension (HOME-07, D-12, D-13)</name>
  <files>src/components/case-study/ImagePlaceholder.tsx, src/components/case-study/ImagePlaceholder.test.tsx, src/content/about.ts, src/components/home/About.tsx, src/components/home/About.test.tsx</files>
  <read_first>src/components/case-study/ImagePlaceholder.tsx, src/components/case-study/ImagePlaceholder.test.tsx, .planning/PROJECT.md (Context section), src/content/hero.ts (statement to condense from, do not repeat verbatim), .planning/phases/03-homepage-build/03-RESEARCH.md (Pitfall 5)</read_first>
  <behavior>
    - ImagePlaceholderSize gains 'portrait' additively; existing banner/stage/centerpiece values and all existing ImagePlaceholder.test.tsx assertions are unchanged
    - aboutContent.bio is 2-4 sentences, textually distinct from heroContent.statement
    - About.tsx renders the full bio with zero truncation classes, plus one ImagePlaceholder size="portrait"
  </behavior>
  <action>
    First, modify `src/components/case-study/ImagePlaceholder.tsx`: extend `export type ImagePlaceholderSize = 'banner' | 'stage' | 'centerpiece';` to `'banner' | 'stage' | 'centerpiece' | 'portrait';`, and add one new entry to the `sizeClasses` record: `portrait: 'aspect-[3/4] max-h-[320px]'` (a comment above it, matching the file's existing per-entry comment style, e.g. `// About headshot (Phase 3) -- compact portrait aspect.`). This is additive-only: do not change the existing `banner`/`stage`/`centerpiece` values, and do not remove or alter any existing assertion in `ImagePlaceholder.test.tsx` -- only add new test cases covering `portrait` if useful.

    Create `src/content/about.ts` exporting `interface AboutContent { label: string; heading: string; bio: string; headshotCaption: string }` and `export const aboutContent: AboutContent = { label: 'About', heading: 'About', bio: "Hazra is a UX/UI Designer with about four years shaping interfaces for banking, e-commerce, telecom, martech, beauty, and sports-tech products across India, the UAE, Saudi Arabia, Canada, and the USA. Every project starts from the same brief: understand what the user needs and what the business needs the screen to do, then design and hand off systems a team can run without her in the room. Currently remote, working comfortably across CET-to-GST hours.", headshotCaption: 'Hazra Farhin — photo pending' }`. This bio (3 sentences, within D-12's 2-4 sentence cap) is fresh, condensed copy derived from `heroContent.statement` plus `PROJECT.md`'s Context section -- NOT a verbatim repeat of the Hero statement. Add a JSDoc noting this bio is Claude's-discretion original copy per D-12, not sourced verbatim from any file.

    Create `src/components/home/About.tsx`: `<section id="about" ref={ref}>` with `useScrollReveal(ref)`, a two-column layout (`grid grid-cols-1 md:grid-cols-2 gap-xl items-center`) with `<ImagePlaceholder caption={aboutContent.headshotCaption} size="portrait" />` in one column and `<Label>{aboutContent.label}</Label>`, `<Heading as="h2">{aboutContent.heading}</Heading>`, `<Body>{aboutContent.bio}</Body>` in the other -- no truncation/clamp class on the bio paragraph (the 2-4 sentence cap is a copy-authoring constraint per UI-SPEC, not a CSS clamp).
  </action>
  <verify>
    <automated>npm run test -- src/components/case-study/ImagePlaceholder.test.tsx src/components/home/About.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `ImagePlaceholderSize` includes `'portrait'`; every pre-existing test in `ImagePlaceholder.test.tsx` still passes unmodified (no regression to `banner`/`stage`/`centerpiece` usages across all 6 case-study pages)
    - `aboutContent.bio` splits into 2-4 sentences (by `. ` boundary count) and is not textually identical to `heroContent.statement`
    - About.tsx renders `<section id="about">`, the full bio text with no `truncate`/`line-clamp`/`overflow-hidden` class present on the bio paragraph, and exactly one `ImagePlaceholder` with `size="portrait"`
  </acceptance_criteria>
  <done>About renders a fresh, condensed 2-4 sentence bio alongside a portrait-sized headshot placeholder; ImagePlaceholder's size union is extended additively with zero regression to existing case-study usages -- verified by About.test.tsx and the full ImagePlaceholder.test.tsx suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Skills & Tools and About render only developer-authored static content plus a real, computed (not user-influenced) tag aggregation over build-time content -- no user input, no external navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Information Disclosure | About.tsx bio/headshot placeholder | low | accept | Bio content is intentionally public-facing professional copy (job-search site); no PII beyond what the résumé/Hero already state publicly |
| T-03-07 | Tampering | ImagePlaceholder.tsx additive change | low | accept | The `portrait` token is a pure addition to an existing `Record`/union; the full pre-existing `ImagePlaceholder.test.tsx` suite is required to still pass, guarding against accidental regression to the 6 live case-study pages that already consume `banner`/`stage`/`centerpiece` |
</threat_model>

<verification>
- `npm run test -- src/components/home/SkillsTools.test.tsx src/components/home/About.test.tsx src/components/case-study/ImagePlaceholder.test.tsx` all pass
- `npm run test` (full suite) still passes with no regressions to any Phase 2 case-study test
</verification>

<success_criteria>
Skills & Tools renders real, computed tag data (not invented); About renders fresh, condensed bio copy; `ImagePlaceholder` gains a backward-compatible `portrait` size with zero regression to Phase 2's 6 case-study pages. Both ready to mount into `home.tsx` by Plan 03-07.
</success_criteria>

<output>
Create `.planning/phases/03-homepage-build/03-03-SUMMARY.md` when done
</output>
