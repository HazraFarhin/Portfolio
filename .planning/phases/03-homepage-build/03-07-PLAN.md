---
phase: 03-homepage-build
plan: 07
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-06"]
files_modified:
  - src/routes/home.tsx
  - src/content/hero.ts
  - src/routes/home.test.tsx
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, HOME-06, HOME-07, HOME-08, CONT-04]

must_haves:
  truths:
    - "Above the fold, the hero states role, specialization, and availability status (HOME-01, unchanged behavior, still true after this phase's changes)"
    - "Scrolling the homepage reveals, in IA order: hero, proof strip, Selected Work, Field Archive, How I Work, Skills & Tools, About, Contact/Footer, with a persistent Nav above them -- the complete D-01 scope (all 7 IA-locked sections plus the 3 additions beyond the locked IA: nav bar/D-06, Field Archive/D-04, Studio Method folded into How I Work/D-02), composing HOME-02 through HOME-08 (Roadmap Success Criteria #2)"
    - "The Hero's primary CTA now navigates to #selected-work, the real Selected Work section -- never the Phase-1 placeholder #hero anchor (HOME-01 fix, Pitfall 1 resolved)"
    - "Nav bar is mounted inside home.tsx only -- it does not render on /case-study/:slug routes (nav/scope, UI-SPEC UI Considerations #12, covered)"
    - statement: "Full homepage scroll order (hero -> proof strip -> Selected Work -> Field Archive -> How I Work -> Skills & Tools -> About -> Footer) is visually confirmed correct when loaded in a real browser"
      verification: backstop
  artifacts:
    - "src/routes/home.tsx (modified: mounts Nav + 7 new section components as siblings of the existing Hero)"
    - "src/content/hero.ts (modified: ctaHref repointed from #hero to #selected-work)"
    - "src/routes/home.test.tsx (modified: stale #hero assertion corrected, composition-order assertions added)"
  key_links:
    - "home.tsx -> Nav, ProofStrip, SelectedWork, FieldArchive, HowIWork, SkillsTools, About, Footer (all 7 new components + Nav, built across Plans 03-01/02/03/04/06)"
    - "heroContent.ctaHref (#selected-work) -> SelectedWork.tsx's own <section id=\"selected-work\">"
---

<objective>
Wire every homepage section built in Wave 1/2 (Nav, Proof Strip, Selected Work, Field Archive, How I Work, Skills & Tools, About, Footer) into `src/routes/home.tsx` as siblings of the existing Hero, in locked Information Architecture order, and repoint the Hero's CTA from the Phase-1 placeholder `#hero` anchor to the real `#selected-work` section (HOME-01's remaining fix, RESEARCH.md Pitfall 1).

Purpose: This is the phase's capstone integration -- no individual section is "viewable" by a user (the actual observable truth every HOME-XX requirement demands) until it is mounted into the live route. This plan is deliberately last (Wave 3) because it needs every other plan's component to already exist.
Output: `src/routes/home.tsx` composing all 8 new sections + Nav, `src/content/hero.ts`'s corrected `ctaHref`, `src/routes/home.test.tsx` updated to match.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-build/03-CONTEXT.md
@.planning/phases/03-homepage-build/03-UI-SPEC.md
@src/routes/home.tsx
@src/routes/home.test.tsx
@src/content/hero.ts
@src/components/home/Nav.tsx
@src/components/home/ProofStrip.tsx
@src/components/home/SelectedWork.tsx
@src/components/home/FieldArchive.tsx
@src/components/home/HowIWork.tsx
@src/components/home/SkillsTools.tsx
@src/components/home/About.tsx
@src/components/home/Footer.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mount Nav + all 7 sections into home.tsx, repoint heroContent.ctaHref (HOME-01, HOME-02..08, CONT-04)</name>
  <files>src/routes/home.tsx, src/content/hero.ts</files>
  <read_first>src/routes/home.tsx, src/content/hero.ts, src/components/home/Nav.tsx, src/components/home/ProofStrip.tsx, src/components/home/SelectedWork.tsx, src/components/home/FieldArchive.tsx, src/components/home/HowIWork.tsx, src/components/home/SkillsTools.tsx, src/components/home/About.tsx, src/components/home/Footer.tsx, .planning/phases/03-homepage-build/03-UI-SPEC.md (Homepage Layout & Composition -- locked section order)</read_first>
  <behavior>
    - heroContent.ctaHref equals '#selected-work'
    - home.tsx renders exactly 9 top-level elements: 1 nav + 8 sections, in the exact locked IA order
    - No new <section> wrapper is added around any of the 7 imported components (each already owns its own root <section>)
  </behavior>
  <action>
    Modify `src/content/hero.ts`: change `ctaHref: '#hero'` to `ctaHref: '#selected-work'`. Update the surrounding comment block (currently explaining the Phase-1 "no Selected Work section exists yet" rationale) to instead note: "Phase 3 repoints this to the real Selected Work section id once it exists (`src/components/home/SelectedWork.tsx`'s `id=\"selected-work\"`)." Remove the now-stale "Phase 1 ships Hero-only" framing from the comment.

    Modify `src/routes/home.tsx`: add imports for `Nav`, `ProofStrip`, `SelectedWork`, `FieldArchive`, `HowIWork`, `SkillsTools`, `About`, `Footer` from `'../components/home/Nav'`, `'../components/home/ProofStrip'`, etc. Render them as literal, hardcoded JSX siblings in this EXACT order (matching UI-SPEC's locked section order and RESEARCH.md's architecture diagram): `<Nav />` (above/outside the existing `<section id="hero">`), then the existing, untouched Hero `<section id="hero">` block, then `<ProofStrip />`, `<SelectedWork />`, `<FieldArchive />`, `<HowIWork />`, `<SkillsTools />`, `<About />`, `<Footer />`. Each of these 7 components already owns its own `<section id="...">` wrapper internally (built in Plans 03-01/02/03/06) -- render them as plain component tags, NOT re-wrapped in another `<section>`. Do not otherwise modify the existing Hero JSX.
  </action>
  <verify>
    <automated>npm run test -- src/routes/home.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `heroContent.ctaHref === '#selected-work'`
    - `src/routes/home.tsx` renders exactly 1 `<nav>` element and exactly 8 `<section>` elements with ids `hero, proof-strip, selected-work, field-archive, how-i-work, skills-tools, about, contact-footer`, in that exact document order
    - `grep -c '<section' src/routes/home.tsx` returns at most 1 (the file itself introduces zero new `<section>` wrappers -- the 8 sections all come from the imported components)
    - `src/router.tsx` is unmodified by this task (not part of this plan's `files_modified`)
  </acceptance_criteria>
  <done>home.tsx composes all 8 sections plus the persistent Nav in locked IA order on top of the existing Hero, and the Hero CTA now resolves to the real Selected Work section.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update home.test.tsx -- fix stale ctaHref assertion, add composition-order assertions</name>
  <files>src/routes/home.test.tsx</files>
  <read_first>src/routes/home.test.tsx (current, line 26's hardcoded `toBe('#hero')` assertion)</read_first>
  <behavior>
    - The existing test's hardcoded '#hero' assertion for ctaHref is corrected to '#selected-work'
    - New assertions confirm nav precedes all 8 sections, and the 8 section ids appear in exact locked order
  </behavior>
  <action>
    Modify `src/routes/home.test.tsx`: change the existing hardcoded assertion `expect(heroContent.ctaHref).toBe('#hero')` (currently inside the "gives the Hero section root an id=hero..." test) to `expect(heroContent.ctaHref).toBe('#selected-work')`, and update that test's own description/comment if it references `#hero` as the CTA target. Add a new `it(...)` block asserting: (a) `container.querySelector('nav')` exists and precedes the first `section` element in document order (via `compareDocumentPosition` or by comparing each element's index in `container.querySelectorAll('nav, section')`); (b) `container.querySelectorAll('section')` yields exactly 8 elements whose `id` attributes, read in DOM order, equal `['hero','proof-strip','selected-work','field-archive','how-i-work','skills-tools','about','contact-footer']`.
  </action>
  <verify>
    <automated>npm run test -- src/routes/home.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "toBe('#hero')" src/routes/home.test.tsx` returns 0 (the stale assertion is gone)
    - A test asserts `heroContent.ctaHref === '#selected-work'` and that the rendered Hero CTA's `href` matches
    - A test asserts the 8 section ids appear in `container.querySelectorAll('section')` in the exact order: `hero, proof-strip, selected-work, field-archive, how-i-work, skills-tools, about, contact-footer`
    - A test asserts a `nav` element exists and precedes the first `section` in DOM order
    - `npm run test` (full suite, all plans' test files combined) passes with zero failures
  </acceptance_criteria>
  <done>home.test.tsx's previously-stale #hero assertion is corrected and new composition-order assertions cover the full 9-element (1 nav + 8 sections) locked IA sequence; the full test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan only composes already-built, already-verified components into one route -- no new rendering logic, no new external surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12 | Tampering | home.tsx composition | low | accept | Composition-only change; every component it mounts was already verified in its own plan. `grep -c 'dangerouslySetInnerHTML' src/components/home/*.tsx src/content/*.ts` returns 0 across the whole phase -- no dynamic HTML injection surface exists anywhere in this phase's new code |
</threat_model>

<verification>
- `npm run test -- src/routes/home.test.tsx` passes
- `npm run test` (full suite, every test file created across Plans 03-01 through 03-07) passes with zero failures
- `npm run build` succeeds (confirms `tsc -b` type-checks the full composed route tree with no errors)
- Manual: load `/` in a browser, scroll top to bottom, confirm section order visually matches IA (VALIDATION.md Manual-Only Verifications)
</verification>

<success_criteria>
The homepage is a single, complete, scrollable page presenting Hero → Proof Strip → Selected Work → Field Archive → How I Work → Skills & Tools → About → Contact/Footer, with a persistent Nav, and the Hero CTA resolves to a real section. Every HOME-01 through HOME-08 and CONT-04 requirement is now observably true on the live `/` route, not just built in isolation.
</success_criteria>

<output>
Create `.planning/phases/03-homepage-build/03-07-SUMMARY.md` when done
</output>
