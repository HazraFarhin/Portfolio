---
phase: 03-homepage-build
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/coming-soon.tsx
  - src/routes/coming-soon.test.tsx
  - src/router.tsx
  - src/router.test.tsx
autonomous: true
requirements: [HOME-04]

must_haves:
  truths:
    - "Navigating to /case-study/<deferred-slug> for any of the 5 deferred slugs renders ComingSoonRoute, never the generic case-study 'not found' fallback or the app-level 404 (D-10)"
    - "Deferred-slug route rendering is an empty/no-content state that never crashes on an undefined lookup (UI-SPEC UI Considerations #3, covered)"
    - "The 5 deferred-slug routes are isolated in one clearly labeled, Phase-4-deletable block in router.tsx, since DEPL-03 (Phase 4) requires removing or guarding them once real content lands (D-11)"
    - "ComingSoonRoute's visible copy is distinct from both not-found.tsx's 'Page not found.' and case-study.tsx's 'Case study not found.' fallbacks"
  artifacts:
    - src/routes/coming-soon.tsx
    - "src/router.tsx (modified: 5 literal deferred-slug routes added, case-study/:slug and * catch-all untouched)"
    - src/router.test.tsx
  key_links:
    - "router.tsx's 5 literal case-study/<slug> routes -> routes/coming-soon.tsx (ComingSoonRoute)"
    - "Plan 03-02's SelectedWork.tsx deferred-card hrefs -> these same 5 literal routes"
---

<objective>
Build the coming-soon fallback route for the 5 deferred case-study slugs (D-10) and wire it into `router.tsx` as 5 literal, isolated routes that outrank the generic `case-study/:slug` route for those exact paths (D-11).

Purpose: This closes the click-through target for Plan 03-02's Selected Work "see more" cards. Isolating the 5 routes in one labeled block (rather than special-casing inside Phase 2's `case-study.tsx`) gives Phase 4 a single, obvious block to delete or guard as part of its own DEPL-03 work.
Output: `src/routes/coming-soon.tsx` (+test), `src/router.tsx` modified with 5 new literal routes, `src/router.test.tsx` (new).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-build/03-CONTEXT.md
@.planning/phases/03-homepage-build/03-UI-SPEC.md
@src/routes/not-found.tsx
@src/routes/case-study.tsx
@src/router.tsx
@src/components/ui/Button.tsx
@src/components/ui/Typography.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ComingSoonRoute component (D-10)</name>
  <files>src/routes/coming-soon.tsx, src/routes/coming-soon.test.tsx</files>
  <read_first>src/routes/not-found.tsx, src/routes/case-study.tsx (the "Case study not found." fallback block, for distinct-copy comparison), src/components/ui/Button.tsx, .planning/phases/03-homepage-build/03-UI-SPEC.md (Copywriting Contract, Coming-Soon Route section)</read_first>
  <behavior>
    - Renders the exact locked copy "Coming soon." and "This case study hasn't shipped yet. Explore the six live projects instead."
    - Renders a ghost Button "← Back to Selected Work" linking to /#selected-work
    - An sr-only heading announces the humanized project title for the given slug
  </behavior>
  <action>
    Create `src/routes/coming-soon.tsx` exporting default `ComingSoonRoute({ slug }: { slug: string })`. Define a local `const DEFERRED_TITLES: Record&lt;string, string&gt; = { riyaah: 'Riyaah', 'icici-bank-atm-kiosk': 'ICICI Bank ATM Kiosk', ambit: 'Ambit', northernarc: 'NorthernArc', citrus: 'Citrus' };` (keep these 5 title strings identical to Plan 03-02's `deferredCaseStudies` titles). Render, using the SAME layout classes as `not-found.tsx` (`flex flex-col items-center justify-center gap-md min-h-[50vh] text-center px-lg`): an `<h1 className="sr-only">{`${DEFERRED_TITLES[slug] ?? slug} — coming soon`}</h1>` (screen-reader-only project identification), then `<Body>Coming soon.</Body>`, then `<Body>This case study hasn't shipped yet. Explore the six live projects instead.</Body>` (exact locked copy from UI-SPEC's Copywriting Contract -- do not interpolate the slug into this visible line), then `<Button variant="ghost" href="/#selected-work">← Back to Selected Work</Button>`. The `href="/#selected-work"` triggers a full navigation to `/` and the browser jumps to the `#selected-work` anchor once that page loads (this project's plain-anchor precedent, no Lenis anchor interception per `MotionProvider.tsx`).
  </action>
  <verify>
    <automated>npm run test -- src/routes/coming-soon.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Renders exactly the strings `Coming soon.` and `This case study hasn't shipped yet. Explore the six live projects instead.`
    - Renders a link with visible text `← Back to Selected Work` and `href="/#selected-work"`
    - For each of the 5 deferred slugs passed as `slug`, the `sr-only` heading text contains the correct humanized title (`Riyaah`, `ICICI Bank ATM Kiosk`, `Ambit`, `NorthernArc`, `Citrus`)
    - `grep -c "Page not found." src/routes/coming-soon.tsx` and `grep -c "Case study not found." src/routes/coming-soon.tsx` both return 0
  </acceptance_criteria>
  <done>ComingSoonRoute renders quiet, on-brand "coming soon" copy distinct from both existing not-found fallbacks, verified by coming-soon.test.tsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Router wiring -- 5 literal deferred-slug routes (D-10, D-11)</name>
  <files>src/router.tsx, src/router.test.tsx</files>
  <read_first>src/router.tsx, .planning/phases/03-homepage-build/03-RESEARCH.md (Pattern 5), .planning/phases/03-homepage-build/03-PATTERNS.md (router.tsx section)</read_first>
  <behavior>
    - router.routes[0].children contains 5 new entries with path case-study/&lt;deferred-slug&gt; rendering ComingSoonRoute
    - The existing case-study/:slug and * catch-all entries are untouched (still exactly one of each)
  </behavior>
  <action>
    Modify `src/router.tsx`: add `import ComingSoonRoute from './routes/coming-soon';`. Immediately above the `router` export, add: a comment line `// D-11: Phase 4 MUST delete/guard this whole block as part of its own DEPL-03 work (deferred slugs must not be publicly linked/indexed once real content lands).`, then `const DEFERRED_SLUGS = ['riyaah', 'icici-bank-atm-kiosk', 'ambit', 'northernarc', 'citrus'] as const;`, then `const DEFERRED_SLUG_ROUTES = DEFERRED_SLUGS.map((slug) => ({ path: `case-study/${slug}`, element: &lt;ComingSoonRoute slug={slug} /&gt; }));`. Spread `...DEFERRED_SLUG_ROUTES` into the router's `children` array, positioned before the existing `{ path: 'case-study/:slug', element: &lt;CaseStudyPage /&gt; }` entry (React Router's specificity-based ranking means array position doesn't affect matching -- literal segments always outrank the dynamic `:slug` segment for an exact-match URL -- placing them first just keeps the diff readable). Do NOT modify the `index: true` route, `case-study/:slug`, or the `*` catch-all. Do NOT modify `src/routes/case-study.tsx` or `src/routes/not-found.tsx`.

    Create `src/router.test.tsx` (new Wave-0 scaffold, no prior test file exists for `router.tsx`): import `{ router }` from `./router`; assert `router.routes[0].children` contains exactly 5 entries whose `path` is `case-study/riyaah`, `case-study/icici-bank-atm-kiosk`, `case-study/ambit`, `case-study/northernarc`, `case-study/citrus`; assert exactly one entry with `path: 'case-study/:slug'` and exactly one with `path: '*'` remain present.
  </action>
  <verify>
    <automated>npm run test -- src/router.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `router.routes[0].children` contains exactly 5 entries with `path` values `case-study/riyaah`, `case-study/icici-bank-atm-kiosk`, `case-study/ambit`, `case-study/northernarc`, `case-study/citrus`
    - `router.routes[0].children` still contains exactly one entry with `path: 'case-study/:slug'` and exactly one `path: '*'` entry
    - `src/routes/case-study.tsx` and `src/routes/not-found.tsx` are byte-unmodified by this task (neither appears in this plan's `files_modified`)
  </acceptance_criteria>
  <done>router.tsx gains 5 literal deferred-slug routes rendering ComingSoonRoute, isolated as one labeled, Phase-4-deletable block, verified by router.test.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side route table | All 5 new routes are literal, developer-authored path strings -- no dynamic route construction from user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering/Spoofing | router.tsx's 5 literal routes | low | accept | Routes are hardcoded literal path strings generated from a fixed, developer-authored `DEFERRED_SLUGS` array -- no user-controlled route registration exists |
| T-03-09 | Information Disclosure | 5 deferred-slug routes temporarily publicly reachable/linked | medium | accept | Explicit, user-approved cross-phase tradeoff (D-11): DEPL-03 (Phase 4) requires these routes not be linked/indexed; the user was shown this conflict and chose to accept a temporary linked window rather than ship non-clickable "see more" entries now. Already logged in STATE.md's Blockers/Concerns as a required Phase 4 follow-up -- not this phase's to resolve |
</threat_model>

<verification>
- `npm run test -- src/routes/coming-soon.test.tsx src/router.test.tsx` both pass
- `npm run test` (full suite) still passes with no regressions to `src/routes/case-study.test.tsx` or `src/routes/not-found.test.tsx`
</verification>

<success_criteria>
Every one of the 5 deferred case-study slugs resolves to a distinct, on-brand coming-soon page instead of a 404 or Phase 2's generic "not found" fallback, isolated in one Phase-4-deletable block.
</success_criteria>

<output>
Create `.planning/phases/03-homepage-build/03-04-SUMMARY.md` when done
</output>
