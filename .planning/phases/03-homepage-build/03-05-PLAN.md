---
phase: 03-homepage-build
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - public/resume.pdf
autonomous: false
requirements: [HOME-08]

must_haves:
  truths:
    - "public/resume.pdf exists and is a real, human-verified conversion of Hajra Farhin Resume UX.docx -- not a placeholder path (D-15, status/populated UI Considerations #11, covered once this checkpoint resolves)"
    - "Footer's LinkedIn and Behance links (and optionally Website) use only real, human-confirmed URLs supplied via this plan's checkpoint -- any link the user doesn't yet have is recorded as 'omit', not guessed"
    - statement: "Résumé PDF content visually matches the source .docx -- all sections present, legible"
      verification: backstop
  prohibitions:
    - "MUST NOT fabricate or guess a destination URL for LinkedIn/Behance/Website (or any other social/contact link) when the real value isn't present in any source artifact -- a wrong guess risks linking to the wrong profile or a dead page under Hazra's own name/brand"
  artifacts:
    - public/resume.pdf
  key_links:
    - "This plan's confirmed LinkedIn/Behance/Website URLs -> consumed literally by Plan 03-06's src/content/footer.ts"
    - "public/resume.pdf -> consumed by Plan 03-06's Footer.tsx href=\"/resume.pdf\" (already wired to this real path, not a placeholder)"
---

<objective>
Resolve the two pieces of real-world content this phase cannot fabricate: convert the real résumé `.docx` to a public PDF (D-15), and collect the real LinkedIn/Behance/Website destination URLs the footer needs to "navigate correctly" (CONT-04) -- neither exists anywhere in this repo's source content today.

Purpose: No LibreOffice/pandoc/docx-to-pdf CLI is available in this environment (03-RESEARCH.md Environment Availability), so the résumé conversion is a one-time human action, not an automatable step. Separately, `Homepage Copy V2.md` §12 and the résumé `.docx` both show only the bare labels "LinkedIn"/"Behance"/"Website" with no attached hyperlink anywhere in any source artifact -- inventing a URL risks shipping a broken or, worse, an incorrect third-party link under Hazra's own name. Both are genuine "missing information" gaps (not implementation work), so this plan is checkpoint-only and blocks Plan 03-06 (Footer), which depends on it.
Output: `public/resume.pdf` (real, converted résumé); the confirmed LinkedIn/Behance/Website URLs (or explicit "omit" answers), captured in this session's execution record for Plan 03-06 to consume.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-build/03-CONTEXT.md
@.planning/phases/03-homepage-build/03-RESEARCH.md
@Portfolio-Documentation/Homepage Copy V2.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Convert Hajra Farhin Resume UX.docx to public/resume.pdf (D-15)</name>
  <files>public/resume.pdf</files>
  <read_first>Hajra Farhin Resume UX.docx (repo root), .planning/phases/03-homepage-build/03-RESEARCH.md (Pattern 3, Environment Availability)</read_first>
  <action>Checkpoint task -- the executor makes no code changes here. Pause and hand off to the human: no docx-to-pdf CLI (LibreOffice/pandoc) is installed in this environment, and this is a one-time asset conversion, not a repeatable build step, so it cannot be automated (03-RESEARCH.md's own recommendation: manual conversion is the correct low-maintenance choice, not a fallback of necessity). The human performs the conversion steps in &lt;how-to-verify&gt; and confirms via &lt;resume-signal&gt;.</action>
  <what-built>Nothing automatable here -- no LibreOffice/pandoc/docx-to-pdf CLI is installed in this environment, and this is a one-time asset conversion, not a repeatable build step (03-RESEARCH.md's own recommendation: manual conversion is the correct low-maintenance choice, not a fallback of necessity).</what-built>
  <how-to-verify>
    1. Open `Hajra Farhin Resume UX.docx` (repo root, real content, 11KB) in Microsoft Word or upload it to Google Docs.
    2. Export/Save As PDF -- use "Save As PDF" / "Export as PDF" (NOT File → Print → Save as PDF, which rasterizes the text and breaks copy/search, per 03-RESEARCH.md Pattern 3).
    3. Save the resulting file as `public/resume.pdf` in this repo (create the `public/` directory if it does not already exist -- Vite serves everything under `public/` at the site root unmodified, the same mechanism already serving `public/favicon.svg`).
    4. Open `public/resume.pdf` and visually confirm every section/text from the source `.docx` is present and legible.
  </how-to-verify>
  <verify>
    <automated>test -f public/resume.pdf</automated>
  </verify>
  <resume-signal>Type "done" once public/resume.pdf exists and has been visually verified against the source .docx, or describe any issue</resume-signal>
  <done>public/resume.pdf exists, is a valid non-empty PDF, and has been visually confirmed by the human to match the source .docx.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirm real LinkedIn/Behance/Website URLs for the footer (CONT-04, D-16)</name>
  <files>none -- this task writes no files; its answer is consumed as literal values by Plan 03-06's src/content/footer.ts</files>
  <read_first>Portfolio-Documentation/Homepage Copy V2.md (§12 Footer -- labels only, no URLs present)</read_first>
  <action>Checkpoint task -- the executor makes no code changes here. Ask the question in &lt;decision&gt; and wait for the user's reply; do not guess or fabricate a URL. The confirmed answer (or explicit "omit") becomes the literal input Plan 03-06 uses to populate footer.ts's elsewhere[] array.</action>
  <decision>Real destination URLs for the footer's LinkedIn and Behance links (CONT-04 requires these to "navigate correctly"; optionally a personal Website URL too, per D-16's "Elsewhere" column). No URL for any of these three exists anywhere in this repo's source content -- `Homepage Copy V2.md` §12 and the résumé `.docx` both show only the bare labels "LinkedIn"/"Behance"/"Website" with no attached hyperlink.</decision>
  <context>D-16 locks footer content to Homepage Copy V2.md §12 "as-is," and CONT-04 plus ROADMAP Success Criteria #4 both require the LinkedIn/Behance links to actually navigate correctly. Plan 03-06 (Footer) cannot guess a profile URL -- a wrong guess would ship a broken or incorrect third-party link under Hazra's name, which is worse than omitting the link entirely until it's supplied.</context>
  <options>
    <option id="provide-urls">
      <name>Supply the real URLs now</name>
      <pros>Footer ships fully functional for CONT-04 on the first pass</pros>
      <cons>Requires pasting 1-3 real URLs into this response</cons>
    </option>
    <option id="omit-missing">
      <name>Omit whichever link(s) you don't have yet</name>
      <pros>No broken/guessed links ever ship</pros>
      <cons>Footer's "Elsewhere" column ships with fewer than 3 links until the rest are supplied later (Plan 03-06 renders any omitted item as a plain non-interactive label instead of a live link, never a placeholder href)</cons>
    </option>
  </options>
  <verify>
    <automated>MISSING -- this is a decision checkpoint with no automatable postcondition of its own; Plan 03-06's Footer.test.tsx acceptance criteria verify the recorded answer was honored (no fabricated href in footerContent.elsewhere)</automated>
  </verify>
  <resume-signal>Reply with the real LinkedIn URL and Behance URL (required for CONT-04), and a Website URL if you have one -- or "omit" for any you don't have yet</resume-signal>
  <done>The user has replied with real LinkedIn/Behance/Website URLs and/or explicit "omit" instructions for any they don't have.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human-provided real-world content | The résumé PDF and social URLs are supplied directly by the site owner (Hazra), not derived from any untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Information Disclosure | Hajra Farhin Resume UX.docx source file, committed alongside the derived PDF | low | accept | Résumé content is intentionally public-facing (job-search site); no NDA/sensitive data exists in it. Committing both the source `.docx` and the derived `public/resume.pdf` is a deliberate, documented choice per RESEARCH.md's Security Domain, not an accidental `git add -A` sweep |
</threat_model>

<verification>
- `public/resume.pdf` exists on disk and is a valid, non-empty PDF file
- The confirmed (or "omit") answer for LinkedIn/Behance/Website is captured in this execution's session record for Plan 03-06 to read
</verification>

<success_criteria>
A real, human-verified `public/resume.pdf` exists at a public path, and Plan 03-06 has an explicit, non-fabricated answer (URL or "omit") for each of LinkedIn/Behance/Website before it authors `footer.ts`.
</success_criteria>

<output>
Create `.planning/phases/03-homepage-build/03-05-SUMMARY.md` when done
</output>
