---
phase: 03-homepage-build
plan: 06
type: execute
wave: 2
depends_on: ["03-05"]
files_modified:
  - src/content/footer.ts
  - src/components/home/Footer.tsx
  - src/components/home/Footer.test.tsx
autonomous: true
requirements: [HOME-08, CONT-04]

must_haves:
  truths:
    - "Footer renders a working résumé download link pointing at the real converted /resume.pdf (HOME-08, D-15)"
    - "Footer renders direct email, LinkedIn, and Behance links that navigate correctly, using only real, human-confirmed URLs from Plan 03-05's checkpoint (CONT-04)"
    - "Contact/Brief form UI renders all 3 fields (What are you working on? / Reach me at, Email * / What needs to become clearer?) and the 'Send the Brief →' submit CTA with zero submission handler wired -- CONT-01/CONT-02 remain Phase 4's job (D-07)"
    - "Every target=_blank footer/social link pairs with rel=noopener noreferrer (reverse-tabnabbing mitigation, T-03-01)"
    - "Footer legal links (Privacy Policy, Terms) render as plain non-interactive text since no destination page exists this phase -- resolves UI-SPEC's previously-unresolved nav/error consideration"
  prohibitions:
    - "MUST NOT fabricate or guess a destination URL for LinkedIn/Behance/Website when the real value isn't present -- only render an Elsewhere link that Plan 03-05's checkpoint explicitly confirmed"
  artifacts:
    - src/content/footer.ts
    - src/components/home/Footer.tsx
  key_links:
    - "footer.ts's elsewhere[] -> the exact URLs confirmed in Plan 03-05's checkpoint (never a placeholder)"
    - "Footer.tsx résumé Button href=\"/resume.pdf\" -> public/resume.pdf produced by Plan 03-05"
---

<objective>
Build the combined Contact/Brief + Footer section (HOME-08, CONT-04, D-07/D-15/D-16) -- the static 3-field contact form UI (markup only, no submission handler), the résumé download CTA wired to the real converted PDF, and the 3-column footer (Contact / Elsewhere / Legal) using only the real URLs confirmed in Plan 03-05's checkpoint.

Purpose: This is the phase's one section that depends on real-world content only a human can supply (the social profile URLs), so it must run after Plan 03-05's checkpoint resolves. Building it as its own plan (rather than mixing checkpoint and implementation tasks) keeps the checkpoint isolated per the plan-format's "never mix checkpoint + implementation in one plan" rule.
Output: `src/content/footer.ts`, `src/components/home/Footer.tsx` (+test).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-build/03-CONTEXT.md
@.planning/phases/03-homepage-build/03-UI-SPEC.md
@Portfolio-Documentation/Homepage Copy V2.md
@src/components/case-study/Overview.tsx
@src/components/ui/Button.tsx
@src/components/ui/Typography.tsx
@src/motion/useScrollReveal.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contact/Brief + Footer -- content module + component + test (HOME-08, CONT-04, D-07/D-15/D-16)</name>
  <files>src/content/footer.ts, src/components/home/Footer.tsx, src/components/home/Footer.test.tsx</files>
  <read_first>Portfolio-Documentation/Homepage Copy V2.md (§11 Brief, §12 Footer), .planning/phases/03-homepage-build/03-05-SUMMARY.md (the confirmed LinkedIn/Behance/Website URLs -- or "omit" answers -- from this session's checkpoint), src/components/case-study/Overview.tsx (external-link Button + rel="noopener noreferrer" precedent), .planning/phases/03-homepage-build/03-UI-SPEC.md (Contact/Footer + Security note sections)</read_first>
  <behavior>
    - Form renders 3 fields + submit CTA, zero onSubmit handler
    - Résumé link has href="/resume.pdf" and download attribute
    - Every target=_blank anchor pairs with rel="noopener noreferrer"
    - elsewhere[] contains only checkpoint-confirmed URLs; legal links render as plain text, never an <a>
  </behavior>
  <action>
    Create `src/content/footer.ts` exporting `interface FooterLink { label: string; href: string }`, `interface FooterContent { briefLabel: string; briefIntro: string; formFields: { workingOn: string; emailLabel: string; clarify: string }; submitLabel: string; resumeLabel: string; resumeHref: string; wordmark: string; tagline: string; contactEmail: string; contactEmailHref: string; contactPhone: string; contactLocation: string; elsewhere: FooterLink[]; legal: string[]; bottomLine: string }`. Populate: `briefLabel: 'Brief'`, `briefIntro: "Have a product that needs clearer thinking on screen, for your users or for your team's roadmap? Tell me what's not working yet."`, `formFields: { workingOn: 'What are you working on?', emailLabel: 'Reach me at, Email *', clarify: 'What needs to become clearer?' }`, `submitLabel: 'Send the Brief →'`, `resumeLabel: 'Download Résumé'`, `resumeHref: '/resume.pdf'`, `wordmark: 'Hazra Farhin'`, `tagline: 'UX/UI Designer building interfaces that reduce friction and hold up to business requirements, across FinTech, commerce, dashboards, and whatever comes next.'`, `contactEmail: 'hazrafarhinwork@gmail.com'`, `contactEmailHref: 'mailto:hazrafarhinwork@gmail.com'`, `contactPhone: '+91 93992 18725'`, `contactLocation: 'Bhilai, Chhattisgarh, India (Remote)'`, `legal: ['Privacy Policy', 'Terms']`, `bottomLine: '© 2026 Hazra Farhin · Available for on-site and remote engagements across Europe, the Gulf, and North America'` -- all sourced verbatim from `Homepage Copy V2.md` §11/§12. Populate `elsewhere: []` with ONLY the entries Plan 03-05's checkpoint confirmed as real URLs (e.g. `{ label: 'LinkedIn ↗', href: '<the confirmed LinkedIn URL>' }`, `{ label: 'Behance ↗', href: '<the confirmed Behance URL>' }`, and `{ label: 'Website ↗', href: '<the confirmed Website URL>' }` only if supplied) -- for any of the three the checkpoint recorded as "omit," do NOT add an entry for it at all; never write a placeholder/guessed URL.

    Create `src/components/home/Footer.tsx`: `<section id="contact-footer" ref={ref}>` with `useScrollReveal(ref)`. Top "Brief" block: `<Label>{footerContent.briefLabel}</Label>`, `<Body>{footerContent.briefIntro}</Body>`, then a static `<form>` with NO `onSubmit` handler (D-07): a text `<input>` labeled `formFields.workingOn`, an `<input type="email" required>` labeled `formFields.emailLabel`, a `<textarea>` labeled `formFields.clarify`, styled with `border border-line bg-secondary/55` (no new form-primitive component, native elements + `Label` only), and `<Button variant="primary" type="submit">{footerContent.submitLabel}</Button>` (pass `type="submit"` through `Button`'s `...rest` spread -- `Button.tsx` spreads `...rest` after its own hardcoded `type="button"` on the native `<button>` element, so a `type="submit"` in `rest` overrides it). Then the résumé CTA: `<Button variant="primary" href={footerContent.resumeHref} download>{footerContent.resumeLabel}</Button>` (same-origin, no `target`/`rel` needed). Then the Footer block: wordmark + `<Body>{footerContent.tagline}</Body>`, 3 columns (`grid grid-cols-1 md:grid-cols-3 gap-lg`): Contact column (3 literal items -- `<Button variant="ghost" href={footerContent.contactEmailHref}>{footerContent.contactEmail}</Button>`, `<Body>{footerContent.contactPhone}</Body>`, `<Body>{footerContent.contactLocation}</Body>`); Elsewhere column (`.map()` over `footerContent.elsewhere` -- variable length depending on the checkpoint outcome, each `<Button variant="ghost" href={link.href} target="_blank" rel="noopener noreferrer">{link.label}</Button>`); Legal column (2 literal `<Label>{footerContent.legal[0]}</Label>` / `<Label>{footerContent.legal[1]}</Label>`, plain non-interactive text, NEVER wrapped in an `<a>` or given an `href` -- resolves UI-SPEC's previously-unresolved "no destination route" item as plain text, not a placeholder link). Then `<Body className="text-center">{footerContent.bottomLine}</Body>` at the very bottom.
  </action>
  <verify>
    <automated>npm run test -- src/components/home/Footer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Form renders exactly 3 labeled fields matching `footerContent.formFields.workingOn`, `.emailLabel`, `.clarify`, and a submit control reading `Send the Brief →`; `grep -c "onSubmit=" src/components/home/Footer.tsx` returns 0
    - Résumé control has `href="/resume.pdf"` and a `download` attribute
    - Every `target="_blank"` occurrence in `Footer.tsx` has a `rel="noopener noreferrer"` on the same element (count of `target="_blank"` equals count of `rel="noopener noreferrer"`)
    - `footerContent.legal` entries (`Privacy Policy`, `Terms`) render as plain text elements, excluded from `screen.getAllByRole('link')`
    - Contact email renders as `mailto:hazrafarhinwork@gmail.com`; phone and location render as plain text matching `Homepage Copy V2.md` §12 exactly
    - `footerContent.elsewhere` contains only entries confirmed by Plan 03-05's checkpoint -- zero occurrence of `href="#"`, zero occurrence of a bare unconfirmed domain (e.g. `https://linkedin.com` with no profile path) anywhere in `footer.ts`
  </acceptance_criteria>
  <done>Contact/Footer renders the static 3-field Brief form (no submit handler) plus the full Footer block with a working résumé link, real confirmed social links only, and non-interactive legal text, verified by Footer.test.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Footer.tsx -> third-party origins | LinkedIn, Behance, Website links navigate to external, human-confirmed origins outside this app's control |
| Contact form -> nowhere (this phase) | The 3 form fields collect no data and submit nowhere this phase (D-07); Phase 4 (CONT-01) will add the real submission boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering / Spoofing | Footer.tsx's `target="_blank"` external links (LinkedIn/Behance/Website/résumé if opened in a new tab) | high | mitigate | Every `target="_blank"` anchor pairs with `rel="noopener noreferrer"` -- enforced by this task's own grep-count acceptance criterion; `Button`'s existing `...rest` spread already passes both attributes through without any `Button.tsx` change |
| T-03-11 | Information Disclosure | Contact form fields (D-07, markup only) | low | accept | No submission handler exists this phase -- no data ever leaves the browser; CONT-01 (Phase 4) will own real validation/sanitization when a handler is added |
</threat_model>

<verification>
- `npm run test -- src/components/home/Footer.test.tsx` passes
- `npm run test` (full suite) still passes with no regressions
</verification>

<success_criteria>
Footer ships with a real, working résumé link and only real, human-confirmed social links -- never a guessed or placeholder URL -- plus a fully static, unwired 3-field contact form per D-07. Ready to mount into `home.tsx` by Plan 03-07.
</success_criteria>

<output>
Create `.planning/phases/03-homepage-build/03-06-SUMMARY.md` when done
</output>
